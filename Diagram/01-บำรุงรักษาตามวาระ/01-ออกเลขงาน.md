# ช่วงที่ 1 — ออกเลขงาน (Master Plan)

> ที่มา: `maintainance-yearly/maintannance-yearly.md` (ช่วงที่ 1) · flow: บำรุงรักษาตามวาระ (To-be)
> 💡 prototype จริง **รวม "ชื่อแผน + เลือกรถ (เขต)" เป็นขั้นเดียว และตัด "เกณฑ์ ทรัค/เนต" ออก**

```mermaid
flowchart TB
    S([เริ่ม]) --> A[กบก. สร้างแผนบำรุงรักษา Master Plan]
    A --> B[เลือกเกณฑ์ ทรัค / เนต<br/>ในผังเดิม — prototype ตัดออก]
    B --> C[เลือกรถเข้าแผน กรย. 12 เขต]
    C --> D[ระบบแสดงรายการอะไหล่ที่ต้องบำรุงรักษา<br/>น้ำมัน · ไส้กรอง]
    D --> E[ระบุช่วงเวลา ไทรมาส]
    E --> F[ผบพ. ตรวจอะไหล่ที่จะใช้ในไทรมาส<br/>เตรียมของไว้ล่วงหน้า]
    F --> G[ขออนุมัติเลขงาน]
    G --> H[ตรวจสอบจำนวนรถตามแผน<br/>สถานะ: ไม่ใช้ / รออนุมัติ / โอน]
    H --> Z([ได้เลขงาน → ไปช่วง 2])
```
