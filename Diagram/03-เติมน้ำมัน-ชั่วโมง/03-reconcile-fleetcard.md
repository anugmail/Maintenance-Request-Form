# Flow ตั้งหนี้ + Reconcile Fleet Card รายเดือน (บัญชี/ธุรการ) — mock คร่าวๆ

> ที่มา: `Maintenance-Request-Form/04-Flow-Fuel-and-Hours.md` · แยกมาเพื่อดูเป็นภาพง่าย (GitHub render mermaid ให้อัตโนมัติ)

```mermaid
flowchart TD
    A([ธุรการเลือกเดือนที่จะตั้งหนี้]) --> B[ระบบแสดงตาราง: รายการที่คนขับบันทึก Fleet Card ในเดือนนั้น<br/>เทียบคู่กับ Statement ธนาคาร ทีละแถว]
    B --> C{ผลเทียบต่อแถว}
    C -- ยอดตรงกัน --> D["✅ ตรงกัน — พร้อมตั้งหนี้"]
    C -- ยอดไม่ตรง --> E["⚠️ ยอดไม่ตรง — ต้องตรวจสอบก่อน"]
    C -- มีใน Statement แต่ไม่มีบันทึก --> F["❌ ธุรการต้องติดตามคนขับ (ลืมบันทึก/ใช้บัตรผิดคน)"]
    C -- มีบันทึกแต่ไม่พบใน Statement --> G["❌ อาจเป็น sync ไม่ทัน/ตกหล่นฝั่งธนาคาร — ตรวจสอบ"]
    D --> H[กดยืนยันตั้งหนี้ SAP<br/>เฉพาะแถวที่ตรงกัน]
    E --> I[ธุรการแก้ไข/ติดต่อคนขับ แล้วกลับมาเทียบใหม่]
    F --> I
    G --> I
    H --> J([รายการตั้งหนี้ SAP แล้ว — ปิดรอบเดือนนี้])
```
