# 🧭 Maintain-D Design System

> **เวอร์ชัน:** v0.32 (31 ส.ค. 2569) · **สถานะ:** ใช้กับทุกหน้าใน prototype แล้ว (24/24)
> **ที่มา:** **Figma `EXT_PEA_VMS_v1.0.2_Component`** (`EXT-PEA-T0REUY1W-2026-V102`) — ดึงทั้งไฟล์ผ่าน REST API แล้ว (43 หน้า · 58,331 node) · ค่าที่กำกับ `✔` ใน `tokens.css` **ยืนยันแล้วว่ามีใช้จริงในไลบรารี** · `⚠` = ไม่พบในไลบรารี เราเพิ่มเอง
> **แหล่งที่มา + สถานะการอ่านจาก Figma:** [SOURCES.md](SOURCES.md) — สี ✅ · radius/typography ✅ · **base component เทียบ+จูนตรงไลบรารีแล้ว 12 ส.ค.** (`compare-figma.js` — input/select/textarea/badge/tag/table/checkbox/radio/breadcrumb/ปฏิทิน) · โครงหน้า (.shell/stepper/.sect) อิง screenshot หน้าจริง ไลบรารีไม่มีของเทียบ
> **Style guide:** [index.html](index.html) · **ปุ่มครบทุก variant:** [buttons.html](buttons.html) · **ตัวอย่างหน้าจริง:** [ฟอร์มแจ้งซ่อม (mock)](../mock/Maintenance-Request-Form.html)

---

## 0. 🔒 กฎบังคับสำหรับงานออกแบบทุกชิ้นนับจากนี้

> เจ้าของงานกำหนดไว้ **8 ส.ค. 2569:** *"นับจากนี้ การออกแบบให้อิงจาก design system ด้วย"*

**ก่อนสร้างหน้าใหม่หรือแก้หน้าเดิม ต้องผ่านทั้ง 5 ข้อ**

1. `<link>` **`tokens.css` + `components.css`** เสมอ — ห้ามทำหน้า self-contained ที่นิยาม palette เอง
2. **สี** ใช้ `var(--…)` เท่านั้น · ถ้าไม่มี token ที่ต้องการ **ให้เพิ่มใน `tokens.css` ก่อน** แล้วค่อยเรียกใช้ ห้าม hardcode hex ในหน้าจอ
3. **ไอคอน** ใช้ **Material Symbols Outlined** ผ่าน `<span class="ms">ชื่อไอคอน</span>` — ห้าม inline `<svg>` และห้าม emoji แทนไอคอน UI *(ยกเว้นกราฟ/แผนภาพที่เป็น data-visualization)*
4. **ปุ่ม** ใช้คลาสจาก `components.css` (`.btn` + `.btn-p/.btn-s/.btn-t/.btn-link/.btn-d` + `.btn-sm/lg/xl`) — ห้ามเขียนสไตล์ปุ่มเอง
5. **light เท่านั้น** — ยังไม่มี dark token ในระบบ ห้ามใส่ `prefers-color-scheme:dark` รายหน้า (ถ้าจะมี dark ต้องเพิ่มใน `tokens.css` ก่อนแล้วใช้ทั้งระบบพร้อมกัน)
### ⛔ ขั้นตอนบังคับก่อนแตะงานออกแบบทุกครั้ง (เจ้าของงานสั่ง 12 ส.ค. 2569)

> *"ต่อไปถ้าจะต้องไปแก้หน้านั้น ให้ทุกครั้งที่จะ design มาอ่านจากไฟล์ design system ก่อน ก่อนจะไปกำหนดแบบนั้น"*

**ลำดับนี้ห้ามข้าม — อ่านก่อน แล้วค่อยตัดสิน แล้วค่อยเขียนโค้ด**

| # | ทำอะไร | ที่ไหน |
|---|---|---|
| 1 | อ่านกฎข้อ 0 นี้ + ตารางคอมโพเนนต์ **หัวข้อ 4** (คอลัมน์ "ใช้เมื่อ") | ไฟล์นี้ |
| 2 | เปิดดูคลาสจริงที่มีให้ใช้ | `components.css` |
| 3 | 🔴 **เช็คว่า Figma ของ VMS Plus มี component นี้จริงไหม** — เจ้าของงานสั่ง 25 ส.ค. 2569 ว่า*ทุกครั้งที่สร้าง prototype ให้มาอ่านจากของที่โหลดมา* · ไม่มีในไลบรารี = ถามก่อน อย่าออกแบบเอง | `node design-system/figma-screens.js component <ชื่อ> --deep` |
| 4 | ดูว่าไฟล์ไหนคือแหล่งจริง + ค่าที่อ่านมาแล้ว | [SOURCES.md](SOURCES.md) |
| 5 | ถ้ามีในไลบรารี → **ยกค่าจากไลบรารีมาตรงๆ** ห้ามกะเอง | `FIGMA_SRC=component node design-system/verify-tokens.js` |
| 6 | ถ้าไม่มีในไลบรารี → ทำได้ แต่ต้องเขียนกำกับว่ามาจากไหน และแจ้งเจ้าของงาน | คอมเมนต์ใน `components.css` + SOURCES.md |
| 7 | เขียนเสร็จ **รันตรวจ 3 ตัวก่อนบอกว่าเสร็จ** | `verify-tokens.js` · `compare-figma.js` · `audit-usage.js [เฟส]` |

```bash
node design-system/verify-tokens.js      # ทุก token ต้องตรงไลบรารี + ป้าย ✔/⚠ ต้องตรงความจริง
node design-system/compare-figma.js      # ค่าคอมโพเนนต์เทียบไลบรารีทีละตัว
node design-system/audit-usage.js        # วิธีใช้: ห้ามนิยามซ้ำ/ปุ่มต้อง .btn/ไอคอนต้อง .ms/ขนาดต้องอยู่ในสเกล
```

**ห้ามเริ่มจากการดูหน้าที่มีอยู่แล้วลอกแพตเทิร์น** — หน้าที่มีอยู่อาจผิดเอง (เจอจริง: `.qty` ถูกเขียนทับในหน้า, 3 หน้าวิเคราะห์ไม่ลิงก์ `components.css` เลย)

6. 🔴 **ห้ามคิดเอง** — เจ้าของงานกำหนด **9 ส.ค. 2569:** *"ทำตาม design system ทั้งหมด ห้ามคิดเอง"*
   - **ต้องเปิดอ่าน `design-system/README.md` + `components.css` ก่อนลงมือทุกครั้ง** ห้ามเดาจากแพตเทิร์นของหน้าที่มีอยู่
   - ต้องใช้ **คลาสที่มีอยู่แล้ว** เป็นอันดับแรก · จะเขียน CSS ใหม่ได้ต่อเมื่อ**ยืนยันแล้วว่าไม่มีของเดิมที่ใช้ได้**
   - CSS ที่เขียนใหม่ **ต้องไปอยู่ `components.css`** ไม่ใช่ `<style>` ในหน้า · แล้วเพิ่มแถวในตารางข้อ 4 + Changelog ข้อ 8
   - `<style>` ในหน้าเก็บได้เฉพาะสิ่งที่**เป็นของหน้านั้นจริงๆ ใช้ที่อื่นไม่ได้** และต้องมีคอมเมนต์บอกเหตุผล
   - แก้ `components.css`/`tokens.css` เมื่อไหร่ → **บั๊ม `?v=` ทุกหน้าที่ลิงก์ไฟล์นั้น** (ไม่งั้น browser cache ค้าง)

**ข้อยกเว้นที่อนุญาตให้เป็นค่าตายตัว** (มีเท่านี้ ต้องมีคอมเมนต์กำกับ)
`<meta name="theme-color">` (meta ใช้ `var()` ไม่ได้) · theme preset ใน `config.js`/`config-daily.js` (เป็น**ข้อมูล** ไม่ใช่สไตล์) · rainbow ของ colour picker ใน `admin-config.html` · หน้าเอกสารสีที่ตั้งใจโชว์ hex (`design-system/*.html`) · test harness

**ตรวจว่ายังสะอาดอยู่ไหม**
```bash
# ห้ามใช้ grep -o ตรงนี้ — มันตัดบริบททิ้ง ทำให้ filter บรรทัดถัดไปกรอง theme-color ไม่โดน
grep -rniE '#[0-9a-f]{3,8}\b' --include='*.html' . \
  | grep -viE '#fff|#000|design-system/|config.*\.js|admin-config|theme-color|/test/|backup'
```
```bash
# ห้ามมี emoji แทนไอคอน UI (ข้อ 3) — ต้องได้ผลว่าง
grep -rnP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' --include='*.html' --include='*.js' \
  maintainance-yearly/ mock/ daily-record/ | grep -v '/test/'
```

---

## 1. หลักการ

1. **แหล่งความจริงเดียว (Single Source of Truth)** — ค่าสี/ฟอนต์/ระยะ/รัศมีทั้งหมดอยู่ใน `tokens.css` เท่านั้น
2. **ห้าม hardcode** — ในหน้าจอใช้ `var(--brand-600)` เสมอ ห้ามเขียน `#A80689` ตรงๆ
3. **Component ก่อน หน้าจอทีหลัง** — ถ้าต้องใช้ UI ที่ยังไม่มีในระบบ ให้เพิ่มเป็น component กลางใน `components.css` ก่อน แล้วค่อยเรียกใช้จากหน้าจอ
4. **อัปเดตจากของจริง** — ดึงค่าจาก Figma ไลบรารีจริงเป็นหลัก (read-only เท่านั้น **ห้ามแก้ไฟล์ Figma**) แล้วปรับ token ให้ตรง — แก้ที่เดียว ทุกหน้าเปลี่ยนตาม
5. **token 3 ชั้น** ตามไลบรารีจริง — `primitive` (`--brand-600`, `--gray-*`) → `semantic` (`--color-text-secondary`) → `component` (`--btn-primary-bg`) · หน้าจอควรเรียกชั้น semantic/component ก่อน primitive

## 2. วิธีใช้

ทุกหน้า HTML ใส่ 4 บรรทัดนี้ใน `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Thai:wght@400;500;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,300..600,0..1,0" rel="stylesheet">
<link rel="stylesheet" href="../design-system/tokens.css">
<link rel="stylesheet" href="../design-system/components.css">
```

```css
/* ✅ ถูก */  color: var(--primary-600);
/* ❌ ผิด */  color: #A80689;
```

ไอคอนใช้ Material Symbols ผ่านคลาส `.ms`:

```html
<span class="ms">build</span> <span class="ms">warehouse</span> <span class="ms">event_available</span>
```

## 3. Design Tokens (`tokens.css`)

### 3.1 สี Brand

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--primary-700` | `#8A0570` | active/pressed เข้ม |
| `--primary-600` ★ | `#A80689` | สีหลัก — ปุ่ม, ลิงก์, active state, แถบ section (สีที่ใช้บ่อยสุดใน CSS จริงของ VMS Plus) |
| `--primary-500` | `#CF07AA` | hover, ปลาย gradient |
| `--primary-50` | `#FDEEFC` | พื้นหลังอ่อน chip/highlight |
| `--primary-25` | `#FDF2FA` | พื้นหลังอ่อนสุด (แถวที่เลือก) |
| `--secondary-600` | `#172B85` | น้ำเงินรอง (tile "ส่งซ่อม กบค.") |
| `--secondary-700` | `#15409A` | น้ำเงินเข้ม hover |

### 3.2 สี Semantic (สถานะ)

| กลุ่ม | เข้ม | กลาง | พื้นอ่อน | ใช้กับ |
|---|---|---|---|---|
| Success | `--success-700` `#067647` (ตัวอักษร badge) · `--success-600` `#17B26A` | `--success-200` `#ABEFC6` (ขอบ badge) | `--success-50` `#ECFDF3` | พร้อมเบิก, จองสำเร็จ, เสร็จสิ้น |
| Warning | `--warning-600` `#F79009` · text `--warning-700` `#B54708` | `--warning-200` `#FEDF89` | `--warning-50` `#FFFAEB` | ใกล้หมด, แถบ mock/draft |
| Error | `--error-600` `#D92D20` · `--error-700` `#B42318` | `--error-500` `#F04438` · `--error-200` `#FECDCA` | `--error-50` `#FEF3F2` | หมด/รอของ, validation error |
| Info | `--info-600` `#6172F3` | — | — | ลิงก์/ข้อมูลเสริม |

### 3.3 Gray scale (Untitled UI **v2** — ramp ที่ไลบรารีจริงใช้)

> ⚠️ **v0.1 ของเราเคยใช้ ramp v1** (`#344054` / `#D0D5DD` / `#667085` …) ซึ่งเป็น**คนละชุดกันทั้งแถบ**
> `tokens.css` เปลี่ยนเป็น v2 ตั้งแต่ 7 ส.ค. 2569 แต่ตารางนี้ค้างเป็น v1 อยู่จนถึง 11 ส.ค. — **แก้แล้ว**
> ถ้าเจอ hex ชุด v1 ที่ไหนในโปรเจกต์ แปลว่าเป็นของค้างที่ยังไม่ได้ไล่แก้

| Token | ค่า | ที่มา | ใช้กับ |
|---|---|---|---|
| `--gray-950` | `#0A0D12` | ✔ | เข้มสุด (ยังไม่ได้ใช้ในหน้าจอ) |
| `--gray-900` | `#181D27` | ✔ | หัวข้อ, ตัวเลขสำคัญ (`--color-text-primary`) |
| `--gray-800` | `#252B37` | ✔ | `color/text/secondary-hover` |
| `--gray-700` | `#414651` | ✔ | เนื้อความหลัก (`--color-text-secondary`) |
| `--gray-600` | `#535862` | ✔ | เนื้อความรอง (`--color-text-tertiary`) |
| `--gray-500` | `#717680` | ✔ | ข้อความ secondary, ไอคอนในช่องกรอก |
| `--gray-400` | `#A4A7AE` | ✔ | placeholder, ข้อความจาง (`--color-fg-disabled`) |
| `--gray-300` | `#D5D7DA` | ✔ | เส้นขอบช่องกรอก (`--color-border-primary`) |
| `--gray-200` | `#E9EAEB` | ✔ | เส้นคั่น, ขอบการ์ด (`--color-border-disabled-subtle`) |
| `--gray-100` | `#F5F5F5` | ✔ | พื้น hover, ปุ่มเทา (`--color-bg-disabled`) |
| `--gray-50` | `#FAFAFA` | ✔ | พื้นหลังหน้า (`--color-bg-primary-hover`) |

**ทั้งแถบยืนยันแล้วว่าตรงกับไลบรารี** — เทียบกับทั้งไฟล์เมื่อ 11 ส.ค. 2569 ไม่ใช่แค่หน้า Buttons — ดู [SOURCES.md](SOURCES.md)

### 3.4 Typography

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--font` | `'IBM Plex Sans Thai', sans-serif` | ทั้งระบบ (ตรงกับ VMS Plus) |
| `--fs-h1` | 18px | หัวแอปบนแถบสี |
| `--fs-h2` | 16px | หัวการ์ด |
| `--fs-body` | **16px** (= text-md ไลบรารี) | เนื้อความ, ตัวอักษรในช่องกรอก — *เดิม 15px ไม่มีในสเกลไลบรารี จูนแล้ว 12 ส.ค.* |
| `--fs-sm` | **14px** (= text-sm) | ข้อความรอง/label — *เดิม 13px* |
| `--fs-xs` | **12px** (= text-xs) | stepper, หมายเหตุ, badge — *เดิม 11px* |
| ชื่อหน้า (web) | 28px/700 (`.page-title`) | หัวหน้าแบบหน้าจริง |
| ไอคอน | Material Symbols Outlined (`.ms`) | ทั้งระบบ |

### 3.5 รูปทรงและเอฟเฟกต์

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--rounded-sm` | 6px | tag/chip, กล่อง checkbox (ตามไลบรารี) |
| `--r-sm` / `--rounded-md` | 8px | ปุ่ม, ช่องกรอก |
| `--r-md` | 12px | การ์ดย่อย, รายการ, stepper กล่อง, กรอบตาราง |
| `--r-lg` | 16px | การ์ดหลัก, tile |
| `--r-pill` | 99px | badge, วันในปฏิทิน |
| ช่องกรอก (web) | สูง **44px** · r **8** · pad นอน 14 · `--shadow-xs` | `.f .in input` — ตามไลบรารี Input md (*เดิม 46/r10 ไม่ตรง จูนแล้ว 12 ส.ค.*) |
| `--shadow-xs` | เงา 0 1 2 (#0A0D12 5%) | ช่องกรอก, ตาราง — ตามไลบรารี |
| `--shadow-app` | เงากรอบแอป | `.app` |
| `--shadow-pop` | เงา tile ที่เลือก | `.tile.sel` |
| `--grad-header` | gradient magenta | header มือถือ |

## 4. Components (`components.css`)

### 4.1 โครงหน้าเว็บ (จากหน้าจริง "สร้างคำขอใช้ยานพาหนะ")

| Component | คลาส | ลักษณะ |
|---|---|---|
| โครงหน้า | `.shell` > `.side` + `.work` > `.topbar` + `.content` | sidebar ซ้าย 80px + พื้นที่งาน |
| Sidebar | `.side` `.nv` `.nv.on` | ไอคอนอย่างเดียว, active = พื้นเทา+ไอคอน magenta |
| **Sidebar กางชื่อเมนู** | `.side.wide` + `.nv` > `.ms` + `.nvlbl` | **opt-in ต่อหน้า** — ปกติยังเป็นแถบไอคอน 80px เหมือนเดิม เอาเมาส์ชี้แล้วกางเป็น 280px **ทับเนื้อหา ไม่ดันหน้า** พร้อมชื่อเมนูเต็ม + ฉากหลังมืดเบลอ · **ใช้เมื่อ** เมนูในโฟลว์เยอะจนจำไอคอนไม่ไหว · หน้าที่ไม่ใส่ `.wide` ไม่เปลี่ยนพฤติกรรมเลย · ⚠️ ไลบรารีไม่มีของเทียบ (Application navigation `3:8` เป็น slide-out มือถือ คนละแพตเทิร์น) |
| Topbar | `.topbar` | ขาว, ไอคอนธีม/กระดิ่งชิดขวา |
| Breadcrumb | `.crumbs` `.sep` `.cur` | บ้าน › ระดับกลาง › ปัจจุบัน (magenta) |
| ชื่อหน้า | `.page-title` | 28px หนา |
| Stepper ลูกศร | `.wsteps` > `.wstep` (`.active`/`.passed`) + `.num` `.lbl` | กล่องขาว คั่น chevron ›, ผ่านแล้ว = วงกลม magenta ✓ |
| Stepper หลายกลุ่ม | `.wgrp` + `.wsteps` (`.wrap`) | หัวกลุ่มเทาเล็กเหนือแถว · `.wrap` = ชื่อขั้นยาวให้ตัดบรรทัดแทน … |
| ขั้นที่ยังเข้าไม่ได้ | `.wstep.locked` | จาง + เคอร์เซอร์ห้าม — ต้องคู่กับการกันคลิกในโค้ด ไม่ใช่กันด้วยสไตล์อย่างเดียว |
| **Sub-stepper (ขั้นย่อยในเฟส)** | `.wsteps.sm` > `.wstep` | stepper ย่อขนาดสำหรับ **wizard ที่ซ้อนอยู่ในเฟสของ stepper ใหญ่** · **ใช้เมื่อ** เฟสหนึ่งมีหลายขั้นย่อย (เช่น เฟส 2 แผนเดินทาง = ทำแผน → ทวน+ยืนยัน) · ยกขึ้นมาจาก `<style>` ของ `index.html` + `trip-plan.html` ที่นิยามซ้ำกัน (25 ส.ค. 2569) |
| กล่องจัดกลุ่มรายการ | `.rzone` + `.rzone-head` / `-caret` / `-count` / `-allchk` / `-body` | ลิสต์ยาวที่แบ่งเป็นชั้น เช่น ภาค → จังหวัด → รถ · หัวกล่องกดพับ/กางได้ · `.rzone-body.flush` = ใส่ตารางเต็มความกว้างไม่ต้องมี padding |
| **แกลเลอรีพรีวิวหน้าจอ** | `.gallery` > `.gcard` > `.gframe` + `.gcard-body` | โชว์หน้าจริงย่อส่วนด้วย `<iframe>` + `pointer-events:none` = เห็น UI แต่กดไม่ได้ · ใช้ iframe แทนภาพนิ่งเพราะแก้หน้าไหนพรีวิวอัปเดตเอง |
| **เลือกช่วงวันที่** | `.daterange` > `.daterange-field` + `.cal` | กดเปิดปฏิทิน คลิกวันเริ่ม → คลิกวันสิ้นสุด · ใช้เมื่อต้องระบุ **ช่วง** ไม่ใช่วันเดียว (`<input type="date">` ทำช่วงไม่ได้) · เปิด/ปิดด้วยคลาส `.open` ที่ตัว `.daterange` |
| **คุมระยะแนวตั้ง** | `.stack` (`.tight` / `.loose`) | **ห่อทุกครั้งที่วางคอมโพเนนต์ต่อกันในแนวตั้ง** — ระยะมาจาก `gap` ที่เดียว ไม่ต้องพึ่ง margin ของแต่ละตัวซึ่งไม่สม่ำเสมอ · `.tight` 8px · ปกติ 12px · `.loose` 20px |

> ⚠️ **`.search` เป็นช่องกรอกในฟอร์ม ไม่ใช่หัวลิสต์** — ไม่มี margin ในตัวเพราะออกแบบให้อยู่ใต้ `label` ซึ่งเป็นตัวให้ระยะ
> ถ้าจะวางเหนือลิสต์ **ต้องห่อด้วย `.stack`** ไม่งั้นจะติดกับรายการแรกสนิท 0px (เคยพลาดมาแล้ว 10 ส.ค. 2569)
| ป้ายสถานะบนขั้น | `.wstep` > `.st` > `.ms.done` / `.ms.todo` / `a` | ทำแล้ว (เขียว) · ยังไม่ทำ (เทา) · ลิงก์ไปหน้าจริง |
| หัวข้อ section | `.sect` | แถบตั้ง magenta 4px + ตัวหนา |
| ฟอร์ม grid | `.fgrid` (+ `.sp2` `.sp3` `.sp4` กว้าง 2/3/เต็มแถว) | 4 คอลัมน์ → 2 (≤1100px) → 1 (≤760px) · `.sp3` ใช้คู่กับ `.f` เปล่า ได้อัตราส่วน 3:1 (ช่องข้อความยาว + ช่องตัวเลขสั้น) · ที่ ≤1100px `.sp3` กลายเป็นเต็มแถวเอง กันคอลัมน์เกิน |
| ช่องกรอก | `.f` > `label` + `.in` > `.ms` + `input` | label หนาอยู่บน, ไอคอนนำหน้าในช่อง |
| ช่อง readonly | `.f.ro` | เส้นประ พื้นเทาอ่อน (ข้อมูลดึงอัตโนมัติ) |
| ช่อง error | `.f.err` + `.help` | ขอบแดง + ข้อความแดงใต้ช่อง — ครอบ `input` / `textarea` / `.daterange-field` |
| Radio | `.rads` > `label` > `input[type=radio]` | accent magenta |
| ช่องจำนวน | `.numfld` | − ตัวเลข + |
| แถวปุ่มท้ายฟอร์ม | `.actions` | desktop ชิดขวา / มือถือตรึงขอบล่าง |

```html
<div class="sect">ข้อมูลรถ</div>
<div class="fgrid">
  <div class="f sp2">
    <label>ค้นหารถที่มีปัญหา</label>
    <div class="in"><span class="ms">search</span><input type="text" placeholder="ค้นหาทะเบียน…"></div>
  </div>
  <div class="f ro">
    <label>สังกัด</label>
    <div class="in"><span class="ms">apartment</span><input value="กฟภ. เขต ฉ.3" readonly></div>
  </div>
  <div class="f err">
    <label>เบอร์ภายใน</label>
    <div class="in"><span class="ms">call</span><input type="text" placeholder="ระบุเบอร์ภายใน"></div>
    <div class="help">กรุณาระบุเบอร์ภายใน</div>
  </div>
</div>
```

### 4.2 Component ทั่วไป

| Component | คลาส | ใช้เมื่อ |
|---|---|---|
| ปุ่มหลัก | `.btn.btn-p` | action หลักของหน้า (ส่งเรื่อง, ถัดไป) |
| ปุ่มรอง (outline) | `.btn.btn-o` | action รอง (จองอะไหล่, แจ้งเรื่องใหม่) |
| ปุ่มเทา | `.btn.btn-g` | ย้อนกลับ/ยกเลิก |
| ปุ่มปิดใช้ | `.btn:disabled` | ทำไม่ได้ + บอกเหตุผลในตัวปุ่ม |
| Badge สถานะ | `.badge` + `.b-ok`/`.b-low`/`.b-out`/`.b-brand` | สถานะคลัง/สถานะเรื่อง — ตามไลบรารี Badge md: 14/500 + **เส้นขอบโทน 200** + ตัวอักษรโทน 700 (แบรนด์ = brand-500) |
| Tabs (เส้นใต้) | `.tabs` > `.tab-btn` (`.on`) | สลับกลุ่มข้อมูลในกล่องเดียวกัน (เช่น รายละเอียดการซ่อม / ประวัติการซ่อม) — ⚠️ ยังไม่ได้เทียบกับไลบรารี Tabs `3:19` ตรงๆ (ดู SOURCES.md หมวด ง) |
| หน้ารายละเอียดเหตุการณ์ | `.incident-head` + `.incident-layout` (`.incident-summary`/`.incident-card`/`.incident-contact-group`) | หน้ารายละเอียดงานซ่อมแบบเต็มหน้า 2 คอลัมน์ — ส่วนหลักแสดงเหตุการณ์และรูป ส่วนข้างแสดงรถ ผู้แจ้ง และผู้ติดต่อ · อิงไฟล์ออกแบบ `Incident detail` วันที่ 25 ส.ค. 2569 |
| Modal | `.modal-overlay` > `.modal` (`.modal-head`, `.modal-close`, `.modal-foot`) | ป๊อปอัปทับหน้าจอ (เช่น ยืนยันรับซ่อม/ส่งกลับ) — `.modal-foot` = แถวปุ่ม action ชิดขวา มีเส้นคั่นด้านบน — ⚠️ ยังไม่ได้เทียบกับไลบรารี Modal `3:9` ตรงๆ (ดู SOURCES.md หมวด ง) |
| Chip (เลือกหลายอัน) | `.chips.pick` > `.chip` (`.sel` · `[aria-disabled=true]`) | อาการเสีย, วัน/เวลาที่เปิดว่าง — ตามไลบรารี **Tag lg + Checkbox=True**: ป้าย r6 · เส้น 1px gray-300 · 14/500 · กล่องติ๊ก 18×18 r4 (`::before`) · ติ๊กแล้ว = พื้น brand-600 + check ขาว · **hover ตอนยังไม่ติ๊กไม่เปลี่ยนสี** (ไลบรารีไม่มี state นี้) · ติ๊กแล้ว hover = brand-800 · disabled = พื้น gray-50 + ตัวถูกจาง |
| **Checkbox list (เลือกหลายอัน)** | `.chks` > `label` (`.sel`) > `input[type=checkbox]` (ซ่อน) + `.cbox` | ตัวเลือกข้อความล้วนที่มีหลายรายการ เช่น **อาการเสีย** — ไม่มีกรอบป้ายล้อม อ่านง่ายกว่าเมื่อรายการยาว · กล่อง 20×20 · r6 ตาม `_Checkbox base` md ของไลบรารี · **ต่างจาก `.chips.pick`** ที่เป็น Tag มีกล่องติ๊กอยู่ข้างใน (ยังใช้กับตารางเลือกวัน/เวลา) |
| **Radio card (เลือกอันเดียว)** | `.radcards` (`.cols-3`) > `.radcard` (`.sel` · `[aria-disabled=true]`) > `.rdot` + `.rc-ic` (ไอคอน · ไม่บังคับ) + `.rc-tx` · ใส่ `<input type=radio>` จริงข้างในได้ (ซ่อนไว้ให้เอง) | ตัวเลือก 2–6 อันที่มีคำอธิบายกำกับและเลือกได้อันเดียว เช่น **งบที่จะตัด** ขั้น 3 ของฟอร์มแจ้งซ่อม · โครงการ์ดเหมือน `.veh` (r-md · 1.5px gray-200 · hover primary-500 · เลือกแล้ว primary-600 + primary-25) · จุดวิทยุตามไลบรารี `_Radio button base` 16×16 · **ต่างจาก `.rads`** ที่เป็นวิทยุบรรทัดเดียวไม่มีกรอบ และ **ต่างจาก `.chips.pick`** ที่เลือกได้หลายอัน |
| **Vehicle detail** | `.vehicle-detail-card` > `-header`/`-icon`/`-title` + `.vehicle-selected-badge` · `-grid`/`-item`/`-label`/`-value` · `.vehicle-target` (+ `-legend`/`-required`/`-help`) | การ์ดยืนยัน “รถที่เลือก” ในขั้น 1 ของฟอร์มแจ้งซ่อม — หัวการ์ด + ข้อมูลรถ 3 ช่อง + กลุ่มเลือก **จุดที่พบปัญหา** (ใช้ `.radcards` ข้างใน) · ชื่อคลาสตั้งตาม component `Vehicle detail` ของไฟล์ PEA (ยังไม่มี geometry ให้เทียบ) |
| **กล่องรายการเลื่อนได้** | `.parts-scroll` (ครอบกล่องรายการใน `.parts-pane`) | รายการยาวจนดันหน้าให้ยืด แต่คอลัมน์ข้าง ๆ ยังสั้น — สูงสุด `min(60vh,560px)` แล้วเลื่อนในตัวเอง · **ครอบเฉพาะกล่องรายการ** ไม่ครอบหัวข้อ/ช่องค้นหา (ค้นหาจึงตรึงอยู่ด้านบน) · ใช้ที่ “คลังอะไหล่” ของขั้นอะไหล่ในฟอร์มแจ้งซ่อม |
| Segmented (เลือกอันเดียว) | `.seg` > `.sg` (`.sel`) | ตัวเลือกสั้นๆ 2–4 ตัว |
| ตัวปรับจำนวน | `.qty` | เพิ่ม/ลดจำนวนจอง |
| Tile เลือกใหญ่ | `.tile` + `.tile-magenta`/`.tile-blue` (`.sel`) | ทางเลือกใหญ่ 2 ทาง (ซ่อมเอง/ส่ง กบค.) |
| แถวรายการ | `.job` > `.sp` (`.no`/`.meta`) (`.sel`) | แถวเรื่อง/รายการในลิสต์ — ใส่ `.sel` เมื่อใช้เป็น**รายการให้เลือก** (เช่น เลือกช่างจากรายชื่อ) · ⚠️ **ลิสต์งานที่มีหลายคอลัมน์ให้ใช้ตาราง (แถวถัดไป) ไม่ใช่ `.job`** |
| **หน้ารายการ (list page)** | `.list-toolbar.split` > `.lt-search` + `.lt-actions` → `.tblwrap` > `.tbl` → `.tblfoot` | **ลิสต์งานที่ต้องเทียบหลายค่าในแถวเดียว** (เลขที่ · รถ · หน่วยงาน · วันที่ · สถานะ) — โครงเดียวกับหน้ารายการจริงของ VMS Plus · กดแถวแล้ว**ไปหน้ารายละเอียดเต็มหน้า** ไม่กางในลิสต์ |
| หัวตารางกดเรียง | `.tbl th.sortable` (`.on`) > `button` > `.ms` | คอลัมน์ที่เรียงได้ — ทั้งช่องเป็นปุ่ม · ไอคอน `swap_vert` (ยังไม่เรียง) → `arrow_upward`/`arrow_downward` (เรียงอยู่ + ใส่ `.on` ที่ `th`) · **ห้ามใส่ `.btn`** ปุ่มในหัวจะสูง 40 มีขอบ/เงาซ้อน |
| ค่าหลักในเซลล์ | `.tbl .cell-key` | เลขที่ใบงาน/รหัสอ้างอิงในคอลัมน์แรก — เน้นให้กวาดตาหาเจอ (คู่กับ `.cell-sub` ที่เป็นบรรทัดรอง) |
| ตัดข้อความในเซลล์ | `.tbl .cell-clip` | ค่ายาวกว่าคอลัมน์ (ชื่อหน่วยงาน · ชื่อคน · รุ่นรถ) → ตัดด้วย … บรรทัดเดียว **ต้องใส่ `title="ค่าเต็ม"` เสมอ** |
| สถานะแบบข้อความสี | `.tbl .cell-state` + `.ok`/`.no` | สถานะที่มีแค่ **2 ค่าตรงข้าม** (ใช้งานได้/ไม่ได้) — ข้อความสีล้วน ไม่มีพื้น ไม่มีขอบ · **ต่างจาก `.badge`** ที่เป็น pill มีกรอบ ใช้กับสถานะที่มีหลายค่า · อิง Figma หน้า **Repair & Maintain** `A1.1 Incident list` ที่ใช้ทั้งสองแบบในตารางเดียวกัน |
| แผงตัวกรอง | `.filter-panel` (`.open`) > `.filter-field` + ปุ่มล้าง | เงื่อนไขกรองของหน้ารายการ — **กางจากปุ่ม "ตัวกรอง" (`.btn.btn-s` + `filter_list`) บน `.lt-actions`** ห้ามวาง `<select>` เปลือยบนแถบ · ตอนแผงปิดต้องมี `.badge` นับเงื่อนไขบนปุ่ม ไม่งั้นลิสต์สั้นลงโดยผู้ใช้ไม่รู้สาเหตุ |
| Filter drawer | `.filter-panel.filter-drawer` (`.open`) > `.filter-drawer-sheet` | ตัวกรองหลายเงื่อนไขแบบแผงเลื่อนเต็มความสูงด้านขวา มี overlay, header, body และ footer · อิง `Filter.svg` วันที่ 25 ส.ค. 2569 |
| หน้าเบิกอะไหล่ | `.parts-workspace` > `.parts-pane` + `.parts-stock-*`/`.parts-picked-*` | หน้าเลือกอะไหล่แบบ 2 คอลัมน์ รองรับไม่พบข้อมูล, พบของในคลัง, เลือก/กรอกอะไหล่และสรุปราคา · อิง `Screen (1)–(3)` วันที่ 25 ส.ค. 2569 |
| แถบท้ายตาราง | `.tblfoot` > `.tf-left` (+ `.select-inline`) + `.pager` | "แสดง x ถึง y จาก z รายการ" + จำนวนแถวต่อหน้า ชิดซ้าย · เลขหน้าชิดขวา |
| select ในแถบ | `.select-inline` | select ที่วางในแถบเครื่องมือ/แถบท้าย **ไม่มี label ของตัวเอง** สูง 40 เท่าปุ่ม กว้างตามเนื้อหา — ต่างจาก `.filter-field` ที่มี label กำกับด้านบน |
| การ์ด | `.card` (+ `h2`, `.sub`) | กล่องเนื้อหาบนพื้นหน้า |
| Toast | `.toast` (`.show`) | แจ้งผลชั่วคราว ~2.5 วิ |
| แถบ mock/draft | `.draft` | ติดบนสุดของ prototype ทุกหน้า |
| อื่นๆ | `.hidden` `.empty` `.search` | utility |

### 4.3 โครงหน้าแบบมือถือ (legacy — ใช้กับ demo แอปมือถือ)

`.app` + `header` (gradient magenta) + `.steps`/`.st`/`.dot` (stepper แบบจุด) + `.footer` — ยังอยู่ใน components.css สำหรับหน้าจอสไตล์ mobile-app; งานใหม่แนะนำใช้โครง `.shell` (4.1) ที่ตรงกับหน้าจริง

## 5. กติกาเฉพาะโดเมน (ระบบซ่อมบำรุง)

### สถานะคลังอะไหล่ (ตาม US-07)

| สถานะ | เงื่อนไข (mock) | Badge |
|---|---|---|
| 🟢 พร้อมเบิก | คงเหลือ > 3 | `.b-ok` เขียว |
| 🟡 ใกล้หมด | คงเหลือ 1–3 *(เกณฑ์จริงรอคำตอบลูกค้า — คำถามเปิด #3 ของ BL01)* | `.b-low` ส้ม |
| 🔴 หมด | คงเหลือ 0 | `.b-out` แดง |
| 🔴 หมด-รอของ | คงเหลือ 0 + มี ETA | `.b-out` แดง + "รอของ (ETA …)" |

- Badge ต้องอ่านรู้เรื่องโดยไม่ต้องอธิบาย: ใส่ทั้งคำ + จำนวน เช่น "พร้อมเบิก · เหลือ 6"
- แสดงเวลาอัปเดตข้อมูลคลังกำกับเสมอ · ปุ่มจองใช้ได้เฉพาะคงเหลือ > 0

### ระดับความรุนแรง

`ต่ำ / ปานกลาง / สูง / หยุดใช้งาน` — ใช้ radio (`.rads`)

## 6. Responsive

| ช่วงจอ | พฤติกรรม |
|---|---|
| > 1100px | ฟอร์ม 4 คอลัมน์, sidebar แสดง, ปุ่มชิดขวา |
| 761–1100px | ฟอร์มลดเหลือ 2 คอลัมน์ |
| ≤ 760px | sidebar ซ่อน, ฟอร์ม 1 คอลัมน์, stepper ย่อ (เลข + label เฉพาะขั้นปัจจุบัน), ปุ่มตรึงขอบล่าง (`.actions`), ชื่อหน้า 22px |

## 7. โครงไฟล์ + การอัปเดต

```
design-system/
├── README.md          ← เอกสารนี้
├── tokens.css         ← ค่าสี/ฟอนต์/รัศมี (แก้ที่นี่ที่เดียว)
├── components.css     ← component + pattern + responsive
└── index.html         ← style guide เปิดดู/เทียบกับ screenshot จริง
```

**ขั้นตอนเมื่อได้ screenshot หน้าจริงเพิ่ม:**

1. เปิด [index.html](index.html) เทียบข้างกันกับ screenshot
2. ต่างที่ *ค่า* (สี/ระยะ/ฟอนต์) → แก้ `tokens.css`
3. ต่างที่ *รูปแบบ* หรือเจอ component ใหม่ (ตาราง, modal, dropdown, tab ฯลฯ) → เพิ่มใน `components.css` + เพิ่มตัวอย่างใน `index.html` + เพิ่มแถวในตารางข้อ 4
4. บันทึกลง Changelog ด้านล่าง

## 8. Changelog

| วันที่ | เวอร์ชัน | สิ่งที่เปลี่ยน |
|---|---|---|
| 1 ก.ย. 2569 | v0.43–0.44 | 🔴 **แก้ stepper ให้ตรงของจริง หลังเจ้าของงานทักว่า “ใน design เป็นแบบนี้หรอ”** — กลับไปอ่าน**หน้าจอจริง** `Create vehicle (Step 1)` ในไฟล์ `(UI) VMS Plus – Release#2` แล้วพบว่า `Progress steps` เป็น **กล่องเดียว r8 · ขอบ 1px #D0D5DD · พื้นขาว** ครอบทุกขั้น (v0.41–0.42 ทำเป็นวงกลมลอยไม่มีกรอบ = ผิด) · และตัวคั่นคือ **VECTOR 20×72 เส้น 1px** = ทรง **chevron “›”** ไม่ใช่เส้นตรง (v0.43 เดาเป็นเส้นแบ่งแนวตั้ง = ผิดอีก) — ตรงกับเว็บ VMS Plus ตัวจริงที่เจ้าของงานส่งมาเทียบ · ค่าที่ใช้: ขั้นละ pad ซ้าย-ขวา 24 บน-ล่าง 16 · gap 16 · วงกลม **40px** ขอบ 1px (ขั้นปัจจุบัน #A80689) · ข้อความ **16/SemiBold** · chevron เอียง 29° (= atan 20/36 ตามสัดส่วน 20×72) · ข้อความขึ้นได้ 2 บรรทัดตามไลบรารี · บั๊ม `?v=20260901-ds44` |
| 1 ก.ย. 2569 | v0.42 | **แก้ stepper ที่เพิ่งยกมา (v0.41) แล้วดูขาด ๆ** — เดิมวาง connector เป็น `::before` ยาวคงที่ 20px ชิดซ้ายของขั้นถัดไป พอความกว้างจริงไม่ใช่ 300 ตามไลบรารี เส้นเลยลอยไม่เชื่อมวงกลม · แก้เป็น **`::after` ที่ยืดเต็มช่องว่างที่เหลือ** (`flex:1 1 20px` · ขั้นต่ำ 20px ตามไลบรารี) วางต่อท้ายข้อความของแต่ละขั้นเหมือนโครง `Content + Connector` ของไลบรารี · จัดวงกลมกับข้อความให้อยู่กึ่งกลางแนวตั้ง (`align-items:center`) แทนการดัน `padding-top` · บั๊ม `?v=20260901-ds42` ทุกหน้า |
| 1 ก.ย. 2569 | v0.41 | 🔴 **ยก 3 คอมโพเนนต์ที่เคยเป็นทรงของเราเองให้ตรงไลบรารีใหม่** (เจ้าของงานเคาะ) — **Stepper** เลิกใช้กล่องคั่น chevron → `Horizontal step item`: วงกลม 40 ขอบ 1px (ยังไม่ถึง #D0D5DD/เลข #98A2B3 · ขั้นนี้ #A80689 · ผ่านแล้วพื้น #A80689 + ถูกขาว) · ข้อความ 16/SemiBold · เส้น connector 20px · **`.sect`** ถอดแถบ magenta นำหน้า → หัวข้อ **20/SemiBold** + **เส้นคั่นล่าง 1px #D0D5DD** ตาม `Section header` · `--fs-h1` 18→**20px** (18 ไม่มีในสเกลไทยของไลบรารีใหม่ 12/14/16/20/32) · **การ์ดเลือกรถ `.veh`** เลิกเป็นแถวเตี้ยไอคอนซ้าย → `Card/select vehicle`: การ์ด r16 · pad 12 · gap 16 · ขอบ #EAECF0 · ช่องรูป r12 (ไม่มีรูปจริง ⇒ พื้น gray-100 + ไอคอนรถ) · ชื่อ 16/SemiBold · บรรทัดรอง 14/Regular · เลือกแล้วขอบ 2px แบรนด์ · **การตัดสินใจ (ซ่อมเอง/ส่ง กรย.)** เปลี่ยนจาก `.rads` เป็น `.radcards` ชุดเดียวกับ “งบที่จะตัด” · บั๊ม `?v=20260901-ds41` ทุกหน้า |
| 1 ก.ย. 2569 | v0.40 | 🔴 **ไล่จูนคอมโพเนนต์ทั้งชุดตามไลบรารีใหม่ `(Component) VMS Plus`** — อ่านค่าจริงด้วย `design-system/lib-spec.js` (สคริปต์ใหม่) แล้วแก้ตามทีละตัว: **token 18 ตัว** ที่ไลบรารีใหม่ไม่มีค่าเดิม (brand-25/50/100/200 · gray-25/800/900/950 · success-200/700 · info-100 ฯลฯ) แก้เป็นค่าจริง/ติดป้าย ⚠ ที่ไม่มีคู่ · **ปุ่ม** pad ซ้ายขวา 16→12 · gap 8→4 · `btn-lg` 44→48 · **hover primary เปลี่ยนจากเข้มลง (#74045F) เป็นสว่างขึ้น (#CF07AA)** ตามไลบรารี · secondary hover พื้น #EAECF0 · tertiary hover พื้น #F2F4F7 · **ช่องกรอก/ดรอปดาวน์** h44→**40** pad 14→12 (textarea 12/14→8/12) · **Badge** `.b-ok` พื้น success-25→**#ECFDF3** · `.b-neutral` พื้น/ขอบ →#F9FAFB/#EAECF0 · **แท็บ** ข้อความ →#475467 · hover มีพื้น #EAECF0 · **Breadcrumbs** 14/400→**12/Regular** สี →#475467 · **Pagination** เลิกเชื่อมกรอบเป็นแถบเดียว → ปุ่มละ **40×40 · r4** · hover/active พื้น #EAECF0 + r8 · **Alert** pad 12/14→12 ทุกด้าน gap 10→12 · **Nav button** 48→**40×40** · hover พื้น #EAECF0 · **Modal footer** gap →12 · **Checkbox ติ๊กแล้ว hover ไม่เข้มลง** (ไลบรารีใช้สีเดิม) · บั๊ม `?v=20260901-ds40` ทุกหน้า |
| 1 ก.ย. 2569 | v0.39 | 🔴 **จูนตามไลบรารีตัวใหม่ `(Component) VMS Plus`** (แหล่งเดียวที่ใช้ตั้งแต่นี้ไป) — อ่านค่าจริงจาก 3 หน้า: `Checkbox` · `Radio button` · `Radio text card` · เพิ่ม token **`--rounded-xs:4px`** (ไลบรารีใช้ 1,585 ครั้ง) · **Checkbox** r6→**r4** เส้น 1px→**2px** ไอคอนถูก 14→18px + state disabled · **จุดวิทยุ** 16→**20px** เส้น **2px** จุดใน 6→**8px** · **Radio card** r12→**r8** ขอบ gray-200→**gray-300 1px** · **เลือกแล้วเปลี่ยนจาก “ขอบชมพู+พื้นชมพูอ่อน” เป็น “เส้น 2px ชมพู พื้นขาว”** ตามไลบรารี · hover ขอบ `--brand-500` (#CF07AA) · padding 12/14→**15** (เลือกแล้ว 14 ชดเชยเส้นหนา) gap 10→**12** · บั๊ม `?v=20260901-ds39` ทุกหน้า |
| 1 ก.ย. 2569 | v0.38 | เพิ่ม **`.chks`** (checkbox list) แล้วเปลี่ยน **“อาการเสีย”** จาก `.chips.pick` (Tag + กล่องติ๊ก) มาใช้แบบนี้ — เจ้าของงานทักว่ากล่องติ๊กในป้ายดูใหญ่เกินตัวป้าย (กล่อง 18 ในป้ายสูง 30 ⇒ เหลือที่ว่างแค่ 20) · แบบใหม่ = **กล่อง 20×20 r6 ตาม `_Checkbox base` md** + ข้อความ 16/24 ไม่มีกรอบล้อม · ใช้ `<input type=checkbox>` จริงซ่อนไว้เพื่อ a11y · `.chips.pick` ยังอยู่ ใช้กับตารางเลือกวัน/เวลาเหมือนเดิม · บั๊ม `?v=20260901-ds38` ทุกหน้า |
| 1 ก.ย. 2569 | v0.37 | **ยกการ์ด “รายละเอียดรถที่เลือก” ออกจาก `<style>` ของ mock** (21 คลาส `vehicle-*` · ผิดกฎข้อ 0) มาเป็นคอมโพเนนต์ **Vehicle detail** ใน `components.css` + แปลงขนาดตัวอักษรที่ hardcode เป็น token · **ตัวเลือก “จุดที่พบปัญหา” เปลี่ยนมาใช้ `.radcards > .radcard`** ให้เป็นภาษาเดียวกับ “งบที่จะตัด” และ “คัดแยกช่องทางซ่อม กรย.” (เดิมเป็นการ์ดของตัวเองที่ใช้ `check_circle` ทางขวา) · ขยาย radio card: เพิ่ม **`.rc-ic`** (ไอคอนนำหน้า) + รองรับ **`<input type=radio>` จริง** ที่ซ่อนไว้เพื่อ a11y/คีย์บอร์ด (`:has(input:focus-visible)` = วงแหวนโฟกัส) · บั๊ม `?v=20260901-ds37` ทุกหน้า |
| 1 ก.ย. 2569 | v0.36 | เพิ่ม **`.radcards.cols-3`** (3 คอลัมน์ · ยุบเหลือ 1 คอลัมน์ที่ ≤900px) แล้วเปลี่ยน **ปุ่มคัดแยกช่องทางซ่อมของ กรย.** (ซ่อมที่อู่ · กรย. ซ่อมเอง · ส่งต่อ กบค.) จาก `.tile` สีทึบ 3 ใบ มาเป็น **radio card** ตามที่เจ้าของงานสั่ง — พฤติกรรมเหมือนเดิม (เลือกแล้วกางแผงกรอกข้อมูลด้านล่าง) · ถอด CSS `.kry-route-options` ที่เคยเขียนไว้ใน `<style>` ของ mock ออก (ผิดกฎข้อ 0) · บั๊ม `?v=20260901-ds36` ทุกหน้า |
| 1 ก.ย. 2569 | v0.35 | เพิ่ม **`.parts-scroll`** — modifier ให้กล่องรายการใน `.parts-pane` เลื่อนในตัวเองแทนที่จะยืดยาวลงไป (`max-height:min(60vh,560px)` + `overscroll-behavior:contain`) · เกิดจากคอลัมน์ **คลังอะไหล่** ของขั้นอะไหล่ที่โชว์อะไหล่ทั้งคลัง ⇒ ยาวกว่าคอลัมน์ขวามาก · บั๊ม `?v=20260901-ds35` ทุกหน้า |
| 1 ก.ย. 2569 | v0.34 | เพิ่ม **`.radcards` / `.radcard`** — ยกการ์ด “งบที่จะตัด” ออกจาก `<style>` ของ `mock/Maintenance-Request-Form.html` (เดิมชื่อ `.ctlist` / `.ctopt` / `.ct-tx` · ผิดกฎข้อ 0) · เจ้าของงานสั่งให้ยึดแบบเดียวกับขั้น 1–3 ของฟอร์ม ⇒ โครงการ์ดใช้ค่าเดียวกับ `.veh` และ **จุดวิทยุยกตามไลบรารี `_Radio button base`** (`589:206913`): 16×16 · เส้น 1px gray-300 · เลือกแล้วพื้น primary-600 + จุดในขาว 6px · disabled พื้น gray-50 (ของเดิมวาดเอง 17px เส้น 2px แล้วพอเลือกดันเป็นเส้น 5px ซึ่งไลบรารีไม่มี) · เพิ่มตัวอย่างข้อ 8.1 ใน `index.html` · บั๊ม `?v=20260901-ds34` ทุกหน้า |
| 1 ก.ย. 2569 | v0.33 | **จูน state ของกล่องติ๊กใน `.chips.pick` ให้ครบตามไลบรารี `_Tag checkbox`** (node `1:1378` — 24 variant `Checked × Size × State`) · ขนาด/สี/รัศมีเดิมวัดแล้วตรงสเปก **Tag lg + Checkbox=True** อยู่แล้ว (18×18 · r4 · ขอบ gray-300 · ติ๊ก brand-600 + check 14) จึงไม่แตะ · ที่เพิ่ม/แก้: **ถอด hover ที่ทำให้ขอบเข้มขึ้นตอนยังไม่ติ๊ก** (ไลบรารี `Checked=False,State=Hover` ใช้สีเดียวกับ Default) · เพิ่ม **`.sel:hover` = brand-800** (`#74045F` ชุดเดียวกับปุ่ม primary hover) · เพิ่ม **`[aria-disabled=true]`** พื้น gray-50 ขอบ gray-300 เครื่องหมายถูก gray-400 · บั๊ม `?v=20260901-ds33` ทุกหน้า |
| 31 ส.ค. 2569 | v0.32 | เพิ่ม **`.tbl .cell-state`** (`.ok`/`.no`) — สถานะแบบข้อความสีล้วนสำหรับค่าที่มีแค่ 2 ทางเลือกตรงข้าม · เกิดจากหน้า **จัดการงานซ่อม** ที่อิง Figma `Repair & Maintain / A1.1 Incident list` ซึ่งใช้ **ข้อความสี** กับคอลัมน์ *สถานะรถ* และใช้ **`.badge` pill** กับคอลัมน์ *สถานะเหตุการณ์* ในตารางเดียวกัน ⇒ เป็นคนละ component ตามดีไซน์ ไม่ใช่ variant ของกัน · บั๊ม `?v=20260831-ds32` ทุกหน้า (รวม 3 หน้าที่ค้างเวอร์ชันเก่ามาก่อน: `approve-close` · `design-system/buttons` · `design-system/index`) |
| 25 ส.ค. 2569 | v0.31 | เปลี่ยน Empty state หน้าเบิกอะไหล่จาก Material icon เป็นภาพประกอบจริงที่ฝังอยู่ใน `Screen (1).svg` และกำหนดขนาดผ่าน `.parts-empty-image` · บั๊ม `?v=20260825-ds31` ทุกหน้า |
| 25 ส.ค. 2569 | v0.30 | ช่องกรอกอะไหล่นอกระบบไม่ถูก seed ล่วงหน้าอีกต่อไป — แสดงเฉพาะเมื่อกด “ระบุอะไหล่เพิ่มเติม” · จูนช่องว่างระหว่าง picked list กับปุ่มด้วย `--space-2/4` · บั๊ม `?v=20260825-ds30` ทุกหน้า |
| 25 ส.ค. 2569 | v0.29 | จูนหน้าเบิกอะไหล่ตามพิกัด SVG: เพิ่ม header status/action และ tabs 3 รายการ, ทำ search เต็มคอลัมน์, จูนระยะ stepper/panels และสี locked state ให้ตรง `Screen (1)–(3)` · บั๊ม `?v=20260825-ds29` ทุกหน้า |
| 25 ส.ค. 2569 | v0.28 | ปรับหน้าเบิกอะไหล่ให้รองรับ 3 state ตาม `Screen (1)–(3)`: ไม่พบข้อมูล, พบรายการในคลัง, เลือก/กรอกอะไหล่พร้อมสรุปราคา · เพิ่ม `.parts-*` และ `?case=1|2|3` สำหรับตรวจแต่ละกรณี · บั๊ม `?v=20260825-ds28` ทุกหน้า |
| 25 ส.ค. 2569 | v0.27 | เพิ่ม filter drawer ด้านขวาตาม `Filter.svg` และการ์ดเลือกวิธีซ่อมใน modal ตาม `Select contract type.svg` · ปรับตัวกรองคิวงาน กบค. ให้กรองสถานะ ยี่ห้อ รุ่น สภาพรถ และวันที่ได้จริง · บั๊ม `?v=20260825-ds27` ทุกหน้า |
| 25 ส.ค. 2569 | v0.26 | เพิ่มชุด `.incident-*` สำหรับหน้ารายละเอียดเหตุการณ์ตามไฟล์ออกแบบ `Incident detail` — header action, layout 2 คอลัมน์, summary, gallery, vehicle/person/contact cards และ responsive · ปรับหน้า “งานซ่อม กบค. > รายละเอียด” ให้ใช้ชุดนี้ พร้อมบั๊ม `?v=20260825-ds26` ทุกหน้า |
| 25 ส.ค. 2569 | v0.25 | **รวมสองสายที่บั๊มเป็น `ds24` พร้อมกันคนละเนื้อ** — `8ea0704` (check before repair) เพิ่ม `.btn-block` · `.page-title-row` · `.ml-auto` · `.mb-*` และแก้ `.crumbs`/`.stack` ส่วนสายนี้ยก stepper ขึ้นมา ⇒ `components.css` ตอนนี้มีของทั้งสองฝั่ง จึงตั้งเป็น **v0.25** และบั๊ม `?v=20260825-ds25` ให้ทุกหน้าตรงกัน (ก่อนหน้านี้ `tokens.css` ค้างที่ `ds23` ส่วน `components.css` เป็น `ds24` — ไม่ตรงกัน) |
| 25 ส.ค. 2569 | v0.24 | ยก **`.wstep{cursor:pointer}` · `.wstep.locked` · `.wsteps.sm` (sub-stepper)** ขึ้นมาจาก `<style>` ของหน้า — `index.html` กับ `trip-plan.html` นิยามชุดเดียวกันซ้ำกันคนละที่ ผิดกฎข้อ 0 (`audit-usage.js` เฟส 3 จับได้ 7+6 จุด เหลือ 0) · **README v0.7 เคยบันทึกว่ายก `.wstep.locked` ขึ้นมาแล้วตั้งแต่ 10 ส.ค. แต่ของจริงยังค้างอยู่ในหน้า** — รอบนี้ยกจริง · เกิดจากงานแยกขั้น "ทำแผนการเดินทาง" ออกเป็นหน้าเดี่ยว ซึ่งไปก๊อป `<style>` ชุดนี้มาเป็นสำเนาที่สอง · บั๊ม `?v=20260825-ds24` ทุกหน้า |
| 20 ส.ค. 2569 | v0.23 | เพิ่ม **`.sp3`** (กว้าง 3 ใน 4 คอลัมน์ของ `.fgrid`) — เกิดจากฟอร์มพนักงาน กบค. ในแผนเดินทางที่ต้องการ **ช่องชื่อยาว + ช่องตัวเลขสั้น** ในแถวเดียว ซึ่ง `.sp2`+`.sp2` (50/50) ให้ช่องตัวเลขกว้างเกินจำเป็น · ที่ ≤1100px กริดเหลือ 2 คอลัมน์ `span 3` จะดันคอลัมน์เกิน จึง override เป็น `1/-1` เต็มแถวแทน · บั๊ม `?v=20260820-ds23` ทุกหน้า |
| 20 ส.ค. 2569 | v0.22 | เพิ่ม **`.side.wide` + `.nvlbl`** — sidebar กางตอน hover พร้อมชื่อเมนูเต็ม (80px → 280px ทับเนื้อหาด้วย `margin-right` ติดลบ จึงไม่ดันหน้า · ฉากหลังมืด+เบลอผ่าน `.shell:has(>.side.wide)::after` · badge `.cnt` ย้ายมาชิดขวาของแถวตอนกาง) · **เป็น variant opt-in ต่อหน้า ไม่ใช่ค่า default** — เปิดเฉพาะ 6 หน้าโฟลว์บำรุงรักษาประจำปี (`maintainance-yearly/`) เพราะเมนู 6 ตัวจำไอคอนไม่ไหว · หน้าอื่นที่ใช้ `.side` เปล่ายังเป็นแถบไอคอน 80px เท่าเดิมทุกประการ · ชุดนี้เคยเป็นพฤติกรรมของ `.side` **ทุกหน้า** ใน `fdb704c` แล้วถอดทั้งชุดใน `3eac9ff` ตอนจูนให้ตรง runtime — รอบนี้ยกกลับมาโดยผูกกับคลาส ตามที่เจ้าของงานสั่ง · ⚠️ ไลบรารีไม่มีของเทียบ (`3:8` เป็น slide-out มือถือ คนละแพตเทิร์น ดู SOURCES.md หมวด ข/ค) · เพิ่ม `text-decoration:none` ที่ `.side .nv` — เป็น `<a>` จึงติดขีดเส้นใต้ค่าเริ่มต้นของเบราว์เซอร์ ตอนเป็นไอคอนล้วนมองไม่ออก แต่พอมีชื่อเมนูแล้วเห็นชัด · แก้ตารางข้อ 4.1 ที่ยังเขียนว่า sidebar 96px (ของจริง 80px ตั้งแต่ v0.18) · บั๊ม `?v=20260820-ds22` ทุกหน้า |
| 17 ส.ค. 2569 | v0.21 | **ถอด `.tbl tr.vrow-blocked` ออก** — เจ้าของงานเปลี่ยนกติกาตอนบ่ายเป็น *"ตอนเลือก ทุกสถานะสามารถเลือกได้ แล้วคนสร้างแผนค่อยมาเลือกออก"* ⇒ ไม่มีแถวที่ถูกล็อกอีก สถานะกลายเป็นข้อมูลประกอบการตัดสินใจ (ป้าย + คำเตือนใต้ป้าย) ไม่ใช่ตัวกั้น · เก็บ `.cell-sub` ไว้ใช้แสดงคำเตือน · บั๊ม `?v=20260817-ds21` ทุกหน้า |
| 17 ส.ค. 2569 | v0.20 | เพิ่ม **`.note`** + `.note-warn`/`.note-ok`/`.note-info` — กล่องบอกสถานะ/เงื่อนไขของหน้า (ไอคอน + ข้อความ · โทนสีชุดเดียวกับ `.badge` ให้ความหมายสีทั้งระบบตรงกัน) **ใช้เมื่อ** ต้องบอกว่า *"ตอนนี้ยังทำอะไรไม่ได้ และต้องทำอะไรก่อน"* หรือยืนยันว่าครบแล้ว — ต่างจาก `.draft` (แถบคาดหัวบอกว่าเป็น mock) และ `.sub` (คำโปรยใต้หัวข้อ) · เพิ่ม **`.seg .sg .sg-sub`** บรรทัดรองในปุ่มแถบ (ช่วงเดือนใต้ชื่อไตรมาส) · เพิ่ม **`.worknos`/`.workno`** แถวเลขงานหลายใบของงานเดียว (ออกพร้อมกัน ต้องเห็นทุกเลขเท่าๆ กัน ไม่มีตัวหลักตัวรอง) · เกิดจากหน้าทำแผนที่ต้องบอกว่ายังขาดไตรมาสไหน และออกเลขงานทีเดียว 4 ใบ · บั๊ม `?v=20260817-ds20` ทุกหน้า |
| 17 ส.ค. 2569 | v0.19 | เพิ่ม **`.tbl .cell-sub`** — บรรทัดรองในเซลล์ (12/18 gray-500) สำหรับค่าขยายความของค่าหลัก เช่น ยี่ห้อใต้ชนิดรถ · หน่วยงานใต้จังหวัด · เหตุผลใต้ป้ายสถานะ · ก่อนหน้านี้เขียน `style="font-size:12px;color:…"` ซ้ำในหน้า ⇒ ขนาดหลุดสเกล · **ต่างจาก `.sub`** ที่เป็นคำโปรยใต้หัวข้อ section (14px + margin ล่าง) ห้ามใช้สลับกัน · ~~`.tbl tr.vrow-blocked`~~ **ถอดออกแล้วใน v0.21** — เจ้าของงานเปลี่ยนกติกาเป็น "เลือกได้ทุกสถานะ" จึงไม่มีแถวที่ถูกล็อกอีก · บั๊ม `?v=20260817-ds19` ทุกหน้า |
| 17 ส.ค. 2569 | v0.18 | **ยกชุดให้ตรง VMS Plus ตัวจริง (สาขา `design/vmsplus-runtime-alignment` เข้า main)** — ค่าทั้งหมดวัดจาก `vmsplus-dev` ไม่ได้เดาจากไลบรารีอย่างเดียว · **โครงหน้าพื้นขาว** `body`/`.shell`/`.work` = `#fff` (เดิม `body` `#E9EAEC` · `.work` `--gray-25` ⇒ หน้าจอออกเทา) · หัวเรื่อง 32/600 · sidebar 96→**80px** · gray ramp กลับเป็น Untitled UI **v1** · `--color-text-primary` gray-900→`#000` · **ตาราง** ถอดกรอบ/radius/เงารอบตาราง · หัวพื้น gray-100 14/600 สูง 56 · เซลล์ pad 8/16 กลางแนวตั้ง · **badge** 12/600 pad 4/8 (สูง 28) + `.b-info`/`.b-neutral`/`.badge .dot` · `--r-pill` 99→9999px · **`.search`** เป็นกล่องจริง h40 (เดิม wrapper ไอคอนลอย) · **ฐานปุ่ม** คุมสูง 40 ตรงๆ + `--shadow-xs` · เพิ่ม `.btn-tb`/`.btn-td`/`.btn-icon`/`.btn-circle`/`.pager`/`.tbl .dt-action` · `.tab-btn` สูง 48 พื้นขาว · เทียบ element ต่อ element 17/20 ตรงทุกค่า (เดิม 0) · บั๊ม `?v=20260817-ds18` ทุกหน้า |
| 14 ส.ค. 2569 | v0.17 | **`.f.err` ครอบ `.search input` เพิ่ม** — ช่องค้นหาที่ใช้เป็น*ตัวเลือกรายการ* ต้องขึ้นกรอบแดงได้เหมือนช่องกรอกอื่น · **เพิ่ม `.job.sel`** — state "เลือกแล้ว" ของ `.job` ตอนใช้เป็นรายการให้เลือก โทนเดียวกับ `.sg.sel`/`.chip.sel` (ขอบ primary-600 พื้น primary-25) · เกิดจากตัวเลือกรายชื่อช่างใน `design-mock/kbk-self-repair-appointment.html` (ค้นหาชื่อ/รหัส/หน่วยงาน → เลือกแล้ว auto fill เบอร์โทร) · บั๊ม `?v=20260814-ds17` ทุกหน้า |
| 14 ส.ค. 2569 | v0.16 | `.f.err` ครอบ `.daterange-field` ด้วย (เดิมครอบแค่ `.in input`/`textarea` ⇒ ช่องเลือกช่วงวันที่ขึ้นกรอบแดงไม่ได้) · เกิดจากหน้า `design-mock/kbk-self-repair-appointment.html` ที่ทำ validate ตอนกด "ส่งคำนัดให้เจ้าของรถ" — ช่องที่ยังไม่กรอกขึ้นกรอบแดง + `.help` "กรุณากรอก{ชื่อหัวข้อ}" · บั๊ม `?v=20260814-ds16` ทุกหน้า |
| 13 ส.ค. 2569 | v0.15 | เพิ่ม `.modal-foot` (แถวปุ่ม action ชิดขวา เส้นคั่นบน gray-200 · gap/margin ตามสเกล `--space-*` เดียวกับ `.rowbtns`) · หน้า `mock/Maintenance-Request-Form.html` modal "ส่งกลับ/ปฏิเสธ" เพิ่มปุ่ม "ยกเลิก" (`.btn-g`) + "ส่งกลับให้ผู้สร้างใบแจ้งซ่อม" (`.btn-p`) พร้อม validate ต้องกรอกเหตุผลก่อนส่ง (`submitKbkReturn`) · ⚠️ ยังอิง Modal `3:9` แบบเดิม ไม่มี `.figma-extract/` ให้เทียบ footer โดยตรง (ดู SOURCES.md หมวด ง) · บั๊ม `?v=20260813-ds15` ทุกหน้า |
| 13 ส.ค. 2569 | v0.14 | เพิ่ม `.modal-overlay` > `.modal` (`.modal-head`/`.modal-close`) — overlay มืด+เบลอแบบ `.shell::after` การ์ด r-lg + shadow-pop · หน้า `mock/Maintenance-Request-Form.html` เพิ่มปุ่ม "ส่งกลับ/ปฏิเสธ" + "รับซ่อม" ท้ายแท็บ "รายละเอียดการซ่อม" (แยกจากการ์ดเลือกเดิมด้านล่างของหน้า) กด "รับซ่อม" เปิด modal ว่าง (ยังไม่ใส่เนื้อหา — รอออกแบบฟอร์มต่อ) · ⚠️ ไลบรารีมี Modal `3:9` แต่เครื่องนี้ไม่มี `.figma-extract/` ให้เทียบ — ค่ายังไม่ผ่าน `compare-figma.js` ต้องเทียบซ้ำภายหลัง (ดู SOURCES.md หมวด ง) · บั๊ม `?v=20260813-ds14` ทุกหน้า |
| 13 ส.ค. 2569 | v0.13 | เพิ่ม `.tabs` > `.tab-btn` (`.on`) — เส้นใต้ 14/600 gray-500→primary-600 · หน้า `mock/Maintenance-Request-Form.html` ใช้แยกกล่องข้อมูลใบแจ้งซ่อม กบค. เป็น "รายละเอียดการซ่อม" / "ประวัติการซ่อม" (แท็บประวัติแสดงตารางประวัติซ่อมย้อนหลังในที่เลย ไม่ต้องกดลิงก์ออกไปหน้าแยกแล้ว) · เปลี่ยนหัวข้อ "ประวัติสถานะ" → "สถานะใบงาน" ให้สื่อความหมายชัดขึ้น (ไม่ปนกับประวัติการซ่อม) · ⚠️ ไลบรารีมี Tabs `3:19` แต่เครื่องนี้ไม่มี `.figma-extract/` ให้เทียบ — ค่ายังไม่ผ่าน `compare-figma.js` ต้องเทียบซ้ำภายหลัง (ดู SOURCES.md หมวด ง) · บั๊ม `?v=20260813-ds13` ทุกหน้า |
| 12 ส.ค. 2569 | v0.12 | **เทียบ base component กับไลบรารีทั้งชุดแล้วจูนให้ตรง** (`compare-figma.js` อ่านจาก `.figma-extract/` — คำสั่งเจ้าของงาน "ยึดตาม design ห้ามกำหนดเอง เช็คทั้งโปรเจ็ค ยกเว้น admin") · ช่องกรอก/select/numfld/daterange 46→**44** r10→**8** pad 12→14 + `--shadow-xs` · focus = ขอบ brand-500 + วงแหวน · label 600→**500** gray-900→**gray-700** ห่าง 6 · error border → `--error-300` `#FDA29B` (ใหม่ ✔) · `.f.ro` เส้นประ→ทึบ (= Disabled) · **badge มีเส้นขอบโทน 200** ตัวโทน 700 14/500 (`--success-200` แก้เป็น `#ABEFC6` ✔) · chip = Tag lg r6 (`--rounded-sm` 6px ใหม่ ✔) · ตาราง: หัวขาว 12/600 gray-500 pad 12/24 · เซลล์ pad 16/20 เส้น gray-200 · breadcrumb 14/600 · checkbox/radio 20px ข้อความ 16/500 · ปฏิทินวัน 40px กลม · `--fs-body/sm/xs` 15/13/11→**16/14/12** (สเกลไลบรารี) + กวาดขนาดเศษ (13.5/12.5/14.5/11.5) ทั้งโปรเจ็ค 299 จุด 21 ไฟล์ · `.draft`→`--warning-800` · `.work`→`--gray-25` · บั๊ม `?v=20260812-ds12` ทุกหน้า |
| 17 ก.ค. 2569 | v0.1 | เริ่มระบบ: tokens + component พื้นฐาน จาก CSS สาธารณะ + หน้า landing/SSO ของ VMS Plus |
| 17 ก.ค. 2569 | v0.2 | เพิ่ม pattern หน้าเว็บจาก screenshot หน้าจริง "สร้างคำขอใช้ยานพาหนะ": `.shell/.side/.topbar/.crumbs/.page-title/.wsteps/.sect/.fgrid/.f(.ro/.err)/.rads/.numfld/.actions` — mock ฟอร์มแจ้งซ่อมย้ายมาใช้โครงนี้ |
| 17 ก.ค. 2569 | v0.3 | เพิ่ม component สำหรับ flow กบค./ติดตามสถานะ: `.nv .cnt` (badge ตัวเลขบน sidebar) · `.job` (แถวรายการเรื่อง) · `.tl` (timeline ประวัติสถานะ) · `.chk` + checkbox (checklist ตรวจสภาพ) |
| 17 ก.ค. 2569 | v0.5 | เพิ่ม `.tbl` + `.tblwrap` (ตารางข้อมูล — เมนูคลังอะไหล่) — ตัดออกจากรายการ "สิ่งที่ยังขาด" ได้ |
| 11 ส.ค. 2569 | v0.11 | **`tokens.css` v0.3 — เทียบสีกับไลบรารี Figma ทั้งไฟล์** (ดึงผ่าน REST API 43 หน้า) · แก้ `--secondary-600` `#1B4DB1`→`#172B85` (ผิดคนละสี) · `--brand-50/-25/-200` · เพิ่ม `--brand-100` `--success-700/-500/-100` `--chart-1..6` · `#067647` ที่ค้างใน `buttons.html` เปลี่ยนมาใช้ `--success-700` · provenance เหลือ `✔` 40 · `⚠` 6 (ไม่มี `~` แล้ว) · บั๊ม `?v=20260811-ds11` |
| 11 ส.ค. 2569 | v0.10 | เพิ่ม `.gallery` — แกลเลอรีพรีวิวหน้าจอด้วย iframe ย่อส่วนที่กดไม่ได้ ใช้กับ `design-mock/index.html` · บั๊ม `?v=20260811-ds10` |
| 11 ส.ค. 2569 | v0.9 | เพิ่ม `.daterange` + `.cal` — ปฏิทินเลือก**ช่วงวันที่**ในคลิกเดียว (คลิกวันเริ่ม → วันสิ้นสุด) · เกิดจากหน้านัดหมายวันซ่อมที่ต้องเสนอเป็นช่วงให้อีกฝ่ายเลือกวันเอง ซึ่ง `<input type="date">` ทำไม่ได้ · บั๊ม `?v=20260811-ds09` |
| 11 ส.ค. 2569 | v0.8 | เพิ่ม `.stack` — คุมระยะแนวตั้งที่ตัวครอบแทนการพึ่ง margin ของแต่ละคอมโพเนนต์ · เกิดจากบั๊กจริง: `.search` วางเหนือลิสต์แล้วติดกันสนิท 0px เพราะ `.search` ไม่มี margin ส่วน `.job` มีแต่ margin ล่าง · เพิ่มคำเตือนเรื่อง `.search` ในตารางข้อ 4 |
| 10 ส.ค. 2569 | v0.7 | ยก `.rzone*` (กล่องจัดกลุ่มรายการ) ขึ้นมาจาก `<style>` ของ `index.html` **และ** `plan-new.html` ที่นิยามไว้ซ้ำกันทั้งสองไฟล์ — หน้า "ยืนยันรถเข้าร่วมแผน" ต้องใช้ชุดเดียวกันเพื่อแบ่ง ภาค → จังหวัด · เพิ่ม `.rzone-body.flush` และ `.wstep.locked` ที่เดิมมีแต่คลาสไม่มีสไตล์ · บั๊ม `?v=20260810-ds07` |
| 9 ส.ค. 2569 | v0.6 | เพิ่ม `.wgrp` + `.wsteps.wrap` + `.wstep .st` (stepper หลายกลุ่ม + ป้ายสถานะบนขั้น) — เกิดจากหน้า `plan-skeleton` ที่มี 11 หน้าจอ 3 กลุ่มในหน้าเดียว · **ครั้งแรกที่โดนจับได้ว่าเขียน CSS คอมโพเนนต์ในหน้าแทนที่จะเพิ่มในระบบก่อน** — ย้ายกลับมาที่นี่แล้ว |

## 9. สิ่งที่ยังขาด (รอ screenshot / รอตัดสินใจ)

- [x] ~~ตาราง list (เช่น หน้า request-list)~~ — **ทำแล้ว v0.22** จาก screenshot หน้า "งานแจ้งซ่อม" ที่เจ้าของงานส่งให้ 18 ส.ค. 2569 (ไม่ต้องรอสิทธิ์ 401 แล้ว)
- [x] ~~Pagination, Table sort~~ — **ทำแล้ว v0.22** (`.tblfoot` + `.pager` · `.tbl th.sortable`)
- [ ] Modal / Dialog ยืนยัน
- [ ] Dropdown/Select แบบเปิด, DatePicker, TimePicker
- [ ] Tab — เทียบกับไลบรารี Tabs `3:19` ตรงๆ (ตอนนี้อิง screenshot)
- [ ] **แผงตัวกรองแบบป๊อปอัป** — หน้าจริงมีปุ่ม "ตัวกรอง" เปิดแผงหลายเงื่อนไข ของเรายังเป็น `<select>` สถานะตัวเดียวใน `.lt-actions`
- [ ] Dark theme (topbar หน้าจริงมีปุ่มสลับธีม — ยังไม่รู้หน้าตาโหมดมืด)
- [ ] เกณฑ์ "ใกล้หมด" จริงจากลูกค้า (ตอนนี้ mock ≤3)
