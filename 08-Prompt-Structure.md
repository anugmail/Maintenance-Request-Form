# 🧭 โครงสร้างการเขียน Prompt สำหรับทำ Prototype หน้าเว็บ

> เขียนขึ้นจากบริบทจริงของ repo นี้ (Maintain-D / ฟอร์มแจ้งซ่อม) — ใช้เป็นแม่แบบเวลาสั่งงาน AI ให้ทำหรือแก้หน้าจอ

---

## 0. หลักคิด: prompt ไม่ต้องยาว ถ้าแบ่ง 3 ชั้นถูก

ปัญหาที่เจอบ่อยคือพยายามยัดทุกอย่างลง prompt เดียว → ยาว อ่านยาก แต่ยังหลุด
วิธีที่ได้ผลกว่าคือแยกความรู้เป็น 3 ชั้น แล้ว prompt พูดเฉพาะชั้นที่ 3

| ชั้น | อยู่ที่ไหน | เนื้อหา | เปลี่ยนบ่อยแค่ไหน |
|---|---|---|---|
| ① กติกาถาวร | `CLAUDE.md` | design system, ห้าม hardcode hex, คำสั่ง verify, กติกา commit | แทบไม่เปลี่ยน |
| ② ความจริงของงาน | `02-Flow-and-Mock-Data.md`, `03-Flow-*.md`, `Diagram/` | flow, สถานะ, บทบาท, mock data, ข้อตัดสินใจ | เปลี่ยนเมื่อเจ้าของงานเคาะใหม่ |
| ③ งานครั้งนี้ | **prompt** | จะทำอะไร หน้าไหน เพิ่ม/แก้อะไร เสร็จแล้ววัดยังไง | ทุกครั้ง |

**กติกา:** ถ้าข้อมูลใช้ซ้ำได้ในงานหน้า → ไปอยู่ชั้น ① หรือ ② ไม่ใช่ใน prompt
prompt ที่ดีจึงมักสั้น แต่ **ชี้ไฟล์** ว่าให้ไปอ่านอะไรก่อนลงมือ

---

## 1. โครงสร้าง prompt 9 บล็อก

เรียงตามลำดับนี้เสมอ ข้อไหนไม่มีให้เขียนว่า "ไม่มี" ดีกว่าเว้นว่าง

```
① CONTEXT   — งานนี้อยู่ตรงไหนของระบบ ใครใช้ ใช้ตอนไหน
② READ FIRST — ไฟล์ที่ต้องอ่านก่อนลงมือ (สำคัญที่สุดสำหรับ repo ที่มีเอกสารเยอะ)
③ GOAL      — ผลลัพธ์ที่ต้องการ 1–3 บรรทัด เขียนเป็นผลลัพธ์ ไม่ใช่วิธีทำ
④ SCOPE     — ทำอะไร / ไม่ทำอะไร (out of scope สำคัญพอๆ กับ in scope)
⑤ SCREEN    — รายละเอียดหน้าจอ: โครง, ฟิลด์, ปุ่ม, state, ข้อความจริง
⑥ DATA      — mock data ที่ต้องใช้ อ้างจากไฟล์ไหน หรือแนบมาเลย
⑦ RULES     — ข้อจำกัดทางเทคนิค + กติกาที่ห้ามฝ่าฝืน
⑧ DONE      — เกณฑ์ว่าเสร็จ ที่ "ตรวจได้" ไม่ใช่ "ดูดี"
⑨ ORDER     — ลำดับการทำงาน + จุดที่ต้องหยุดถามก่อน
```

### รายละเอียดแต่ละบล็อก

**① CONTEXT** — 2–4 บรรทัดพอ ตอบ 3 คำถาม: ระบบอะไร · ใครเป็นคนใช้หน้านี้ · เขาเปิดหน้านี้ตอนไหนของ flow
> ตัวอย่าง: *"หน้านี้อยู่ในโหมด 'งานซ่อม กบค.' ของ mock ผู้ใช้คือเจ้าหน้าที่ กบค. ที่รับใบแจ้งซ่อมเข้ามาแล้ว เปิดหน้านี้หลังจากประเมินความคุ้มค่าเสร็จ"*

**② READ FIRST** — บล็อกที่คนลืมบ่อยที่สุด และเป็นตัวตัดสินว่างานจะออกมาถูกหรือมั่ว
ระบุเป็นรายการไฟล์ + บอกว่าอ่านเพื่ออะไร
> ```
> อ่านก่อนลงมือ:
> - CLAUDE.md            → กติกา design system (บังคับ)
> - design-system/README.md หัวข้อ 4 → ตารางคอมโพเนนต์ + คอลัมน์ "ใช้เมื่อ"
> - 03-Flow-KBK-Pickup-Inspection.md → flow ฝั่ง กบค. ที่หน้านี้ต้องตรงตาม
> - mock/Maintenance-Request-Form.html บรรทัด ~1964-2010 → view งานซ่อม กบค. ที่มีอยู่
> ห้ามเดาแพตเทิร์นจากหน้าอื่นโดยไม่เปิดไฟล์เหล่านี้
> ```

**③ GOAL** — เขียนเป็นสิ่งที่ผู้ใช้ทำได้หลังงานเสร็จ ไม่ใช่รายการงานที่ต้องเขียน
> ❌ "เพิ่ม modal + ปุ่ม + ตาราง"
> ✅ "เจ้าหน้าที่ กบค. เปลี่ยนสถานะงานซ่อมจาก 'รอสั่งอะไหล่' เป็น 'อะไหล่ถึงแล้ว' ได้ พร้อมบันทึกวันที่ของถึง และเห็นการเปลี่ยนใน timeline ของใบงาน"

**④ SCOPE** — ตารางสองคอลัมน์ชัดที่สุด
> | ทำ | ไม่ทำ |
> |---|---|
> | UI + state ใน localStorage | ต่อ API จริง |
> | สถานะ 5 ขั้นตาม KBK_STEPS | แก้ flow ซ่อมเอง (SELF_STEPS) |
> | mock ไม่เกิน 8 ใบงาน | เพิ่ม role ใหม่ |

**⑤ SCREEN** — ให้ละเอียดระดับ "ตั้งชื่อฟิลด์และข้อความปุ่มจริง" เพราะ AI จะแต่งเองถ้าไม่บอก ครอบคลุม:
- โครง: เต็มหน้า / modal / bottom sheet · มีกี่ section
- ฟิลด์: ชื่อ · ชนิด · required · validate · ค่าเริ่มต้น
- ปุ่ม: ข้อความบนปุ่ม · กดแล้วไปไหน · disable เมื่อไหร่
- **สถานะพิเศษ** (บล็อกที่ prototype มักขาด): ว่าง (empty) · กำลังโหลด · ผิดพลาด · ไม่มีสิทธิ์ · ข้อมูลเยอะเกิน (pagination)
- responsive: จอเล็กแล้วอะไรยุบ

**⑥ DATA** — ชี้ไฟล์ mock ที่มีอยู่ก่อนเสมอ อย่าให้ AI แต่งข้อมูลใหม่ทับของเดิม
> *"ใช้รถ 4 คันและ mapping อาการ→อะไหล่ จาก `02-Flow-and-Mock-Data.md` เท่านั้น ห้ามสร้างทะเบียนใหม่ · ถ้าต้องมีข้อมูลที่ยังไม่มีในเอกสาร ให้หยุดถามก่อน"*

**⑦ RULES** — เขียนเฉพาะที่เกี่ยวกับงานนี้ ส่วนที่เป็นกติกาถาวรให้อ้าง `CLAUDE.md` พอ
> *"static HTML ล้วน ไม่มี build step · state แยก key ต่อโหมด · สีใช้ var() เท่านั้น · ไอคอน Material Symbols ห้าม emoji · ทดสอบผ่าน http://127.0.0.1:8123 ห้าม file://"*

**⑧ DONE** — ต้อง "รันแล้ววัดได้" ไม่ใช่ความรู้สึก
> ```
> เสร็จเมื่อ:
> 1. node design-system/verify-tokens.js  ผ่าน
> 2. node design-system/audit-usage.js    ไม่มี error
> 3. grep hex/emoji ทั้งสองคำสั่งใน CLAUDE.md ได้ผลว่าง
> 4. flow-regression.js เดินครบขั้นแล้วผ่าน
> 5. เดินมือ: เปิดใบงาน → เปลี่ยนสถานะ → รีโหลด → ค่ายังอยู่ · console ไม่มี pageerror
> ```

**⑨ ORDER** — บอกลำดับ + จุดหยุด เพื่อกันงานบานและกันออกแบบเอง
> ```
> 1. อ่านไฟล์ในข้อ ② แล้วสรุปให้ดูก่อน: จะใช้ component อะไรบ้าง · แตะไฟล์ไหนบ้าง · assumption อะไรบ้าง
> 2. รอเคาะ แล้วค่อยเขียนโค้ด
> 3. ถ้าเจอของที่ไม่มีในไลบรารี → หยุดถาม ห้ามออกแบบเอง
> 4. เสร็จแล้วรันเช็คทั้ง 5 ข้อ แล้วรายงานผลจริง (ไม่ใช่บอกว่าน่าจะผ่าน)
> ```

---

## 2. ตัวอย่างเต็ม — ถ้าจะสั่งทำ "หน้าแจ้งซ่อม" ใหม่ตั้งแต่ต้น

```markdown
## CONTEXT
ระบบซ่อมบำรุงรถ Maintain-D (อิงดีไซน์ VMS Plus ของ กฟภ.)
ผู้ใช้: พนักงานผู้ครอบครองรถ/ผู้ขับ ที่รถหรืออุปกรณ์เสริมเสียหน้างาน
เปิดหน้านี้เป็นจุดเริ่มของ flow ทั้งหมด ผลลัพธ์คือใบแจ้งซ่อม 1 ใบเข้าสู่ระบบ

## READ FIRST
- CLAUDE.md (กติกาบังคับทั้งหมด)
- design-system/README.md หัวข้อ 0 และ 4
- design-system/components.css (คลาสที่มีให้ใช้จริง)
- 00-README.md (ขอบเขต + ข้อตัดสินใจ 17 ก.ค. 2569)
- 02-Flow-and-Mock-Data.md (flow + mock data ที่ต้องใช้)
ยังไม่ต้องเขียนโค้ดจนกว่าจะอ่านครบ

## GOAL
ผู้แจ้งเปิดฟอร์ม เดินครบ 4 ขั้น แล้วได้เลขที่ใบแจ้งซ่อม MTD-YYMMDD-XXX
โดยเห็นอะไหล่ที่ระบบแนะนำ + ของคงเหลือ "ก่อน" ตัดสินใจว่าจะซ่อมเองหรือส่ง กบค.

## SCOPE
ทำ: wizard 4 ขั้น (vehicle → symptom → parts → decision) ในโหมด "แจ้งซ่อม",
    การจองอะไหล่แบบ mock, สรุป + ส่งเรื่อง, ออกเลขที่ใบ
ไม่ทำ: การเบิก/ตัดสต็อกจริง, routing ตามสังกัด (fix ปลายทาง = กบค.),
      หน้าฝั่ง กบค., ระบบ login, backend

## SCREEN
โครง: modal เต็มจอ หัวข้อ "แจ้งซ่อม" + บรรทัดบอกขั้น "ขั้นที่ n: ชื่อขั้น (n/4)"
ปุ่มล่าง: [ย้อนกลับ] [ถัดไป] — ขั้นสุดท้ายเปลี่ยนข้อความเป็น "ส่งเรื่องแจ้งซ่อม"

ขั้น ① เลือกรถ
  - ตารางลิสต์รถ (ไม่ใช่ dropdown) ค้นด้วยทะเบียน มี 4 คันตาม mock
  - เลือกแล้วดึงอัตโนมัติ: ยี่ห้อ/รุ่น · สังกัด · อุปกรณ์เสริม (อ่านอย่างเดียว)
  - ต้องระบุจุดที่มีปัญหา: ตัวรถ / อุปกรณ์เสริม  (required)
  - ยังไม่เลือกรถ → ปุ่มถัดไป disable

ขั้น ② อาการเสีย
  - checkbox รายการมาตรฐาน กรองตามจุดที่เลือกในขั้น ① จัดกลุ่มตามหมวด
  - มี "อื่นๆ ระบุเอง" เสมอ + ความรุนแรง (ปกติ/เร่งด่วน/หยุดใช้งาน) + คำอธิบาย + แนบรูปได้หลายไฟล์
  - required: อย่างน้อย 1 อาการ

ขั้น ③ ตัดสินใจ + อะไหล่
  - บนสุด: การ์ดตัดสินใจ 2 ใบ (ซ่อมเอง / ส่งซ่อม กบค.) — ตัดสินใจก่อน แล้วค่อยเลือกอะไหล่
  - ตารางอะไหล่ที่ระบบแนะนำตามอาการที่ติ๊ก คอลัมน์: รหัส · ชื่อ · ต้องใช้ · คงเหลือ · สถานะคลัง · คลัง · อัปเดตเมื่อ
  - สถานะคลัง: พร้อมเบิก (>3) · ใกล้หมด (1–3) · หมด (0) · หมด-รอของ (0 + ETA)
  - จองได้เฉพาะคงเหลือ > 0 · จำนวนเริ่มต้น = ที่ต้องใช้ แต่ไม่เกินคงเหลือ
  - เปลี่ยนอาการ → รายการอัปเดต + ยกเลิกการจองของอาการที่เอาออก พร้อมแจ้งผู้ใช้

ขั้น ④ สรุปและผู้อนุมัติ
  - สรุปทุกอย่างแบบอ่านอย่างเดียว + ปุ่มแก้ไขกลับไปแต่ละขั้น
  - ถ้าเลือก "ส่ง กบค." ต้องกรอกเหตุผลที่ซ่อมเองไม่ได้ (required) + แสดงหน่วยงานปลายทางก่อนยืนยัน
  - ส่งแล้ว → แสดงเลขที่ใบ + สถานะเริ่มต้น + ปุ่มไป "เรื่องของฉัน"

สถานะที่ต้องมี: อาการที่เลือกแล้วไม่มีอะไหล่ผูก (แสดงข้อความ ไม่ใช่ตารางว่างเปล่า) ·
คลังหมดทุกตัว · ยังไม่กรอก required แล้วกดถัดไป (ชี้ฟิลด์ที่ขาด)

## DATA
รถ 4 คัน · อาการมาตรฐาน 11 รายการ · mapping อาการ→อะไหล่ 15 แถว
ใช้จาก 02-Flow-and-Mock-Data.md เท่านั้น ห้ามแต่งใหม่ ถ้าขาดให้หยุดถาม

## RULES
static HTML ไฟล์เดียว เปิดผ่าน http://127.0.0.1:8123 ได้ ไม่มี build step ·
state ใน localStorage · ทุก component ยกจาก components.css · สีใช้ var() ·
ไอคอน Material Symbols · light mode เท่านั้น · ไม่มี hex/emoji ในหน้า

## DONE
1. เดินครบ 4 ขั้น → ส่งเรื่อง → ใบเข้า "เรื่องของฉัน" ได้จริง
2. รีโหลดแล้ว state ยังอยู่ · console ไม่มี pageerror
3. verify-tokens.js / audit-usage.js / grep hex+emoji ผ่านหมด
4. mock/test/flow-regression.js ผ่าน

## ORDER
1. อ่านไฟล์ในข้อ READ FIRST → สรุป component ที่จะใช้ + ไฟล์ที่จะแตะ + assumption ที่ตั้งไว้
2. รอเคาะ ค่อยเขียน
3. เจอของที่ไลบรารีไม่มี → หยุดถาม ห้ามออกแบบเอง
4. เสร็จ → รันเช็คแล้วรายงานผลจริง
```

---

## 3. Prompt สำหรับ "รอบแก้" (สั้นกว่ามาก)

รอบแก้ไม่ต้องเขียนครบ 9 บล็อก ใช้แค่ 4 บล็อกนี้ แต่ต้องชี้พิกัดให้ชัด

```markdown
WHERE: mock/Maintenance-Request-Form.html · ขั้น ③ ตัดสินใจ + อะไหล่
NOW:   คอลัมน์ "สถานะคลัง" เป็นข้อความธรรมดา แยกไม่ออกตอนสแกนตาราง
WANT:  เปลี่ยนเป็น badge ตาม .badge ของ components.css (เขียว/เหลือง/แดง ตาม token)
       + เพิ่มตัวกรองด้านบนตาราง: ทั้งหมด / เฉพาะที่จองได้
KEEP:  logic การจองเดิม · เกณฑ์ ≤3 = ใกล้หมด เดิม · ห้ามแตะขั้นอื่น
```

หลักคือ **WHERE / NOW / WANT / KEEP** — บอกด้วยว่าอะไร "ห้ามแตะ" เพราะ AI ชอบปรับพ่วง

---

## 4. Checklist ก่อนกดส่ง prompt

- [ ] บอกไหมว่าให้ **อ่านไฟล์ไหนก่อน**
- [ ] เขียน GOAL เป็นผลลัพธ์ที่ผู้ใช้ทำได้ ไม่ใช่รายการงาน
- [ ] มี **ไม่ทำอะไร** ระบุไว้
- [ ] ฟิลด์/ปุ่มมีชื่อและข้อความจริง ไม่ใช่ "ฟอร์มกรอกข้อมูล"
- [ ] ระบุ empty / error / loading / ไม่มีสิทธิ์
- [ ] ชี้แหล่ง mock data แทนปล่อยให้แต่ง
- [ ] เกณฑ์เสร็จ **รันแล้ววัดได้**
- [ ] มีจุดหยุดถามก่อนลงมือ

## 5. Anti-pattern ที่ทำให้ prototype เพี้ยน

| อาการ | สาเหตุใน prompt | แก้ที่ |
|---|---|---|
| หน้าตาไม่เหมือนหน้าอื่นในระบบ | ไม่สั่งให้อ่าน design system ก่อน | ② READ FIRST |
| แต่งทะเบียนรถ/อะไหล่ขึ้นมาเอง | ไม่ชี้แหล่ง mock | ⑥ DATA |
| ได้แค่ happy path | ไม่ระบุ state พิเศษ | ⑤ SCREEN |
| งานบานไปแตะหน้าอื่น | ไม่มี "ไม่ทำ" / ไม่มี KEEP | ④ SCOPE |
| บอกว่าเสร็จแต่พังจริง | เกณฑ์เสร็จเป็นความรู้สึก | ⑧ DONE |
| ออกแบบ component ใหม่เอง | ไม่มีจุดหยุดถาม | ⑨ ORDER |
| สั่ง 10 หน้าทีเดียวได้ของตื้น | ไม่ซอยงาน | ทำทีละ 1–2 หน้า แล้วรีวิว |
