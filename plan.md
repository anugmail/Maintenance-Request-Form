# แผนรีวิว/เดโม prototype "แจ้งซ่อม–กบค." — 6 หมุดหมาย (Review Milestones)

> เอกสารรายละเอียดการทำของแต่ละหมุด คู่กับหน้ารีวิว [05-review-milestones.html](05-review-milestones.html) (การ์ดบนฮับ `index.html`)
> — หน้า 05 ใช้เดโมกับเจ้าของงาน · ไฟล์นี้ใช้ฝั่งทีมทำ (รายละเอียดจอ/validation/ข้อมูล/จุด verify)

**Goal:** หั่น flow แจ้งซ่อม–กบค. (ใน `mock/Maintenance-Request-Form.html`) ออกเป็น **6 หมุดรีวิว/เดโม** เดินรีวิวกับเจ้าของงานทีละช่วง เก็บคำตอบของคำถามเปิดกลับมาปรับ mock ทีละรอบ

**Architecture:** Static HTML ไฟล์เดียว + vanilla JS (ไม่มี build) · 7 view สลับบทบาทผ่าน `switchView()` (`form / my / fuel / kry / kbk / sup / stock`) · เรื่องแจ้งซ่อมเป็น object ใน `JOBS[]` เดินสถานะด้วย `phase` (`self → kry → krd/pending → work → slots → appt → approve → closed` + เส้นอู่ `returned → gclosed`) · deep-link ด้วย hash (`#kry`) + `?seed=review` เติมงานตัวอย่างเฉพาะรอบเดโม (ไม่กระทบ default)

**ขอบเขต:** เฉพาะ flow แจ้งซ่อม–กบค. (ไม่รวม PWA บันทึกการใช้รถ / บำรุงรักษาตามวาระ)

---

## ภาพรวม 6 หมุด

| หมุด | ช่วง | บทบาท | เปิดเดโม (deep-link) | สถานะใน mock |
|---|---|---|---|---|
| **M1** | สร้างคำขอ / แจ้งซ่อม | ผู้แจ้ง | `mock/…html#form` | ✅ เดโมได้ · ⚠️ **ขั้นตัดสินใจรอแก้ตาม AC-D1…D5** (เคาะ 4 ส.ค. 2569) |
| **M2** | คัดแยก (กรย.) + รับเข้า กบค. | กรย. → กบค. | `mock/…html?seed=review#kry` | ✅ ครบ |
| **M3** | ประเมิน + จัดการอะไหล่ | กบค. + คลัง | `mock/…html?seed=review#kbk` | ✅ ครบ |
| **M4** | นัดหมาย + รับรถ + ตรวจสภาพขาเข้า | กบค. + เจ้าของรถ | `mock/…html?seed=review#kbk` | ✅ ครบ |
| **M5** | ดำเนินการซ่อม + บันทึกผล | กบค. (+ผู้แจ้งรับทราบ) | `mock/…html?seed=review#kbk` | ✅ ครบ |
| **M6** | ต้นทุน + ปิดงาน + อนุมัติ | กบค. → ผู้บังคับบัญชา กบค. | `mock/…html?seed=review#sup` | ✅ ครบ + gate เอกสารก่อนปิดงาน (เส้นอู่/จ้างเอกชน) |

> เดโมไล่ต่อกันได้: จบ M(n) ได้สถานะป้อนเข้า M(n+1) — เรียงตาม role journey ผู้แจ้ง → กรย. → กบค. → ผู้อนุมัติ

---

# หมุด M1 — สร้างใบแจ้งซ่อม (ผู้แจ้ง) — รายละเอียดการทำ

## จุดเข้าเดโม

- ฮับ → การ์ด "เฟสรีวิว/เดโม (6 หมุด)" → M1 **เปิดเดโม** → `mock/Maintenance-Request-Form.html#form`
- M1 ไม่ต้องใช้ `?seed=review` — เริ่มจากฟอร์มว่างแล้วสร้างเรื่องจริงได้เลย · ปุ่มรอง "ติดตามสถานะเรื่องของฉัน" → `#my`

## โครงฟอร์ม: wizard 5 ขั้น

Stepper อยู่ใน `#wsteps` เดินตาม `WORDER = ['vehicle','symptom','info','parts','decision']` — **สลับลำดับ/เปิดปิดขั้นได้จาก Admin** (`cfg.steps.wizard`) · ปุ่มท้ายฟอร์ม `ย้อนกลับ/ถัดไป` (`nav(±1)`) ขั้นสุดท้ายเปลี่ยนเป็น "ส่งเรื่องแจ้งซ่อม"

### ขั้น 1 — เลือกรถ (`s1`)

| เรื่อง | รายละเอียด |
|---|---|
| ค้นหา | ทะเบียน/รุ่น จากรายการ "รถของฉัน/รถในทีม" (`VEHICLES` จาก master data — Admin แก้ได้) · รถไม่อยู่ในระบบ → ติดต่อผู้ดูแลข้อมูลรถ (กรย./กบค.) |
| การ์ดรถ | ใช้ shared component `UIC.vehicleCard` (variant สลับได้จาก Admin) |
| ข้อมูลอัตโนมัติ | เลือกแล้วระบบดึง ยี่ห้อ/รุ่น · สังกัด · อุปกรณ์เสริม ให้เอง (read-only) |
| จุดที่มีปัญหา | radio **ตัวรถ / อุปกรณ์เสริม** (ตัวเลือกอุปกรณ์เสริมโผล่เฉพาะรถที่มี เช่น กระเช้า/เครน) — เปลี่ยนรถ/เปลี่ยนจุด → ล้างอาการ+การจองที่เลือกไว้ |
| Validation | ต้องเลือกรถ + จุดที่มีปัญหา ก่อนไปต่อ |

### ขั้น 2 — อาการเสีย (`s2`)

🔄 **6 ส.ค. 2569:** `ระดับความรุนแรง (ต่ำ/ปานกลาง/สูง/หยุดใช้งาน)` → **`สถานะรถ ใช้งานได้/ใช้งานไม่ได้`** — เดิมถามซ้ำกับขั้น 3 และโค้ดเดาว่า "ความรุนแรงสูง = ใช้งานไม่ได้" ซึ่งไม่จริงเสมอ (อาการเบาแต่กฎหมายห้ามใช้ก็มี) · ตัด `sev` ออกทั้งระบบแล้ว

| เรื่อง | รายละเอียด |
|---|---|
| รายการอาการ | chip มาตรฐาน 11 อาการ ใน 5 หมวด (`CATS`): ไฮดรอลิก · บูม/กระเช้า · ไฟฟ้า · เครื่องยนต์ · อื่นๆ — **กรองตามจุดที่เลือก** (หมวดมี `for: vehicle/attach/both`) เลือกได้หลายอาการ |
| อื่นๆ | เลือก `OTH` → เปิด textarea บังคับพิมพ์อธิบาย |
| ความรุนแรง | radio 4 ระดับ: ต่ำ / ปานกลาง / สูง / หยุดใช้งาน (`SEVS`) |
| รูปถ่ายอาการ | แนบไฟล์จริง (multiple) หรือปุ่ม "รูปตัวอย่าง" สำหรับเดโม · ลบได้ · ปิดทั้งช่องได้จาก Admin (`TG.photoUpload`) |
| Validation | ≥1 อาการ · เลือก OTH ต้องกรอกข้อความ · ต้องเลือกความรุนแรง |

### ขั้น 3 — ข้อมูลติดต่อ · สถานที่ · งบ (`sinfo`) 🆕 6 ส.ค. 2569

เพิ่มเพราะ **ฝั่ง กบค. ต้องใช้ข้อมูลชุดนี้ตอนรับเรื่อง แต่ฟอร์มเดิมไม่ได้เก็บ** (เดิมขึ้น "—" ทุกช่อง)

| กลุ่ม | ช่อง | บังคับ |
|---|---|---|
| สถานที่ | **จังหวัด** → **หน่วยงาน กฟภ. ในพื้นที่** (cascade) | จังหวัด ✅ |
| การใช้งาน | เลขไมล์ · ชั่วโมงใช้งานเครน/กระเช้า (ป้ายเปลี่ยนตามอุปกรณ์จริง · รถไม่มีอุปกรณ์ซ่อนช่อง) | — |
| ผู้ติดต่อ | หน่วยงานเจ้าของเรื่อง · ช่างคุมรถ · พขร. (ชื่อ+เบอร์) | **อย่างน้อย 1 เบอร์** ✅ |
| งบ | ประเภทงบ + ฟิลด์อ้างอิงตามประเภท | ✅ |

**เติมค่าอัตโนมัติจากข้อมูลรถ** — จังหวัดจากทะเบียน · หน่วยงานจากต้นสังกัดรถ · ผู้แจ้งแก้ได้

**พื้นที่จริง 2 จังหวัด** จากผัง 74 จังหวัด: ขอนแก่น (กฟฉ.1 · กฟส. 27) · นครราชสีมา (กฟฉ.3 · กฟส. 36) พร้อมขนาดหน่วยงานจริง — แก้สระ า/ำ ที่เพี้ยนจาก font subset ทั้ง 63 ชื่อแล้ว · กฟจ. ขึ้นก่อนแล้วเรียง กฟส. ตามขนาด · มีตัวเลือก "จังหวัดอื่น" แต่ผ่าน validation ไม่ได้ เพื่อให้เห็นว่าต้องเติมอีก 72 จังหวัด

**ประเภทงบ — อิงโครงจริงจาก `vms_ref_cost_type`**

| code | ชื่อ (สมมติ) | ต้องระบุ |
|---|---|---|
| 1 | งบประจำหน่วยงาน | ศูนย์ต้นทุน ✅ |
| 2 | งบส่วนกลาง (กบค.) | ศูนย์ต้นทุน ✅ |
| 3 | งบโครงการ (ตาม WBS) | **WBS ✅** + โครงข่าย/กิจกรรม |
| 4 | งบซ่อมบำรุงตามใบสั่ง (PM Order) | ใบสั่ง/กิจกรรม/โครงข่าย **อย่างน้อย 1** ✅ |

โครง code 1–4 · ฟิลด์ · เงื่อนไขบังคับ ตรงกับ `disbursementModal.tsx` ของระบบจริง
⚠️ **ชื่องบเป็นข้อมูลสมมติ** — `ref_cost_type_name` ดึงจาก `GET /api/ref/cost-type` ตอน runtime หาในโค้ดไม่เจอ พอได้ชื่อจริงมาเปลี่ยนแค่ชื่อ ไม่ต้องรื้อโครง

### ขั้น 4 — อะไหล่ที่ระบบแนะนำ (`s3`)

| เรื่อง | รายละเอียด |
|---|---|
| Suggest | map อาการ → อะไหล่ (`PARTS.sym`) แสดง รหัส/ชื่อ/จำนวนที่ต้องใช้ต่อครั้ง/คลังที่เก็บ/เวลาอัปเดตสต็อก |
| Badge สถานะคลัง | `พร้อมเบิก · เหลือ N` / `ใกล้หมด (≤3)` / `หมด (+ETA ถ้ามี)` |
| มีของ | ปุ่ม **จองอะไหล่** — ปรับจำนวน ± ได้ ไม่เกินสต็อก · ยกเลิกได้ |
| ของหมด | ปุ่ม **สั่งของ** — เห็น ETA ของเข้า · รายการไปโผล่หน้า "คลังอะไหล่" (`#stock`) ฝั่ง กบค. |
| Auto-cleanup | ถอดอาการออก → ยกเลิกจอง/สั่งของของอาการนั้นให้อัตโนมัติ (+toast แจ้ง) |
| อาการอื่นๆ | ไม่มีอะไหล่แนะนำ — ให้ช่าง กบค. ประเมินหลังรับเรื่อง |
| หมายเหตุ | mock ยังไม่ตัดสต็อกจริง (แสดงเป็นการจอง) |

### ขั้น 5 — ตัดสินใจ + สรุป (`s4`)

> 🔒 **เคาะกับเจ้าของงานแล้ว 4 ส.ค. 2569** — ดู [AC ขั้นตัดสินใจ](#acceptance-criteria--m1-ขั้นตัดสินใจ-เคาะแล้ว-4-สค-2569) ด้านล่าง · ส่วนนี้คือสภาพ **ที่ต้องเป็น** (mock ปัจจุบันยังไม่ตรงทุกข้อ)

| เรื่อง | รายละเอียด |
|---|---|
| ทางเลือก | 2 tile: **ซ่อมเอง** (กระบวนการนอกระบบ — ค้างรอกรอกประวัติ) หรือ **ส่งเรื่องให้ กรย.** (ช่องเหตุผล ไม่บังคับ) |
| เส้นทางส่งซ่อม | **ผ่าน กรย. เสมอ** ทุกหน่วยงานทุกสังกัด — ไม่มีเส้นส่งตรง กบค. จากผู้แจ้ง (ยกเลิก routing ตามสังกัดที่ US-08 เคยระบุ) |
| ขนาดหน่วยงาน | เป็น**เพดาน**ว่าเรื่องไปได้ไกลแค่ไหน — **XS = ไปได้ไกลสุดที่ กรย. ไม่มีเส้นทางถึง กบค.** · ขนาดอื่น กรย. คัดแยกได้ครบ 3 ทาง |
| กล่อง routing | อธิบายว่า กรย. (กองระบบไฟฟ้าและงานโยธา 12 เขต) จะคัดแยก **ซ่อมอู่ / กรย. ซ่อมเอง / ส่งต่อ กบค.** — อาการ รูป อะไหล่ที่จอง/สั่ง แนบไปอัตโนมัติ · หน่วยงาน XS ไม่ต้องโชว์ตัวเลือก "ส่งต่อ กบค." |
| ตารางสรุป | รถ · จุดที่มีปัญหา · อาการ · ความรุนแรง · รูปแนบ · อะไหล่จอง/สั่ง · ทางที่เลือก (ชื่อปลายทางต้องตรงของจริง) |
| Validation | ต้องเลือกทางใดทางหนึ่งก่อนส่ง — **เหตุผลที่ซ่อมเองไม่ได้ ไม่บังคับ** (ตัดสินใจแล้ว ไม่ใช่ของตกหล่น) |

## ผลลัพธ์เมื่อส่งเรื่อง (`submit()`)

- ออก**เลขที่ใบแจ้งซ่อม** `MTD-690717-0NN` (running `SEQ`) แสดงหน้าจอสำเร็จ + ปุ่มไป "เรื่องของฉัน" / แจ้งเรื่องใหม่
- สร้าง job เข้า `JOBS[]` เก็บ: เลขที่ · รถ+อุปกรณ์เสริม · สังกัด · จุดที่มีปัญหา · อาการ (รวมข้อความอื่นๆ) · ความรุนแรง · จำนวนรูป · คำอธิบาย · อะไหล่จอง/สั่ง (พร้อม ETA) · เหตุผล/แผน · `history[]` timeline
- **ส่ง กรย.** → `phase:'kry'` เข้าคิว "งานคัดแยก กรย." ทันที (badge เมนูเด้ง) = จุดส่งไม้ต่อเข้า **M2**
- **ซ่อมเอง** → `phase:'self'` สถานะ **"ซ่อมเอง — รอบันทึกประวัติการซ่อม"** · **ยังไม่ปิดเรื่อง** จนกว่าจะกรอกประวัติ (ดู AC-D5)
- ผู้แจ้งติดตามที่ "เรื่องแจ้งซ่อมของฉัน" (`#my`) — เปิดดูรายละเอียด+timeline แบบ accordion

## สคริปต์เดโมแนะนำ (~2 นาที)

1. เปิด M1 จากหน้า 05 → ค้นรถ "83-xxxx" เลือกรถกระเช้า → ชี้ข้อมูลอัตโนมัติ → เลือกจุด "กระเช้า"
2. เลือกอาการ "กระบอกไฮดรอลิกรั่ว/ซึม" + "กระเช้าเอียง/ล็อกไม่อยู่" → ความรุนแรง "สูง" → กดรูปตัวอย่าง 2 รูป
3. ขั้นอะไหล่: จองชุดซีล (มีของ) → สั่งของรายการที่หมด (ชี้ ETA + บอกว่าไปโผล่หน้าคลัง)
4. ตัดสินใจ "ส่งเรื่องให้ กรย." + เหตุผล → ชี้ตารางสรุป → ส่ง → ได้เลขเรื่อง → เปิด "เรื่องของฉัน" ดูสถานะ "รอ กรย. คัดแยกช่องทาง"

## ปรับแต่งได้จาก Admin (ใช้ตอบคำถาม "แก้ได้ไหม" กลางวงรีวิว)

- ลำดับ/เปิดปิด/เปลี่ยนชื่อขั้น wizard (`cfg.steps.wizard`) · เปิดปิดช่องแนบรูป (`TG.photoUpload`) · รูปแบบการ์ดเลือกรถ (`UI.vehicleCard`) · master data รถ/อะไหล่/อู่ · seed ข้อมูลเดโม
- 🆕 **ขนาดหน่วยงาน** (AC-D2) — ต้องเพิ่มเป็น master data ที่ Admin ตั้งค่าได้ เพราะเป็นตัวกำหนดว่าเรื่องส่งถึง กบค. ได้หรือไม่

## Acceptance Criteria — M1 ขั้นตัดสินใจ (เคาะแล้ว 4 ส.ค. 2569)

> เจ้าของงานเคาะ 6 ข้อในรอบทบทวน flow แจ้งซ่อม — ทั้งหมดยัง **ไม่ได้ลงมือแก้** ใน mock

### AC-D1 — ส่งซ่อมผ่าน กรย. เสมอ

> 🔁 **เปิดใหม่ 7 ส.ค. 2569 — อย่าเพิ่งลงมือข้อนี้**
> จากการวิเคราะห์ Hotline/กฝช. เจ้าของงานอธิบายว่า **กรย. คือ "เขต" ที่ดูแลจังหวัดใต้สังกัด** (≈6 จังหวัด → อำเภอ) แต่ **กฝช./กกฟ. ขึ้นตรงสำนักงานใหญ่ ไม่ได้ขึ้นกับ กรย.** จึงขออนุมัติผ่านสายงานตัวเอง — **ไม่ใช่การข้ามขั้นตอน แต่ กรย. ไม่เคยอยู่ในสายของเขาตั้งแต่แรก**
> ⇒ ตรงกับที่ `US-08-บันทึกการตัดสินใจ.md:12` เขียนไว้เดิม (*"พื้นที่เขต → กรย. · สังกัด สนญ. → ส่งตรง กบค."*) ซึ่งข้อนี้เคยสั่งให้ลบ — **โชคดีที่ยังไม่ได้ลบ**
> **ข้อควรระวัง:** กรย. ทำ 2 หน้าที่ปนกัน — (1) *อนุมัติ* (2) *คัดแยกช่องทางซ่อม* · สายงานอนุมัติแก้ได้แค่ข้อ 1 · **ข้อ 2 สาย สนญ. ยังไม่มีเจ้าภาพ** (Hotline ใช้เกณฑ์จุด PTO มาทำแทนคนคัดแยก)
> **ร่าง AC ใหม่ที่เสนอ:** *"ทุกเรื่องต้องผ่าน (1) การอนุมัติตามสายงานของตนเอง และ (2) การคัดแยกช่องทางซ่อม — โดยข้อ 2 ทำได้ทั้งโดยคนหรือโดยกฎ แต่ต้องบันทึกว่าใคร/กฎอะไรเป็นผู้ตัดสิน"*
> **รอเคาะ 4 ข้อ:** (ก) กลับไปใช้ routing ตามสายงานไหม (ข) สาย สนญ. ใครคัดแยก (ค) กรย. ต้องเห็นงานสาย สนญ. ในพื้นที่ตัวเองไหม — เห็นเพื่อทราบ/แทรกได้/ไม่ต้องเห็น (ง) รถ กฝช. มีจุด PTO ไหม
> **กระทบ AC-D2 ด้วย** — *"XS ไม่มีเส้นทางถึง กบค."* ตั้งอยู่บนสเกล กฟฟ. แต่ **กอง สนญ. ไม่ได้อยู่บนสเกลเดียวกัน** (กฝช. มีรถกระเช้าของตัวเอง ถ้าเผลอตีเป็น XS จะถูกตัดเส้นทางถึง กบค. ทั้งที่ต้องใช้) ⇒ AC-D2 ต้องระบุว่าใช้เฉพาะสายเขต
> **โครงสร้างที่ใหญ่กว่าเรื่อง กรย.:** ต้นแบบวันนี้สมมติว่า **เจ้าของรถ = หน่วยผู้แจ้ง** พอเป็นรถยืม (Hotline/กฝช.) ใบแจ้งซ่อมต้องผูก **2 หน่วยงาน** — กระทบว่าใครอนุมัติ · ตัดงบใคร · sync สถานะกลับหาใคร (มีผลต่อการอนุมัติยืมรอบหน้า) — **นี่คือ data model ไม่ใช่แค่ลำดับขั้นตอน**

- [ ] เรื่องที่ผู้แจ้งเลือก "ส่งซ่อม" วิ่งเข้า **กรย.** เสมอ **ไม่ว่าหน่วยงานผู้แจ้งสังกัดเขตหรือสำนักงานใหญ่**
- [ ] **ไม่มี**เส้นทางส่งตรงจากผู้แจ้ง → กบค. — เรื่องถึง กบค. ได้ทางเดียวคือ กรย. คัดแยกแล้วส่งต่อ (M2)
- [ ] แก้ `US-08-บันทึกการตัดสินใจ.md` ที่ยังเขียนว่า "สังกัด สนญ. → ส่งตรง กบค. ไม่ผ่าน กรย." ให้ตรงกับข้อนี้
- [ ] ❓ **ยังไม่เคาะ** — กรย. ต้อง**กดรับทราบ**ก่อนเรื่องเดินต่อ (บล็อก มีคิวรอ เห็นว่าใครรับ) หรือ **แทรกสถานะอัตโนมัติ** (ไม่บล็อก เก็บเป็น trace ว่าผ่าน กรย. แล้ว)

### AC-D2 — ขนาดหน่วยงานเป็นเพดานการส่งซ่อม

- [ ] master data หน่วยงานมีฟิลด์ **ขนาดหน่วยงาน** ตั้งค่าได้จาก Admin (ไม่ hardcode)
- [ ] หน่วยงานขนาด **XS** → รถที่ครอบครองไม่ใช่รถที่ต้องให้ กบค. ซ่อม ⇒ **ไม่มีเส้นทางถึง กบค. เลย** — ในหน้าคัดแยก กรย. ตัวเลือก "ส่งต่อ กบค." ต้องไม่แสดง (หรือ disable พร้อมเหตุผล)
- [ ] หน่วยงานขนาดอื่น → กรย. คัดแยกได้ครบ 3 ทาง (ซ่อมอู่ / กรย. ซ่อมเอง / ส่งต่อ กบค.)
- [ ] **ไม่ตัด tile "ซ่อมเอง"** — ทุกขนาดยังเลือกซ่อมเองหรือส่ง กรย. ได้ (ยกเลิกข้อสันนิษฐานเดิมที่ว่า flow ใหม่ห้ามซ่อมเอง)
- [ ] ❓ **ยังไม่เคาะ** — ชุดค่าขนาดมีอะไรบ้าง (XS / S / M / L / XL?) และผูกกับ**หน่วยงาน**หรือ**ประเภทรถ**
- [ ] ❓ **ยังไม่เคาะ** — หน่วยงาน XS ยังกด "ส่งเรื่องให้ กรย." ได้ตามปกติใช่ไหม (สมมติฐานปัจจุบัน: **ได้** ขนาดมีผลที่ปลายทางถัดไป ไม่ใช่ที่หน้าแจ้ง)

### AC-D3 — ป้ายชื่อต้องตรงปลายทางจริง

- [ ] ปุ่ม · ตารางสรุป · สถานะ · timeline · หน้า success ใช้ชื่อหน่วยงานที่เรื่อง**กำลังไปจริง** ณ จังหวะนั้น
- [ ] ขั้นแจ้งซ่อมเขียน **"ส่งเรื่องให้ กรย."** ทุกจุด — **ลบ**ข้อความ `ส่งซ่อม กบค. (ระบบเลือกปลายทางตามสังกัด)` ในตารางสรุป (`renderSummary`) ที่ขัดกับปุ่มอยู่
- [ ] เมื่อ กรย. คัดแยกส่งต่อ → ข้อความ/สถานะเปลี่ยนเป็น **"ส่งให้ กบค."** และ timeline เก็บครบทั้ง 2 ท่อน (ถึง กรย. → ส่งต่อ กบค.)
- [ ] rename ตัวแปรภายใน `S.decision === 'kbk'` → `'kry'` ให้ตรงกับ `phase:'kry'` ที่เซ็ตจริง (ตอนนี้ชื่อคีย์กับปลายทางไม่ตรงกัน อ่านโค้ดแล้วสับสน)
- [ ] กล่อง routing ระบุ **กรย. เขตที่รับเรื่อง** ตามสังกัดของรถ แทนข้อความกลางๆ "12 เขต" (ถ้าข้อมูลเขตมีใน master data)

### AC-D4 — เหตุผลที่ซ่อมเองไม่ได้

- [ ] เป็นช่อง **ไม่บังคับกรอก** — ปล่อยว่างแล้วส่งเรื่องได้ (**ตัดสินใจแล้ว** ไม่ใช่ของค้าง อย่าไปเพิ่ม validation)
- [ ] ถ้ากรอก → เก็บใน `reason` แสดงในรายละเอียดเรื่องและ timeline ฝั่ง กรย.

### AC-D5 — ซ่อมเอง = กระบวนการนอกระบบ ปิดงานด้วยประวัติการซ่อม

- [ ] ครอบคลุมเคส **หน่วยงานจังหวัดที่เป็นหน่วยย่อยของเขต แต่เลือกไม่ส่งเขต/กรย.** — ใช้เส้นทางเดียวกับซ่อมเองปกติ
- [ ] เส้นนี้ต้องมี **การเสนอราคาซ่อม** ตามระเบียบ แต่ **อยู่นอกระบบทั้งหมด** — ระบบ**ไม่**เก็บขั้นขอใบเสนอราคา/จัดจ้าง/จ่ายเงิน
- [ ] เลือก "ซ่อมเอง" → เรื่องบันทึกทันที สถานะ **"ซ่อมเอง — รอบันทึกประวัติการซ่อม"** (ไม่ใช่ปิดเรื่องทันที)
- [ ] สิ่งเดียวที่ระบบต้องการคือ **กรอกประวัติการซ่อมก่อนปิดงาน** — เป็น **gate บังคับ** ปิดงานไม่ได้ถ้ายังไม่กรอก
- [ ] ประวัติที่กรอกลง**ประวัติรถชุดเดียวกัน**กับเส้น กรย./กบค. เพื่อให้ dashboard/insight นับครบทุกช่องทาง
- [ ] เลิกใช้ช่อง "แผน/บันทึกการซ่อมโดยย่อ" ตอนแจ้ง (เป็น*แผน*ที่จะทำ ไม่ใช่*ผล*) — ย้ายไปเป็นฟอร์มประวัติการซ่อมตอนปิดงาน
- [ ] ❓ **ยังไม่เคาะ** — ฟอร์มประวัติการซ่อมมีฟิลด์อะไรบ้าง (รายการที่ซ่อม · อะไหล่ที่ใช้ · ค่าใช้จ่าย · ผู้ซ่อม/อู่ · วันที่ซ่อมเสร็จ · แนบเอกสาร?)
- [ ] ❓ **ยังไม่เคาะ** — ใครกดปิดงานเส้นซ่อมเอง (ผู้แจ้งเอง หรือต้องมีผู้อนุมัติ)

### คำถามเปิดที่เหลือของ M1

1. ช่องทาง **"ส่งอู่"** ต้องเป็นตัวเลือกที่ 3 ตอนแจ้งไหม (README#1) — โดยปริยาย **ไม่** เพราะทุกเรื่องผ่าน กรย. แล้ว กรย. เป็นคนตัดสินว่าเข้าอู่ (ควรยืนยันแล้วปิดข้อนี้)
2. ยืนยันจำนวนขั้น wizard = 4 ขั้น

## Verification M1

```bash
cd Maintenance-Request/Maintenance-Request-Form
python3 -m http.server 8124 --bind 127.0.0.1   # ห้าม file://
```
- ขับด้วย Playwright + Chrome ติดเครื่อง (ตาม `.claude/skills/verify`) — เดินฟอร์มครบ 4 ขั้น ทั้งเส้นส่ง กรย. และซ่อมเอง
- เช็ค validation ทุกจุด (ไม่เลือกรถ/จุด/อาการ/ความรุนแรง/ทางเลือก → toast + ไปต่อไม่ได้)
- Regression: default ไม่มี hash → เปิด `#form` · `JOBS` ตั้งต้น = 2 เรื่อง ไม่ถูก seed

---

# หมุด M2–M6 — โครงย่อ (จะขยายรายละเอียดเมื่อถึงรอบรีวิวของหมุดนั้น)

## M2 — คัดแยก + รับเข้า กบค. (`?seed=review#kry`)
กรย. เห็นคิวงานคัดแยก (seed 1 เรื่อง) → เลือกช่องทาง **ซ่อมอู่ (+แนะนำอู่ใกล้เคียง) / กรย. ซ่อมเอง / ส่งต่อ กบค.** → เรื่องเข้าคิว กบค. → กบค. **รับไว้ซ่อม / ส่งกลับ** (+เหตุผล +แนะนำอู่)
🔒 **เคาะ 4 ส.ค. 2569:** ทุกเรื่องเข้า กรย. เสมอ (AC-D1) · **หน่วยงาน XS ต้องไม่มีตัวเลือก "ส่งต่อ กบค."** ในหน้านี้ (AC-D2) · ป้ายสถานะเปลี่ยนเป็น "ส่งให้ กบค." เมื่อส่งต่อจริง (AC-D3)
คำถามเปิด: กรย. กดรับทราบ **หรือ** แทรกสถานะอัตโนมัติ (AC-D1) · เส้นทางจริงผ่าน กรย. หรือ "แผนกยานพาหนะของกอง" (flow#10) · Overhaul / จำหน่าย / จ้างเอกชน (flow#2,3,4)

## M3 — ประเมิน + จัดการอะไหล่ (`?seed=review#kbk`)
ประเมินอาการ+ราคา → เดินสถานะ รอสั่งอะไหล่ → อะไหล่ถึงแล้ว (รายการ+ETA) → หน้า "คลังอะไหล่": จอง / สั่งของ / คืน
คำถามเปิด: เบิกจากคลังไหน (flow#1) · เงื่อนไขดัดแปลงอะไหล่ (flow#9)

🆕 **ทำแล้ว 6 ส.ค. 2569 — ยกเครื่องหน้างานซ่อม กบค.** (ดูหัวข้อ "งานที่ทำแล้ว" ท้ายไฟล์)
คำถามเปิดข้อ "เบิกจากคลังไหน" **ตอบไปแล้วบางส่วน**: มี 2 คลังเบิกได้เหมือนกัน (Smart Inventory · คลังสำรอง) ไม่มีทั้งคู่ = สั่งซื้อภายนอก

## M4 — นัดหมาย + รับรถ + ตรวจสภาพขาเข้า (`?seed=review#kbk`)
กบค. เปิดตารางช่วงว่าง → ผู้แจ้งเลือกยืนยัน 1 ช่วง → **ตรวจสภาพขาเข้า** (โซนรอบคัน/น้ำมัน/ไมล์/อาการเดิม) บังคับก่อนเริ่มซ่อม
คำถามเปิด: ซ่อม สนญ. vs ออกหน้างาน · ใบตรวจสภาพตอนออกหน้างาน (flow#5)

## M5 — ดำเนินการซ่อม + บันทึกผล (`?seed=review#kbk`)
เดินสถานะซ่อม (5 ขั้น `KBK_STEPS`) → เจออาการเพิ่ม → **ผู้แจ้งกดรับทราบ** → ตรวจขาออกเทียบขาเข้า → คืนอะไหล่ → บันทึกประวัติ
คำถามเปิด: "หน่วยงานกดรับทราบ" = ใคร ไม่กดบล็อกไหม (flow#8)

## M6 — ต้นทุน + ปิดงาน + อนุมัติ (`?seed=review#sup`)
คำนวณต้นทุน → รออนุมัติ → ผู้อนุมัติ (**ผู้บังคับบัญชาของ กบค.** — ยืนยันแล้ว) อนุมัติ/ตีกลับ + timeline → ปิดงาน + แจ้งทุกฝ่าย
**เส้นอู่/จ้างเอกชน (ซ่อมนอกระบบ):** ช่วงเสนอราคา→ซ่อม อยู่นอกระบบ — กลับมา**กรอกผลซ่อม + วันที่ซ่อมเสร็จ + แนบ ใบแจ้งหนี้/ใบเสร็จ + เอกสารผู้รับจ้าง ครบก่อนจึงปิดงานได้** (gate บังคับ — ทำแล้วใน mock)
🆕 **เส้นซ่อมเอง (AC-D5)** ใช้หลักการเดียวกันแต่เบากว่า: เสนอราคา/จัดจ้างอยู่นอกระบบ — ระบบขอแค่ **กรอกประวัติการซ่อมก่อนปิดงาน** (gate บังคับ — **ยังไม่ได้ทำ**) · ฟิลด์ในฟอร์มประวัติ + ใครกดปิด = ยังไม่เคาะ
คำถามเปิด: สูตร "SUM ต้นทุน" (flow#6) · เกณฑ์ e-GP (ยังไม่ชัด — ข้ามไว้ก่อน)

---

## Backlog

### เคาะแล้ว 4 ส.ค. 2569 — พร้อมลงมือ (ดู AC-D1…D5)

- [ ] **AC-D3** แก้ป้ายให้ตรงปลายทาง + rename `decision:'kbk'` → `'kry'` — *เล็กสุด ทำก่อนได้เลย*
- [ ] **AC-D1** บังคับทุกเรื่องผ่าน กรย. + แก้ `US-08` ที่ยังเขียน routing ตามสังกัด
- [ ] **AC-D5** ซ่อมเอง: สถานะ "รอบันทึกประวัติ" + gate กรอกประวัติก่อนปิดงาน (เลิกใช้ช่อง "แผนการซ่อม")
- [ ] **AC-D2** master data ขนาดหน่วยงาน + ซ่อนตัวเลือก "ส่งต่อ กบค." สำหรับ XS
- [x] ~~ตัดตัวเลือก "ซ่อมเอง" ฝั่งผู้แจ้ง~~ — **ยกเลิก** เจ้าของงานยืนยันว่ายังคง "ซ่อมเอง" ไว้ (AC-D2)
- [x] ~~เพิ่ม validation ช่องเหตุผล~~ — **ยกเลิก** ตั้งใจให้ไม่บังคับ (AC-D4)

### ยังรอเจ้าของงานเคาะ

- [ ] กรย. **กดรับทราบ** หรือ **แทรกสถานะอัตโนมัติ** (AC-D1)
- [ ] ชุดค่า **ขนาดหน่วยงาน** + ผูกกับหน่วยงานหรือประเภทรถ · XS ยังส่ง กรย. ได้ไหม (AC-D2)
- [ ] ฟิลด์ใน**ฟอร์มประวัติการซ่อม** + ใครกดปิดงานเส้นซ่อมเอง (AC-D5)
- [ ] Region scoping: กรย. เห็นเฉพาะเรื่องในเขตตัวเอง (M2)
- [ ] ใบประเมินใหม่เมื่อซ่อมอาการ A แล้วเจออาการ B (M5)
- [ ] ประเมินความพึงพอใจหลังปิดงาน (M6)
- [ ] เส้นอนุมัติกรณีรถประจำจังหวัด

---

## งานที่ทำแล้ว — รอบ 5–7 ส.ค. 2569

### ขั้นอนุมัติหัวหน้าก่อนส่งต่อ (จากที่ประชุม 5 ส.ค.)
สร้างใบแจ้งซ่อมแล้ว **ต้องผ่านหัวหน้าอนุมัติก่อน** จึงเดินต่อ — ทำทั้ง mock หลักและ flow2
- ขั้น "ตัดสินใจและสรุป" เพิ่มช่องเลือกผู้อนุมัติ (บังคับ) · phase ใหม่ `wait` / `reject`
- เมนู + หน้า "อนุมัติใบแจ้งซ่อม (หัวหน้า)" วางก่อน กรย.
- เก็บผู้อนุมัติเป็น snapshot ตาม pattern `confirmed_request_*` ของระบบจริง

⚠️ **ข้อจำกัดที่เจอจากการอ่านโค้ดจริง** — `GetUserManager()` รับ **`dept_sap` ไม่ใช่ `emp_id`** ⇒ คนในหน่วยงานเดียวกันได้รายชื่อหัวหน้าชุดเดียวกัน **ทำ "หัวหน้ารายบุคคล" ไม่ได้ด้วยของที่มีตอนนี้** · และความสัมพันธ์หัวหน้าไม่ได้อยู่ใน UserHub แต่อยู่ที่ HR Platform (บริการที่ 3)

### หน้างานซ่อม กบค. — ยกเครื่อง
- **คิวงาน**: แถวแสดง ทะเบียน · ประเภทรถ · สถานะรถ · สถานะใบงาน
- **รายละเอียด** (กางในลิสต์): 7 ช่องตาม req · อาการ+หมายเหตุ+รูป · ผู้ติดต่อ 3 การ์ด (โทรได้) · งบที่จะตัด · ปุ่มประวัติการซ่อม (เปิดหน้าแยก)
- **แยกขั้นตอนเป็นหน้าละขั้น** `#kbk/<เลขที่ใบ>/<ขั้น>` — รับเรื่อง → เลือกวิธีซ่อม → จัดอะไหล่ → ดำเนินการซ่อม
- **ตัดออกจากหน้านี้**: ประเมินความคุ้มค่า/Overhaul/จำหน่าย · รูปแบบการซ่อม (งานใหญ่/เล็ก)
- **เพิ่ม**: ซ่อมเองได้ / ส่งอู่ประเมินราคา (phase `quote`) · ปุ่มประเมินความคุ้มค่า → หน้า `#worth`

### อะไหล่ — master data + หน้าจอ
- เพิ่ม `price` ครบ 30 รายการ · `fit` **ระดับรุ่นอุปกรณ์** · `stockAlt` (คลังสำรอง)
- เพิ่มอะไหล่เฉพาะรุ่น 15 รายการ (Aichi SK17A / Tadano TM-ZE304 / Unic URV554)
- ตารางแบ่ง **3 กลุ่มตามแหล่ง**: Smart Inventory → คลังสำรอง → ต้องสั่งซื้อภายนอก + แถบสรุปบอกว่างานเริ่มได้ทันทีหรือติดของสั่งซื้อ
- ตัวเลือกเพิ่มอะไหล่: dropdown ยี่ห้อ/รุ่น (ตั้งต้นตามรถคันนั้น) + ช่องค้นหา
- `KBK_STEPS`: `รอสั่งอะไหล่/อะไหล่ถึงแล้ว` → **`จัดหาอะไหล่/อะไหล่พร้อม`**

### ข้อมูลอ้างอิงที่สกัดเพิ่ม
- `pea-org-74provinces-size.csv` (1,888 แถว) + `pea-dept-size-bridge.csv` — ตารางสะพานขนาดหน่วยงาน ↔ `dept_sap` (เว้นช่อง `dept_sap` รอ dump) ดูหน้า `07-dept-size-bridge.html`
- **กระทบ AC-D2**: ขนาดเป็นเกรด**ภายในระดับ** ไม่ใช่สเกลเดียวทั้งองค์กร — กฟจ. มีแค่ L/S ไม่มี XS เลย ⇒ AC-D2 ต้องเขียนเป็นคู่ **(ระดับ, ขนาด)** ไม่ใช่ขนาดลอยๆ · XS = 53% ของ กฟส. ทั้งประเทศ

### ฟอร์มแจ้งซ่อม — เก็บข้อมูลที่ กบค. ต้องใช้ครบ (6 ส.ค.)
- เพิ่มขั้น 3 "ข้อมูลติดต่อ · สถานที่ · งบ" (ดูรายละเอียดในหมุด M1)
- ยุบ "ระดับความรุนแรง" → "สถานะรถ ใช้งานได้/ไม่ได้"
- คิวงาน กบค.: ยุบแถวจาก 3 บรรทัดเหลือ 2 (เลขที่ใบ | ทะเบียน + ชิป / หน่วยงาน · อาการ)
- จัดระนาบขั้นข้อมูลติดต่อ + responsive 4→2→1 คอลัมน์

### แก้ design system
`components.css` ให้ `width:100%` เฉพาะ `input[type=text]` — `number`/`tel` ตกหล่น ทำให้ช่องเลขไมล์/เบอร์โทรหดเหลือ ~200px บนจอแคบ **แก้ที่ต้นทางแล้ว กระทบทุกหน้า**

### Design System v0.2 — ดึงค่าจริงจาก Figma (7 ส.ค. 2569)

**ที่มา** ไฟล์ `EXT_PEA_VMS_v1.0.2_Component` (เวอร์ชันในไฟล์ `EXT-PEA-T0REUY1W-2026-V102`) หน้า **"↳ Buttons"** node `1:1375` — **636 variants** ใน 5 ชุด
อ่านผ่าน Figma MCP ด้วย `get_metadata` / `get_design_context` / `get_screenshot` เท่านั้น (read-only ตามที่เจ้าของงานกำชับ — **ไม่ได้เรียก `use_figma` ซึ่งรันสคริปต์แก้ไฟล์ได้**)

**2 เรื่องที่พบว่าของเราผิด แล้วแก้แล้ว**

| | v0.1 (ผิด) | v0.2 (ตรงของจริง) |
|---|---|---|
| hover ปุ่มหลัก | `#CF07AA` สว่างขึ้น | **`#74045F` เข้มลง** — และ `#CF07AA` ไม่มีในไลบรารีเลย (เราสกัดมาจาก gradient) |
| gray ramp | Untitled UI **v1** (`#344054`/`#D0D5DD`…) | Untitled UI **v2** (`#414651`/`#D5D7DA`…) — คนละชุดทั้งแถบ |

**สิ่งที่ทำ**
- `tokens.css` เขียนใหม่เป็น **3 ชั้นตามไลบรารีจริง**: primitive (`--brand-600`, `--gray-*`) → semantic (`--color-text-secondary`) → component (`--btn-primary-bg`) · เพิ่ม space scale (ฐาน 4px มีครึ่งขั้น) + typography scale (`text-sm 14/20`, `text-md 16/24`) · **คง alias เดิมทุกตัว** (`--primary-600` ฯลฯ) หน้าเดิมจึงไม่พัง
- `components.css` เขียนบล็อก `.btn` ใหม่ — **4 ขนาด** (sm36/md40/lg44/xl48) × **5 ระดับ** (Primary/Secondary/Tertiary/Link color/Link gray) × **5 สถานะ** (Default/Hover/**Focused**/Disabled/**Loading**) + **ชุด destructive**
  - padding เขียนเป็น `calc(token − 1px)` เพราะ Figma วาดเส้นขอบ**ด้านใน** แต่ CSS วาด**ด้านนอก** — ลบแล้วความสูงตรงเป๊ะ
  - `.btn-p` `.btn-o` `.btn-g` เดิม **ใช้อยู่ 209 จุด — คงไว้ทั้งหมด** (`.btn-o` ระบุชัดว่าเป็นส่วนขยายของต้นแบบ ไลบรารีจริงไม่มี)
- **`design-system/buttons.html` (ใหม่)** — เมทริกซ์ปุ่มครบทุก variant + ตาราง token + ที่มา + สิ่งที่ยังค้าง
- `design-system/index.html` แก้ swatch เทาให้ตรงค่าจริง + ลิงก์ไปหน้าปุ่ม
- บั๊ม `?v=20260807-vms102` ทั้ง `tokens.css` + `components.css` **ครบ 16 หน้า** (กันแคชค้างแบบที่เคยโดน)

**กำกับที่มาไว้ในไฟล์ทุกค่า** — `✔` อ่านจาก Figma ตรงๆ · `~` จาก VMS Plus runtime รอบก่อน · `⚠` ยังไม่ได้อ่าน ต้องกลับไปยืนยัน

**ติด rate limit ของ Figma MCP (Starter plan)** ระหว่างทาง — เหลือ 2 ค่าที่ยังไม่ยืนยัน (destructive hover, Link color) + `gray-500/900/950` ใช้ค่ามาตรฐาน UUI v2 ไปก่อน

**ยังไม่ได้เทียบ layout หน้าจอ** — ลิงก์ที่ได้เป็นไลบรารีคอมโพเนนต์ ข้างในไม่มีแบบหน้าจอให้เทียบ · ที่ตรวจแล้วคือหน้าเดิมไม่พังหลังเปลี่ยน token (screenshot 4 หน้า)

### Design System v0.3 — ยกทุกหน้าให้อิง design system (8 ส.ค. 2569)

> 🔒 **กฎใหม่จากเจ้าของงาน:** *"นับจากนี้ การออกแบบให้อิงจาก design system ด้วย"* — เขียนเป็นกฎบังคับ 5 ข้อไว้หัว `design-system/README.md` ข้อ 0 พร้อมคำสั่ง `grep` ไว้ตรวจซ้ำ

**ผล audit** — 17/20 หน้าอิง design system อยู่แล้ว · **หลุด 3 หน้า:** `outcome-dashboard.html` · `parts-insights.html` · `repair-history.html` (เป็นไฟล์ยุค artifact self-contained)

| ปัญหาที่เจอใน 3 หน้านั้น | แก้เป็น |
|---|---|
| ไม่ `<link>` design-system เลย นิยาม palette เอง | `<link>` tokens.css + remap `:root` ทั้งบล็อกไปหา token กลาง |
| ใช้ **สีม่วง** `#6D2C9C` ไม่ใช่ magenta แบรนด์ | `var(--brand-600)` `#A80689` |
| มี **dark mode** ที่ระบบไม่มี (palette ม่วงเข้มคิดเอง) | **ตัดทิ้งทั้ง 3 บล็อก + ปุ่มสลับธีม** → light อย่างเดียวเท่ากับทุกหน้า |
| ไอคอนเป็น **inline `<svg>`** | `<span class="ms">` Material Symbols (edit · star · arrow_forward · search · check · schedule · build · local_shipping · description) |
| 🐛 **ไม่มี `<meta charset>` เลย** — ภาษาไทยเพี้ยนเป็น mojibake ตอนเปิดจาก Pages | เพิ่ม `<!DOCTYPE>` + `charset=UTF-8` + `viewport` (บั๊กเดิม ไม่ได้เกิดจากงานนี้) |

**ที่เหลือทั้งโปรเจกต์** — `mock` ทั้ง 2 ไฟล์ · `05` · `index` · `executive-insights` · `flow-นัดหมายรับรถ` · `admin-config` · `daily-record` แทน hex เป็น token ครบ **22 จุด** (รวม gradient โลโก้ม่วง `#d9b8d2→#8a5c95` ที่หลุดอยู่ใน mock ทั้งสองไฟล์ · เทาชุด v1 ในเทมเพลตใบรับรถของ flow2 · `var(--primary-200)` ที่อ้างถึง token ที่ไม่มีอยู่จริง)

**token ที่เพิ่มเพื่อเลิก hardcode** — `--brand-200` `--brand-accent` (ส้มในโลโก้ MD) · `--secondary-50` · `--info-25/100/200` · `--shadow-card`

**hex ที่เหลือ = ตั้งใจให้เหลือ** (มีคอมเมนต์กำกับทุกจุด) — `<meta name="theme-color">` (meta ใช้ `var()` ไม่ได้) · theme preset ใน `config.js`/`config-daily.js` (เป็นข้อมูลไม่ใช่สไตล์) · rainbow ของ colour picker · หน้าเอกสารสีที่ตั้งใจโชว์ hex · test harness

### API ที่ได้รับเพิ่ม (7 ส.ค. 2569) — `GetMasterMatWiSto`

`GET http://mmapi.pea.co.th/GetMasterMatWiSto?matcode=&plant=&sloc=&batch=` · auth ด้วย header `ApiKeys` · ไฟล์สเปกอยู่ที่ `Maintenance-Request/API_Spec_GetMasterMatWiSto.txt`

**แมปกับข้อมูลอะไหล่ของเราได้** — `MATERAIL`→`code` · `MATERIALDESCTH`→`name` · `MATERIALUNIT`→`unit` · **`STOCKBAL`→`stock`** (คอลัมน์ "Smart Inventory มีของมั้ย" ที่ขาดอยู่) · `MOVINGPRICE`/`STDPRICE`→`price`

**แต่มี 3 เรื่องที่กระทบการออกแบบ**
1. **ค้นหาไม่ได้** — `matcode`+`plant`+`sloc` บังคับทั้ง 3 ⇒ API นี้เป็น **"ตัวเช็กยอด" ไม่ใช่ "ตัวค้นหา"** · หน้าจอที่ต้องการ *"อะไหล่ที่เข้ากับเครน Tadano TM-ZE304"* ยังต้องมี**ทะเบียนอะไหล่ของเราเอง** (รุ่นอุปกรณ์ → matcode) ก่อน
2. **1 คำขอ = 1 วัสดุ 1 คลัง ไม่มี bulk** — หน้าจัดหาอะไหล่ 5–10 บรรทัด × 2 คลัง = ยิง 10–20 ครั้งต่อการเปิดหน้าเดียว ⇒ ต้อง cache หรือขอ endpoint แบบ list
3. **"คลังสำรอง" คือ `sloc` อีกตัว ไม่ใช่ระบบอื่น** ⇒ โครงข้อมูลควรเลิกใช้ 2 ฟิลด์ตายตัว `stock`/`stockAlt` เปลี่ยนเป็น `locations:[{plant,sloc,name,qty}]` รองรับคลังที่ 3-4 ได้โดยไม่ต้องแก้โค้ด

**API ไม่มีให้** — `eta` (วันของเข้า) · `fit` (เข้ากับรุ่นไหน) · จำนวนที่จองค้างไว้

**🔻 ตัดสินใจ 7 ส.ค. — ถอด `eta` ออกจากต้นแบบแล้ว**
- **ถอด `eta`** ทุกจุด: `config.js` (อะไหล่ 5 รายการ + งาน 2 ใบ) · `mock/Maintenance-Request-Form.html` · `mock/Maintenance-Request-Form-flow2.html`
  - ตารางคลัง `#stock` ตัดคอลัมน์ "ETA ของเข้า" ทิ้ง (เหลือ 5 คอลัมน์) · ป้าย `หมด · รอของ (ETA …)` เหลือ `หมด` · ข้อความสรุป/timeline ตัดวงเล็บ ETA ออก
  - **เหตุผล:** ไม่มีแหล่งข้อมูลจริงรองรับ — ต้นแบบไม่ควรสัญญาข้อมูลที่ระบบให้ไม่ได้ · ถ้าจะเอากลับต้องต่อกับใบสั่งซื้อ (PO) ไม่ใช่ API วัสดุ
  - `mock/backup.html` เป็นไฟล์สำรอง **ไม่แตะ**
- **เก็บ `fit` ไว้** — ไม่ใช่ "map ไม่ได้" แต่มาจาก**ทะเบียนอะไหล่ (รุ่นอุปกรณ์ → matcode)** คนละแหล่งกับ API นี้ · ถ้าตัดจะเสียฟีเจอร์กรองอะไหล่ตามยี่ห้อเครน/กระเช้า (แบบ C ที่เคาะไว้)
- เทสหลังถอด **60/60 ผ่าน**

**ต้องถามเจ้าของ API 3 ข้อ** — (1) `STOCKBAL` หักของที่จองค้างแล้วหรือยัง (ถ้ายังจะ oversell) (2) ตั้งงบใบแจ้งซ่อมใช้ `MOVINGPRICE` หรือ `STDPRICE` (3) มี endpoint ค้นหา / รับหลาย matcode ไหม

### 🔧 โฟลว์บำรุงรักษาตามวาระ — ยกโครงใหญ่ (8 ส.ค. 2569)

> เจ้าของงานสลับมาทำโฟลว์นี้ต่อ · **รายละเอียดเต็มอยู่ที่ [`maintainance-yearly/plan.md`](maintainance-yearly/plan.md)** — ตรงนี้เก็บเฉพาะสิ่งที่กระทบภาพรวม

**โครงเปลี่ยนจากเดิมทั้งหมด**
```
plan-new.html    ออกเลขงาน (wizard 3 ขั้น · ไม่มี stepper)
      ↓ MT-2569-Q1-001  ← เลขงาน = "หัวข้อ" ของแผน
index.html       รายการแผน → เปิดแผน → stepper 5 เฟส (ของแต่ละแผน)
supplies.html    รายการเอกสารแจ้งพัสดุ → รับทราบ
```

| | เดิม | ใหม่ |
|---|---|---|
| stepper | 6 เฟส รวมออกเลขงาน · มีอันเดียวทั้งระบบ | **5 เฟส · ของแต่ละแผน** |
| เก็บแผน | 1 ก้อน | **หลายแผน** (`plans[]`) |
| ฝ่ายพัสดุ | อนุมัติ/ตีกลับ | **รับทราบ** เท่านั้น ไม่บล็อกใคร |
| ไตรมาส | ผู้ทำแผนเลือก | ระบบเติมตอนออกเลขงาน |

**เคาะกับเจ้าของงาน 6 ข้อ**
1. **หน่วยงานคือ กบค.** — "กบก." คือพิมพ์ผิด ไล่แก้ **113 จุด** ทั้งโค้ด/เอกสาร/ผัง (ปิดคำถามที่ค้างมาตั้งแต่รอบแรก)
2. แผนประจำปี **ไม่ต้องเลือกไตรมาส**
3. **พัสดุไม่ใช่ผู้อนุมัติ** — กบค. ออกเลขงานเอง
4. ตัดขั้น **"ผบพ. เตรียมอะไหล่"** ตอนทำแผน
5. **เลขงาน = หัวข้อของแผน** · 1 ปีมีหลายแผนได้
6. **ออกเลขงานแยกจาก stepper**

**เพิ่มยี่ห้อ/รุ่นอุปกรณ์** — `TADANO TM-ZE304/504` · `UNIC URV554` · `AICHI SK17A` · `KOMATSU PC130-8`
⚠️ **ไม่ได้คิดขึ้นเอง** — ยกจาก `config.js` + `repair-history.html` ที่มีอยู่แล้วในโปรเจกต์ · ใช้จัดกลุ่มอะไหล่ได้ 4 แบบ (ชนิด/ภาค/เขต/ยี่ห้อ)

**เครื่องมือใหม่ 2 ตัว**
- **`skeleton.html`** — โครงกระดูก 5 เฟส บอกว่าแต่ละเฟสคืออะไร ทำอะไร + รวม**เงื่อนไขที่ต้องเคาะ 17 ข้อ**
- **`plan-skeleton.html`** 🆕 — **skeleton ที่แก้โครงได้** สำหรับเก็บ requirement: เพิ่ม/ลด/แก้ชื่อคอลัมน์ · เลือกแหล่งข้อมูล · ฟิลด์ที่ยังไม่มีข้อมูลขึ้นป้าย `รอข้อมูล` = requirement ใหม่ · ใส่โน้ต · พรีวิวด้วยข้อมูลจริง · **Export Markdown**
  → แนวทางนี้ (B+) เลือกเพราะแนวทาง "แก้บนหน้าจริง" ใส่ฟิลด์ที่ยังไม่มีข้อมูลไม่ได้ ซึ่งทำให้ใช้เก็บ req ไม่ได้จริง

**🐛 บั๊กที่เจอและแก้** — `plan-new.html` บันทึกแผนร่างเปล่าลง localStorage **ทุกครั้งที่เปิดหน้า** (เขียนไว้ว่า "จองที่ให้แผนร่าง") ⇒ เปิด 6 ครั้งได้ร่างเปล่า 6 ใบ และแผนตัวอย่างหายไป
**บทเรียน:** อย่าเขียน record ลง storage ตั้งแต่เปิดหน้า — เขียนเมื่อผู้ใช้ใส่ข้อมูลจริงแล้วเท่านั้น

**สถานะตอนนี้** — ออกเลขงาน ✅ · เฟส 1 (เบิก/จัดหา + แผนเดินทาง) ✅ · **เฟส 2–5 ยังเป็นหน้าเปล่า**
มีแผนตัวอย่างที่**ผ่านเฟส 1 แล้ว** `MT-2569-Q1-001` (12 คัน · พัสดุรับทราบ · แผนเดินทาง 4–8 พ.ย. 2568 · 27,500 บาท) ไว้ใช้เป็นตัวตั้งต้นตอนทำเฟส 2

**เอกสารที่ sync แล้ว** — `Diagram/01-บำรุงรักษาตามวาระ/` (**ครบทั้ง 7 ไฟล์** — ดู "Diagram sync" ด้านล่าง) · `maintainance-yearly/plan.md` · `README.md` · `maintannance-yearly.md`

### Diagram sync ครบทั้งโฟลเดอร์ (9 ส.ค. 2569)

รอบ 8 ส.ค. sync ผังไว้แค่ `00-ภาพรวม` + `01-ออกเลขงาน` — ไฟล์ที่เหลือยังนับเป็น **"6 ช่วง"** ทั้งที่ของจริงเปลี่ยนเป็น **ออกเลขงาน (แยกหน้า) + 5 เฟส** ไปแล้ว ⇒ เลขช่วงเลื่อนกันทั้งชุด

**เปลี่ยนชื่อไฟล์ให้เลขเฟสอยู่ในชื่อ** (เลขนำหน้า = ลำดับการอ่าน · `เฟสN` = เฟสจริงใน stepper)

| เดิม | ใหม่ |
|---|---|
| `02-เบิกจัดหา-แผนเดินทาง.md` | `02-เฟส1-เบิกจัดหา-แผนเดินทาง.md` |
| `03-ดำเนินการบำรุงรักษา.md` | `03-เฟส2-ดำเนินการบำรุงรักษา.md` |
| `04-ตรวจรับ.md` | `04-เฟส3-ตรวจรับ.md` |
| `05-จัดทำรายงาน.md` | `05-เฟส4-จัดทำรายงาน.md` |
| `06-คำนวณต้นทุน.md` | `06-เฟส5-คำนวณต้นทุน.md` |

**แก้เนื้อผัง**
- `00-ภาพรวม` — เขียน mermaid ใหม่ทั้งอัน: เดิมกล่องพัสดุยังเป็น *พิจารณาแผน → อนุมัติ/ตีกลับ → ออกเลขงาน* และยังมีขั้น *ระบุไตรมาส* (ขัดกับหมายเหตุที่แปะหัวไฟล์เอง) → เปลี่ยนเป็น **กบค. ออกเลขงานเอง · พัสดุรับทราบ · ไม่มีไตรมาส** + เพิ่มกล่อง **รายการแผน** คั่นก่อนเข้า stepper + ตารางสถานะ ✅/⬜ ต่อเฟส
- `01-ออกเลขงาน` — หัวเรื่องเป็น **"ก่อนเข้า stepper"** (ไม่ใช่เฟส) · ปลายทางเป็น *แผนเข้าไปอยู่ในรายการแผน → เปิดแผน → เฟส 1*
- `02`–`06` — ทางเข้า/ออกอ้างเลขเฟสใหม่ · ติดป้ายสถานะ prototype ทุกไฟล์ · สายว่าจ้างติดป้าย ⬜ ยังไม่ทำในต้นแบบ · **ยกคำถามที่ยังไม่เคาะจาก `skeleton.html` มาไว้ท้ายไฟล์ของเฟสนั้น** (เฟส 2 มี 5 ข้อ) + จุดที่ยังไม่เคาะติด ❓ ในผังเลย
- `Diagram/README.md` — ดัชนีใหม่ + คอลัมน์สถานะ prototype

**เทส** — headless Chromium + mermaid 11 `parse()` และ `render()` **ผ่านครบ 7/7 ไฟล์**

### plan-skeleton ครอบทั้งโฟลว์ 11 หน้าจอ + ขั้นยืนยันรถ (9 ส.ค. 2569)

สเปกที่ตกลงไว้: [`docs/superpowers/specs/2026-08-09-skeleton-5-phases-design.md`](docs/superpowers/specs/2026-08-09-skeleton-5-phases-design.md)

**ยุบ 2 หน้าเหลือหน้าเดียว** — `skeleton.html` (อ่านอย่างเดียว 5 เฟส) **ลบทิ้ง** · `plan-skeleton.html` ขยายจาก 4 เป็น **11 หน้าจอ**

| กลุ่ม | หน้าจอ |
|---|---|
| ออกเลขงาน (3) | ชื่อแผน+เลือกรถ · รายการอะไหล่ · สรุปทั้งปี |
| 5 เฟสปฏิบัติการ (6) | **1a ยืนยันรถ** 🆕 · 1b เบิก/จัดหา+แผนเดินทาง · 2 ดำเนินการ · 3 ตรวจรับ · 4 รายงาน · 5 ต้นทุน |
| หน้าหน่วยงานอื่น (2) | ฝ่ายพัสดุ · **ต้นทาง — ยืนยันรถ** 🆕 |

- **หน้าโล่ง** ตามที่เจ้าของงานสั่ง — ตัดการ์ด "หน้านี้คืออะไร" / "ทำอะไรบ้าง" / กล่องเขียว / กล่อง "ใช้ยังไง" ทิ้งหมด เหลือแค่ section + ตารางฟิลด์ + พรีวิว
  **เหตุผล:** flow มีเจ้าของอยู่แล้วที่ `Diagram/` เขียนซ้ำ 2 ที่จะเพี้ยนกัน
- แยก **`skeleton-data.js`** (ข้อมูลโครง ~420 บรรทัด) ออกจาก `plan-skeleton.js` (logic) — งานที่ทำบ่อยสุดคือแก้โครงตามที่เจ้าของงานสั่ง แตะไฟล์เดียวพอ
- **41 → 172 ฟิลด์** · แหล่งข้อมูลตัวอย่าง **22 → 31** (เพิ่ม `travelPlan.*` ที่มีค่าจริงใน `SEED_PLAN` อยู่แล้ว)
- **คำถามที่ต้องเคาะ 21 ข้อ** (17 เดิม + 4 ของยืนยันรถ) — พิมพ์คำตอบได้ · ติ๊ก รอ/เคาะแล้ว/ตกไป · ติดไปกับ Export Markdown
- สถานะ ✅/⬜ + ลิงก์ไปหน้าจริง ย้ายไปอยู่บน**แถบนำทาง** แทนการ์ด — เห็นภาพรวมทีเดียวว่าอันไหนทำแล้ว
- **migration** — คง key เดิม (`maintaind.yearly.skeleton.v1`) ใส่ `version: 2` ในก้อนข้อมูล · เทียบราย `screen.id` ของที่เจ้าของงานแก้ไว้ชนะเสมอ · จอที่เพิ่มเองไม่หาย
  **เหตุผลที่ไม่ขึ้น key เป็น `.v2`:** ขึ้น key ใหม่ = ของที่แก้ไว้หายทันที

**ขั้นใหม่ "ยืนยันรถเข้าร่วมแผน"** (ต้นเฟส 1 ก่อนทำแผนเดินทาง)
ของเดิมส่ง Noti แจ้งเจ้าของรถ/กรย. **ทางเดียว** ตอนท้ายเฟส 1 → ของใหม่เป็น **ถาม-ตอบ** · เจ้าของรถตอบเรื่องรถว่าง · กรย. ตอบเรื่องสภาพรถ (คนละหน้าที่) · **คันที่ไม่พร้อมค้างไว้ให้ กบค. ตัดสิน** ไม่ตัดอัตโนมัติ
เหตุผลที่ต้องอยู่ก่อนแผนเดินทาง — ต้องรู้ก่อนว่ารถคันไหนเข้าได้จริง ถึงจะวางวัน/จุดนัด/คำนวณเบี้ยเลี้ยงได้ถูก

**เทส** — `node maintainance-yearly/test/skeleton-data.test.js` **27/27** · headless Chromium **36/36** (migration ไม่ทำของเก่าหาย · แก้แล้วรีโหลดค่าอยู่ · export มีคำตอบ · ลิงก์ ↗ ไม่ 404 · เมนูซ้ายทุกหน้าไม่มีลิงก์ค้าง) · mermaid **7/7**

### 🔴 ถูกจับได้ว่าไม่ได้อ่าน design system ก่อนลงมือ (9 ส.ค. 2569)

เจ้าของงานถาม: *"ของที่ทำทั้งหมด อ่านจาก design system ใช่หรือไม่"* — **ไม่ใช่** ผมใช้ token ถูก แต่เขียน CSS ระดับคอมโพเนนต์ (`.grp` `.st` override `.lbl`) ใน `<style>` ของหน้า โดยเดาจากแพตเทิร์นของหน้าเดิม ไม่ได้เปิด `design-system/README.md` ซึ่งมีกฎเขียนไว้ชัดตั้งแต่ 8 ส.ค.

**ผิด 2 ข้อ** — ข้อ 3 (ใช้ emoji `🔧` แทนไอคอน UI) · หลักการข้อ 3 (component ต้องอยู่ `components.css` ก่อน ไม่ใช่ในหน้า)

**แก้แล้ว**
- ย้าย `.wgrp` · `.wsteps.wrap` · `.wstep .st` ขึ้น **`components.css` v0.6** + เพิ่มแถวในตารางหัวข้อ 4 + Changelog
- แทน emoji ด้วย Material Symbols **ทั้งโปรเจกต์ 9 จุด 4 ไฟล์** (`app.js` 📨 · mock 2 ไฟล์ 🧪 · `repair-history.html` 💡📅🛠📍🔍) — ของเดิมทั้งหมด ไม่ใช่ของรอบนี้
- บั๊ม `?v=20260809-ds06` **21 ไฟล์**
- เพิ่ม **กฎข้อ 6 "ห้ามคิดเอง"** ใน `design-system/README.md` + แก้คำสั่งตรวจ hex ที่เดิมใช้ `grep -o` ซึ่งตัดบริบททิ้งจน filter `theme-color` ไม่โดน
- สร้าง **`CLAUDE.md`** ที่รากโปรเจกต์ — เพราะ README ต้องเปิดอ่านเอง (จึงพลาดได้) แต่ `CLAUDE.md` ถูกโหลดเข้า context อัตโนมัติทุกเซสชัน

**บทเรียน:** กฎที่เขียนไว้ในไฟล์ที่ไม่มีใครถูกบังคับให้เปิด = กฎที่จะโดนลืม — ต้องย้ายไปไฟล์ที่ระบบโหลดให้เอง

### เทส
`mock/test/kbk-flow.html` — เปิดในเบราว์เซอร์รันได้ ครอบทุกเฟสของ กบค. **60/60 ผ่าน**

### โครงหน้าเว็บ (7 ส.ค.)
- **`index.html` เหลือ 2 การ์ด** — เปิดต้นแบบ (mock หลัก) + Design System · เดิมหน้าหลักไม่มีการ์ดของ mock หลักเลย ถูกแทนที่ด้วย Outcome Dashboard ไปก่อนหน้า ตอนนี้เอากลับมาเป็นการ์ดแรก
- **`more.html` (ใหม่)** — 9 รายการที่เหลือ จัด 3 กลุ่ม: ต้นแบบ&เดโม · วัดผล&ข้อมูลอ้างอิง · เครื่องมือ&ซอร์ส
- ยังเข้าถึงทุกหน้าได้เหมือนเดิม แค่ย้ายที่ · ตรวจปลายทางลิงก์ 13 อันแล้วไม่มี 404

### CI/CD — ย้าย GitHub Pages ไป deploy ด้วย Actions (7 ส.ค.)
`.github/workflows/pages.yml` + สลับ `build_type` เป็น `workflow`

**เหตุผล** — legacy branch build พังติดกัน 4 ครั้งโดยให้ log แค่ `"Page build failed."` และ commit เดียวกันที่เคย build ผ่านตอนเช้ากลับมาพังตอนบ่ายทั้งที่ไฟล์ไม่เปลี่ยน

**ได้อะไร** — log เต็มทุกขั้น · กด re-run เองได้ไม่ต้อง push commit เปล่า · ไม่ผ่าน Jekyll จึงไม่มีขั้นแปลงไฟล์ (`.nojekyll` ไม่จำเป็นแล้ว) · ต่อยอดให้รันเทสก่อน deploy ได้ · repo public → Actions ฟรี

⚠️ **6–7 ส.ค. GitHub ล่มจริง** (`Actions` + `Pages` = major_outage, incident ตั้งแต่ 6 ส.ค. 15:22 UTC) — deploy จึงยังค้าง `pending` ไม่ใช่ปัญหาที่โค้ด · `git push` ใช้ได้ปกติตลอด (Git Operations operational)
**บทเรียน:** เจออาการ build พังโดยไม่มีสาเหตุชัด ให้เช็ค githubstatus.com ก่อนไล่หาในโค้ด — รอบนี้เสียเวลาไล่ผิดทางหลายรอบ (ลบ tag ภาษาไทย · เช็ค `.nojekyll` · เช็ค Liquid syntax)

### ข้อมูลตัวอย่าง + ถ้อยคำ (7 ส.ค.)
- **คิวงาน กบค. 2 → 10 ใบ** ครอบทุกสถานะ: รอรับเรื่อง ×3 · จัดอะไหล่ · รอใบเสนอราคาอู่ · ดำเนินการซ่อม · นัดรับรถ · ส่งกลับให้ซ่อมเอง · ปิดงานแล้ว — กระจาย 4 จังหวัด · ครบ 4 ประเภทงบ · ครบ 3 ยี่ห้ออุปกรณ์
- **เลิกใช้คำว่า "จอง" ฝั่งผู้แจ้ง** → "เลือก" (จองอะไหล่→เลือกอะไหล่ · สั่งของ→เลือกไว้ (ของหมด ต้องสั่งซื้อ) · จองแล้ว→เลือกไว้) ยังเห็นสถานะของในคลังเหมือนเดิม
  **เหตุผล:** ขั้นนี้ผู้แจ้งแค่ระบุว่าคาดว่าต้องใช้อะไร ระบบยังไม่ตัดสต็อกจริง — ตัดจริงตอน กบค. ยืนยันรายการ

### จุดย้อนกลับ
- tag **`ก่อนแยกหน้า`** = ก่อนแยกขั้นตอนเป็นหน้า
- กิ่ง **`แยกหน้า-กบค`** = เวอร์ชันแท็บที่ทิ้งไป (รายละเอียดถูกย่อ — ไม่ใช่ที่ต้องการ)

### 🔧 ขั้นยืนยันรถ + แผนเดินทางหลายใบ (10–11 ส.ค. 2569)

> รายละเอียดเต็มอยู่ที่ [`docs/superpowers/specs/2026-08-10-confirm-vehicles-design.md`](docs/superpowers/specs/2026-08-10-confirm-vehicles-design.md)

**เฟส 1 ทำครบทั้งเฟสแล้ว** — ลำดับใหม่ `ยืนยันรถ → เบิก/จัดหาอะไหล่ → แผนเดินทาง → ทวน + ยืนยัน`

| เคาะกับเจ้าของงาน | ผล |
|---|---|
| ลำดับขั้น | **ยืนยันรถมาก่อนเบิกอะไหล่** — ยอดอะไหล่คิดจากคันที่ยืนยันแล้วเท่านั้น (พิสูจน์: 488 → 409 หลังตัดรถ 2 คัน) |
| ผู้ตอบคำขอยืนยัน | **หน่วยงานเจ้าของรถ** · กำหนดตอบ 7 วัน (ตั้งค่าที่ Admin) |
| ไม่ตอบตามกำหนด | ระบบไม่ตัดรถเอง — ให้ กบค. ตัดสิน · **verdict ชนะ answer เสมอ** |
| แผนเดินทาง | **หลายใบต่อหนึ่งแผน** สร้างอิสระ เลือกรถเข้าใบเอง · ช่วงเวลาระดับใบ · **วันนัดรายคัน** |
| ตอบรับวันนัด | หน่วยงานเจ้าของรถตอบต่อใบ · เลือกวันได้เฉพาะในช่วง หรือปฏิเสธ+เหตุผล · **กรย. รับสำเนา ไม่ต้องกด** |
| จบเฟส 1 | จัดรถเข้าใบครบทุกคัน + ทุกใบตอบรับ |

**บั๊กที่เจอและแก้ระหว่างทาง**
- `plan-new.js` — `persist()` เรียกตัวเองแทน `MYD.savePlan()` ⇒ `RangeError` ทุก handler **บันทึกอะไรไม่ได้เลย** (หลุดตั้งแต่ 8 ส.ค. และอยู่บน main) · hotfix + deploy แล้ว
- คลิกหัวข้อบน stepper ข้ามขั้นได้ — `.locked` เป็นแค่สไตล์ ไม่ได้กันจริง · อุดแล้ว
- `.f sp3` ที่ใช้อยู่ **ไม่มีในระบบ** (มีแค่ `.sp2`/`.sp4`)

**`SCHEMA_VERSION` 7 → 8** (`plan.travelPlan` ใบเดียว → `plan.trips[]`)

**เอกสารที่ sync แล้ว** — `Diagram/01-บำรุงรักษาตามวาระ/` ครบ 7 ไฟล์ (เปลี่ยนชื่อ `02-` เป็น `02-เฟส1-ยืนยันรถ-เบิกจัดหา-แผนเดินทาง.md` ตามลำดับใหม่) · `skeleton-data.js` · `plan.md` ทั้งสองไฟล์

**เครื่องมือใหม่** — `design-system/check-spacing.js` (จับคอมโพเนนต์ติดกัน 0px) · `Diagram/check-mermaid.js` (ผัง parse/render ผ่านจริง เดิมอยู่แค่ใน scratchpad)

### 🎨 Design System — เทียบกับ Figma ทั้งไฟล์ (11 ส.ค. 2569)

**ปัญหาเดิม:** Figma MCP บนแพลน Starter ติด rate limit ที่ ~4 call ต่อรอบ ⇒ 33 node ที่เจ้าของงานส่งมา ประเมินแล้วต้องใช้ **8 รอบโควตา** ถึงจะอ่านจบ

**ทางออก:** ใช้ **Figma REST API** แทน — `GET /v1/files/{key}` ดึงทั้งไฟล์รวดเดียว **89 MB · 43 หน้า · 58,331 node · component 3,304** เก็บใน scratchpad แล้ววิเคราะห์ในเครื่อง (ไม่เอาเข้า repo)
token เป็น PAT scope `file_content:read` เก็บที่ `~/.figma-token` (`chmod 600`) **นอก repo**
⚠️ `/v1/variables/local` เป็น **Enterprise-only** ⇒ อ่านชื่อ Figma Variable ไม่ได้ ได้แต่ค่าดิบบน node

**สิ่งที่การเทียบสี 142 ค่าจับผิดได้** (`tokens.css` v0.2 → v0.3)

| token | เคยเป็น | ของจริง |
|---|---|---|
| `--secondary-600` | `#1B4DB1` | **`#172B85`** — ผิดคนละสี |
| `--brand-50` · `--brand-25` · `--brand-200` | `#FEEBFB` · `#FFF5FD` · (ไม่ตรง) | `#FDEEFC` · `#FDF2FA` · `#F8BFF4` |
| เพิ่มใหม่ | — | `--brand-100` · `--success-700/-500/-100` · `--chart-1..6` |

- `#E134C1` ที่เกือบเข้าใจผิดว่าเป็นสีแบรนด์ — ตรวจแล้วใช้เฉพาะหน้า Charts ⇒ **สีข้อมูล ไม่ใช่สีแบรนด์**
- `#067647` ที่ค้างเป็น hex ดิบใน `buttons.html` — พบว่าใช้จริง 107 ครั้ง ⇒ ตั้งเป็น `--success-700` แล้วเรียก token
- **provenance เหลือ `✔` 40 · `⚠` 6** — ไม่มี `~` (เดาจาก runtime) แล้ว · 6 ตัวที่ `⚠` คือของที่เราเพิ่มเอง ไม่มีในไลบรารี

**วิธีกันเอกสารเพี้ยน** — เขียนสคริปต์เทียบ hex ที่โชว์ในเอกสารกับ `tokens.css` แทนการไล่ดูด้วยตา
จับได้ 2 รอบ: ตาราง gray ใน README ค้างเป็น Untitled UI **v1** ผิด 9 ค่า · แล้วหลังอัปเดต token ก็จับ README/`index.html` ที่ยังโชว์ค่าเก่าอีก 6 จุด
`SOURCES.md` เขียนใหม่ทั้งไฟล์ — เดิมบอก "อ่านแล้ว 1 · ค้าง 32" ซึ่งไม่จริงแล้ว

### 🔁 figma-export — ส่งหน้าจอ HTML เข้า Figma (11 ส.ค. 2569 ค่ำ)

**โจทย์:** ไฟล์ Figma ที่ดีไซเนอร์แก้ได้จริง โดยโค้ดยังเป็นต้นทางเสมอ — Figma REST API **เขียน node ไม่ได้** ⇒ ใช้ **Plugin API** (dev plugin บน Figma desktop) · ท่อนี้ไม่ใช้ Figma token และไม่ใช้ MCP เลย

- ท่อครบ 3 ท่อน: `1-extract.js` (playwright เดิน DOM หน้า JS-rendered) → `2-map.js` (แปลงเป็น `spec.json` — geometry บอกโครง · CSS บอกหน้าตา) → `plugin/` สร้าง frame + auto-layout
- **4 หน้าจอออกเป็น frame แล้ว** (`index` · `plan-new` · `supplies` · `confirm`) — `plan-skeleton` พักไว้ตามที่เจ้าของงานสั่ง · `admin` ตัดออก (3,770 node ใหญ่เกิน)
- ไอคอน Material Symbols 48 ตัว ดึง SVG จาก Google Fonts (cache ที่ `out/icons.json`) วาดเป็น vector จริงในไฟล์ — คนเปิดไฟล์ไม่ต้องมีฟอนต์ไอคอน
- รันซ้ำชื่อ page เดิม = ล้างของเดิมสร้างใหม่ ไม่ซ้อน · ภาษาไทยเรนเดอร์ปกติ
- ดีไซน์เต็ม + ข้อจำกัดที่เจอ (ฟรี 3 page/ไฟล์ · sandbox ไม่มี `fetch`/`eval` · `localhost` resolve เป็น `::1` ฯลฯ) อยู่ที่ `figma-export/README.md` และ `docs/superpowers/specs/2026-08-11-html-to-figma-export-design.md`

### 🔁 figma-export — Variables + component/variant จริง (12 ส.ค. 2569)

ปิดสองข้อใหญ่ที่เหลือของ spec (หัวข้อ 4.1–4.2) — ไฟล์ Figma ที่ได้ตอนนี้ "ดีไซเนอร์แก้ได้จริง" ครบเงื่อนไข: auto-layout · instance สลับได้ · fill ผูก variable

- **Variables 99 ตัว** (`tokens-vars.js`): อ่าน `tokens.css` แยก 3 collection ตามชั้นในไฟล์ (primitive 76 · semantic 11 · component 12) · `var(--x)` เป็น alias จริง ⇒ แก้ `brand/600` ที่เดียวทั้งไฟล์เปลี่ยน · ชื่อ = ชื่อ CSS เปลี่ยน `-` เป็น `/` map กลับหาโค้ดได้ · ปลั๊กอินผูก fill/stroke/สี text/radius ทุกจุดที่ค่าตรง token · รันซ้ำอัปเดตตัวเดิมไม่งอก
- **Component 6 ชุด + specimen 6 + icon 10** (`components-map.js`): ชื่อ layer เป็นกุญแจ → tree-diff เทียบตัวนิยาม ตรงก็เป็น **instance 121 จุด** พร้อม override (ข้อความ/สลับไอคอน/ซ่อนส่วนเกิน) โครงไม่ตรงคงเป็น frame (cell 50 จุด — เซลล์มี badge/สองบรรทัด ฯลฯ รายงานใน `map-report.json`) · ชุดสถานะจับไม่ได้ (card, form field, review zone …) เป็น specimen ในหน้า Foundations
- **เทสไม่ต้องเปิด Figma**: `test-plugin.js` mock Plugin API แล้วรัน `plugin/code.js` ทั้งไฟล์ — ตรวจข้อความครบตัวต่อตัว (273/273) · alias ครบ 18 · สีแบรนด์ไม่หลุดผูก · รันซ้ำ variable ไม่งอก — **ผ่าน 12/12**
- ยังไม่ได้กด "โหลด + สร้าง" ใน Figma desktop จริง — mock จับตรรกะได้แต่จับหน้าตา/ฟอนต์/ขนาด svg ไม่ได้ ต้องเปิดไฟล์จริงเทียบ screenshot (เกณฑ์ตาดู ข้อ 6 ของ spec)

### 🔄 เปลี่ยนทางหลักเป็น FigJam capture board (12 ส.ค. 2569 บ่าย)

**เจ้าของงานเคาะ:** ไม่เอาไฟล์ Figma design เป็นหลักแล้ว — เอา **FigJam ที่เป็น capture เรียงต่อกัน** แทน (ท่อ design ที่ทำเช้านี้ **พัก** ไม่ทิ้ง) และหลังเห็น capture รอบแรกสั่งเพิ่ม: อยากเห็น "โฟลว์สร้างแผน" เป็นรายหน้าจอ ไม่ใช่แค่สถานะแรกของแต่ละไฟล์

- **capture 3 ชุด** (playwright ขับจริง เริ่มจากล้าง localStorage ให้ default เสมอ):
  - `figjam-capture.js` — ทุกหน้า 25 หน้า 30 รูป (สไลซ์หน้าสูงเกิน 4,000px เพราะ Figma รับรูปไม่เกิน 4096px/ด้าน)
  - `flow-plan-capture.js` — โฟลว์สร้างแผน/ออกเลขงาน **8 หน้าจอ**: รายการแผน → ขั้น 1 (ว่าง/เลือกรถแล้ว) → ขั้น 2 (2 มุมมองจัดกลุ่ม) → ขั้น 3 → ได้เลขงาน → กลับรายการแผน
  - `flow-after-issue-capture.js` — โฟลว์ต่อ **7 หน้าจอ**: กบค. ส่งคำขอยืนยัน → พัสดุ (รายการ/เปิดเอกสาร/รับทราบ) → หน่วยงานเจ้าของรถตอบรายคัน
  - บทเรียน: ห้ามใช้ `fullPage:true` — element ที่ fixed (โลโก้) โดนวาดซ้ำกลางหน้า ต้องขยาย viewport เท่าหน้าจริงแทน
- **เข้าไฟล์ทาง dev plugin ใหม่ `figjam-plugin/`** (MCP Starter จำกัด **20 call/เดือน — เดือนนี้หมดแล้ว** ตอนติดคือจังหวะจัดวางพอดี รูป 30 ใบที่อัปโหลดค้างในบอร์ดจะถูกปลั๊กอินเก็บกวาดให้): `3-figjam-board.js` รวม manifest → `board.json` (8 section · 40 หน้าจอ · 45 รูป) → ปลั๊กอินสร้าง section+ป้าย+ลูกศร flow · รูปใน FigJam = ShapeWithText ใส่ IMAGE fill (FigJam ไม่มี rectangle/frame) · `serve.js` รองรับ path ย่อย + content-type แล้ว
- **เทส `test-figjam-plugin.js` ผ่าน 9/9** (จำนวนครบ/ลูกศรผูกปลาย/section ครอบลูก/รันซ้ำไม่ซ้อน/เก็บกวาด MEDIA)
- **สโคปบอร์ด (เจ้าของงานย้ำ):** "ทำแค่ flow ที่บอก ไม่ใช่ทั้งหมด" ⇒ `board.json` ค่าเริ่มต้นเหลือ **โฟลว์สร้างแผนอย่างเดียว (1 section · 8 หน้าจอ · ลูกศร 7)** — ชุด after-issue/หน้ารวมยังอยู่เป็นตัวเลือก `--after`/`--pages`/`--all` เมื่อถูกขอเท่านั้น
- **ผู้ใช้กดสร้างบอร์ดสำเร็จ** ("ได้แล้ว ถูกต้อง") — โฟลว์สร้างแผน 8 หน้าจอขึ้นบอร์ดจริงแล้ว
- **เพิ่มแผนผัง flowchart (เจ้าของงานเคาะช่วงบ่าย):** อยากได้ผังกล่อง+ลูกศรของโฟลว์สร้างแผนแบบแก้บนบอร์ดได้ ⇒ `4-figjam-diagram.js` เรนเดอร์ผัง mermaid `Diagram/01-บำรุงรักษาตามวาระ/01-ออกเลขงาน.md` ใน Chromium แล้วสกัด geometry จาก SVG (mermaid เป็นคนจัด layout) ส่วน**เส้นเชื่อม/ชนิด node parse จากซอร์สตรงๆ** (DOM v11 แยกชนิดยาก + ลำดับ edgeLabels ไม่ตรง edgePaths เมื่อบางเส้นไม่มีป้าย — เจอจริง) → section ใหม่บนบอร์ด: node = ShapeWithText ตามชนิด (process/decision/stadium/subroutine) + ConnectorNode ผูกปลายจริง ลาก node แล้วเส้นตาม · เลนเป็น shape สีอ่อนรองพื้น · เทสขยายเป็น **11 ข้อ ผ่านหมด** (node ผัง 13 · เส้น 16 · ป้ายเส้นครบ)
- **โฟลว์บนบอร์ดเพิ่มทีละอันตามสั่ง:** สร้างแผน (ผัง+8 จอ) → วางแผนการเดินทาง (8 จอ) → **แจ้งซ่อมฝั่งผู้แจ้ง (8 จอ — wizard 5 ขั้นของ mock/ จนส่งใบ+รอหัวหน้าอนุมัติ)** ⇒ board.json ปัจจุบัน 4 section · 24 หน้าจอ · เทส 11 ข้อผ่าน
- ไฟล์บอร์ด: `mDn6j6wVtdn0DMtFwiDsRQ` (anu phetcharat's Team) — https://www.figma.com/board/mDn6j6wVtdn0DMtFwiDsRQ

### 🧪 ท่อ design กลับมาใช้ — โฟลว์แจ้งซ่อมเป็น spec แยก (12 ส.ค. 2569 เย็น)

**เจ้าของงานสั่ง:** "ลองให้เขียนไปที่ Figma (design) ซัก 1 flow — เอาแจ้งซ่อมที่เพิ่งเสร็จใน FigJam" + ถามว่าท่อ/JSON ของ Figma กับ FigJam แยกกันได้ไหม ⇒ **แยกไฟล์กันเด็ดขาด**: ท่อ FigJam = `board.json`+`figjam-plugin/` · ท่อ design = `spec.json`(yearly)/`spec-report.json`(แจ้งซ่อม)+`plugin/` — `serve.js` ตัวเดียวเสิร์ฟหมด regen ฝั่งไหนไม่กระทบอีกฝั่ง

- `walkDom` แยกจาก `1-extract.js` เป็นโมดูลแชร์ `dom-walk.js`
- **`flow-report-extract.js` ใหม่** — คู่แฝด `flow-report-capture.js` (จังหวะกดเดียวกันตัวต่อตัว) แต่เก็บ DOM+computed style ทีละ state → `dom-report-01..08.json` (node รวม 1,729 · หนักสุด 342/state ไม่ชนเพดานแบบ admin 3,770)
- **`2-map.js --report`** → `spec-report.json` (406KB · หน้า `Screens — แจ้งซ่อม (ฝั่งผู้แจ้ง)`) — ค่าเริ่มต้นยังเป็น yearly 4 หน้าเหมือนเดิม · `0-icons.js` เก็บไอคอน mock เพิ่มเป็น 58 ตัว · `mapping.js` เพิ่มชื่อคลาสการ์ดรถ `.veh`/`.vehicle-*` (unknown เหลือ 0)
- component จับจาก mock ได้เอง: btn 3 · badge 4 · stepper step 3 · sidebar item 2 · specimen 4 · icon 44 → **instance 144 จุด**
- **`test-plugin.js --report` ผ่าน 12/12** (หน้าจอ 8 · variable 99 · ข้อความครบ 451 ชิ้น · paint ผูก variable 880 จุด · สีแบรนด์ไม่หลุด · รันซ้ำไม่งอก) — ท่อ yearly เดิม regen+เทสซ้ำแล้วยังผ่าน

### 🎯 Phase 2: จูนทั้งโปรเจ็คให้ตรงไลบรารี PEA (12 ส.ค. 2569 ค่ำ)

**เจ้าของงานสั่ง:** "แก้หน้า prototype ให้ตรง design แล้วไล่แก้ทั้งหมดให้ตรง · นับจากนี้ยึดตาม design ไม่สร้าง/กำหนดขึ้นเอง · เช็คทั้งโปรเจ็ค **ยกเว้น admin ที่ปรับแต่ง**"

- **`design-system/compare-figma.js` ใหม่** — เทียบ components.css กับ `.figma-extract/` (รันซ้ำได้ ไม่ต่อ Figma) ครอบ input/select/textarea/badge/tag/table/checkbox/radio/breadcrumb/ปฏิทิน + สรุป radius/typography ทั้งไลบรารี
- **แก้ตามที่เจอ (README design-system §8 v0.12 ละเอียดสุด):** input 46→44 r10→8 pad14 +shadow-xs · label 500/gray-700 · error border→error-300 · readonly เส้นประ→ทึบ · **badge มีขอบโทน 200 ตัวโทน 700 14/500** · chip=Tag lg r6 · ตารางหัวขาว 12/600 เซลล์ 16/20 · checkbox/radio 20px · ปฏิทินวัน 40 กลม · breadcrumb 14/600 · `--fs-body/sm/xs` 15/13/11→**16/14/12** · token ใหม่/แก้: `--success-200 #ABEFC6(แก้)` `--error-300` `--warning-800` `--gray-25` `--rounded-sm 6` `--shadow-xs`
- **กวาดทั้งโปรเจ็ค 299 จุด 21 ไฟล์** (ขนาดเศษ 13.5/12.5/14.5/11.5/15/13/11px → token · r10 → `--rounded-md` · h46 → 44) — ยกเว้น admin-config/admin.html/backup/test ตามคำสั่ง · บั๊ม `?v=20260812-ds12` 25 ไฟล์
- **ตรวจแล้ว:** grep hex/emoji ว่าง · skeleton test 27/27 · check-spacing ผ่าน · smoke 17 หน้าไม่มี pageerror + วัดจริง (body 16 · input 44/r8/เงา xs · label 500) · capture 3 โฟลว์ใหม่ + spec ทั้งคู่ regen + เทส plugin/figjam ผ่านหมด · ตรวจตาภาพ badge/chip ตรงไลบรารี

### 🚢 ยกชุดให้ตรง VMS Plus ตัวจริง — เข้า main + deploy (14–17 ส.ค. 2569)

**เจ้าของงานแจ้ง 17 ส.ค.:** *"จากแผนบำรุงรักษา ยังไม่ตรงตาม design เช่น BG ต้องสีขาว แต่ทำมาสีเทา"*

- **สาเหตุไม่ใช่โค้ดผิด แต่คือของที่แก้ไว้ยังไม่ถึงหน้าเว็บ** — งาน alignment 6 คอมมิตค้างบนสาขา `design/vmsplus-runtime-alignment` ไม่ได้ merge เข้า main ⇒ Pages ยังเสิร์ฟ `body{background:#E9EAEC}` + `.work{background:var(--gray-25)}` ของเดิม · ซ้ำร้ายสาขานั้นแก้ `components.css`/`tokens.css` ไป 4 คอมมิต **โดยไม่บั๊ม `?v=`** (ค้างที่ ds15 ส่วน main เดินไป ds17 แล้ว) ⇒ ต่อให้ merge เฉยๆ เบราว์เซอร์ที่แคชไว้ก็ยังเห็นของเก่า
- **งานที่อยู่ในสาขานั้น** (วัดจาก `vmsplus-dev` ไม่ได้เดาจากไลบรารีอย่างเดียว): พื้นขาวทั้ง `body`/`.shell`/`.work` · หัวเรื่อง 32/600 · sidebar 96→80px · gray ramp กลับเป็น Untitled UI v1 · `--color-text-primary`→`#000` · ตารางถอดกรอบ/หัว gray-100 สูง 56/เซลล์ pad 8-16 · badge 12/600 + `.b-info`/`.b-neutral`/`.dot` · `.search` เป็นกล่องจริง h40 · ฐานปุ่มคุมสูง 40 + `.btn-tb`/`.btn-td`/`.btn-icon`/`.btn-circle`/`.pager`/`.dt-action` · `.tab-btn` สูง 48 — เทียบ element ต่อ element **17/20 ตรงทุกค่า** (เดิม 0)
- **merge สะอาด ไม่มี conflict** (`git merge-tree` ตรวจก่อน) — ของ main 6 คอมมิตเป็นงานฟีเจอร์ทีม (modal เลือกวิธีซ่อม · นัดหมายวันซ่อม · เบิกอะไหล่) แตะ `components.css` แค่ 2 จุดเล็ก (`.f.err` ครอบ `.search input` · `.job.sel`)
- **บั๊ม `?v=20260817-ds18` 24 หน้า** (เว้น `mock/backup.html`) + README design-system §8 เพิ่ม v0.18
- **ตรวจก่อน push:** `verify-tokens.js` ผ่านทุกค่า · `skeleton-data.test.js` 27/27 · grep hex/emoji ว่างทั้งคู่ · เรนเดอร์จริง 6 หน้าของ `maintainance-yearly/` — `body`/`.shell`/`.work`/`.side`/`.topbar` = `rgb(255,255,255)` ทุกหน้า ไม่มี pageerror ไม่มี response ≥400
- **push `design/vmsplus-runtime-alignment` → main** (fast-forward `c5b6b95..7e0d2ee`) · Actions deploy สำเร็จ · **ตรวจซ้ำบนเว็บจริงหลัง deploy: 6 หน้า PASS ทั้งหมด** พื้นขาวจริง โหลด ds18 จริง
- บทเรียน: แก้ `components.css`/`tokens.css` แล้ว**ต้องบั๊ม `?v=` + ลง changelog ในคอมมิตเดียวกัน** ไม่ใช่รอตอน merge — ไม่งั้นสาขาที่ยังไม่ merge จะกลายเป็น "แก้แล้วแต่ไม่มีใครเห็น"

### 🗓️ ทำแผนรอบใหม่: เลือกไตรมาสก่อน · ตัดขั้นอะไหล่ · รถบอกจังหวัด/สถานะ (17 ส.ค. 2569)

**เจ้าของงานสั่งรวด 3 เรื่องระหว่างรอบเดียวกัน** — ทุกเรื่องกลับข้อตัดสินเดิมที่บันทึกไว้ในผัง จึงแก้ผัง mermaid ในคอมมิตเดียวกันทุกครั้ง

1. **"ต้องเลือกไตรมาส แล้วค่อยเลือกรถเข้าไตรมาส"** ⇒ **1 แผน = 1 ไตรมาส** (ทำครบปี = 4 แผน 4 เลขงาน) · ขั้น 1 มีตัวเลือกไตรมาสด้วย `.seg`/`.sg` ของ design system **ไม่ตั้งค่าเริ่มต้นให้** แต่ติดป้าย "· ตอนนี้" ที่ไตรมาสปัจจุบัน · **ยังไม่เลือก = ยังไม่แสดงบล็อกเลือกรถ** · หัวข้อบล็อกรถบอกไตรมาสที่กำลังจัด · เปลี่ยนไตรมาสทีหลังไม่ล้างรถที่เลือก
   - เคยเสนอทางเลือก "แผนเดียวครอบ 4 ไตรมาส" แต่ตกไป เพราะปลายน้ำทั้งเส้น (พัสดุ → ยืนยันรถ → แผนเดินทาง → ตรวจรับ) ทำงานกับแผนเป็นก้อนเดียว ถ้าครอบ 4 ไตรมาสทุกขั้นต้องแตกรายไตรมาสอยู่ดี = 4 แผนแบบนี้ แต่ต้องรื้อโครง
   - `hasContent()` ต้องนับ quarter เป็นเนื้อ ไม่งั้นเลือกไตรมาสอย่างเดียวแล้วร่างไม่ถูกบันทึก
2. **"ไม่มีเลือกอะไหล่ในจังหวะสร้างแผน ตัดออกไป"** ⇒ wizard **3 ขั้น → 2 ขั้น** · ตัด `renderStep2`/`bindStep2`/`groupLines`/`GROUP_MODES`/`state.grp` · **ระบบยังคำนวณรายการอะไหล่จากรถที่เลือกให้เอง** ป้อนหน้าสรุป + เอกสารพัสดุเหมือนเดิม แค่ไม่ให้แก้ · `lineTable` เหลือโหมดอ่านอย่างเดียวแต่**คงคอลัมน์ที่ 6 (ว่าง) ไว้** เพราะ `.itbl` เป็น `table-layout:fixed` ที่จงใจให้คอลัมน์ตรงกันข้ามตารางในหน้าเดียวกัน
3. **"หน้าเลือกรถ อยากให้แสดงสถานะรถปัจจุบัน เช่น ซ่อมอยู่ หมดสภาพการใช้งาน"** ⇒ สถานะ **3 → 6 แบบ** (พร้อมเข้าแผน · รออนุมัติ · ซ่อมอยู่ · โอนย้ายหน่วยงาน · หมดสภาพการใช้งาน · รอจำหน่าย) · ตารางรถเพิ่ม **จังหวัด + หน่วยงานเจ้าของรถ** (จังหวัดยกจาก `OWNER_DEPTS_BY_REGION` ที่มีอยู่แล้ว ไม่ได้ตั้งชื่อขึ้นใหม่) · **รถ 4 สถานะที่ไม่พร้อมเลือกไม่ได้** — ปิดช่องติ๊ก + บอกเหตุผลใต้ป้าย
   - `MYD.canJoinPlan()` เป็นแหล่งความจริงที่เดียว ใช้ทั้งตอนเรนเดอร์และตอน "เลือกทั้งเขต/ภาค/ทั้งหมด" — ถ้าแยกกันจะเจอบั๊กคลาสสิก 2 อัน: เลือกหมู่ติ๊กรถที่ช่องเดี่ยวปิดไว้ · ช่อง "เลือกทั้งเขต" ไม่มีวันขึ้นเครื่องหมายถูกเพราะนับรถที่ติ๊กไม่ได้รวมไปด้วย
   - ⚠️ **ระบบแนะนำไม่ได้ว่าคันไหนควรอยู่ไตรมาสไหน** — ข้อมูลรถมีแค่เลขไมล์/ชม.เครื่องสะสม ไม่มีวันซ่อมล่าสุด/รอบครบกำหนด (`triggerText` เขียนกำกับไว้เองว่า display only) ⇒ คนกำหนดเอง · บันทึกไว้ในผังแล้ว

**design system** เพิ่ม `.tbl .cell-sub` (บรรทัดรองในเซลล์ แทน `style="font-size:12px"` ที่เขียนซ้ำ ⇒ ขนาดหลุดสเกล) + `.tbl tr.vrow-blocked` · บั๊ม `?v=20260817-ds19` · README §8 v0.19

**สะกดผิดทั้งโปรเจกต์** — `ไทรมาส` → **`ไตรมาส`** 74 จุด 11 ไฟล์ (ไตร = สาม · ไทร = ต้นไม้) ขึ้นบนหน้าจอที่ลูกค้าเห็น · แยกคอมมิตให้ย้อนง่าย

**ตรวจแล้ว** ขับเบราว์เซอร์จริง **30 ข้อผ่านหมด ทั้งบนเครื่องและบนเว็บ deploy** · `skeleton-data.test.js` 27/27 (ปรับตัวเลขคาดหวัง: 10 จอ · issue 2 จอ · แหล่งข้อมูล 36) · ผัง mermaid `parse()` ผ่านจริงทั้ง 7 บล็อก · verify-tokens ผ่าน · grep hex/emoji ว่าง

### 🔁 กลับลำ: แผนใบเดียวครอบทั้งปี รถแยกรายไตรมาส (17 ส.ค. 2569 — รอบบ่าย)

**เจ้าของงานดูหน้าจริงแล้วสั่งกลับ** จาก "1 แผน = 1 ไตรมาส" ที่เพิ่งทำเสร็จช่วงเช้า เป็นทางเลือก **ข** ที่เคยเสนอไว้ — *"ตอนเลือกแผน ต้องเลือกแต่ละไตรมาสให้ครบก่อน ค่อยกดถัดไปได้"* + *"ตอนสร้างแผนการเดินทาง จะแสดงรถในไตรมาสนั้นๆ และเลือกออกจากไตรมาสนั้น เลือกไปไว้ไตรมาสอื่น หรือแยกไว้แบบยังไม่ระบุไตรมาสเลย"*

> บทเรียน: ตอนถามครั้งแรกเจ้าของงานเลือก ก (ตามที่แนะนำ) แต่พอ**เห็นหน้าจริง**แล้วเปลี่ยนเป็น ข — คำอธิบายทางเลือกเป็นตัวหนังสือไม่พอ ครั้งหน้าถ้าทางเลือกต่างกันที่ *"หน้าตาการใช้งาน"* ควรทำให้ดูก่อนตัดสิน

**โครงข้อมูล** — จุดสำคัญคือ **ไม่รื้อปลายน้ำ**
- `plan.byQuarter = { Q1..Q4, none }` · `none` = อยู่ในแผนแต่ยังไม่ระบุไตรมาส
- `plan.selectedVehicleIds` กลายเป็น **ผลรวมทุกถังที่ระบบเขียนให้เอง** ⇒ พัสดุ · ยืนยันรถ · เฟส · admin ที่สนใจแค่ "รถทั้งหมดในแผน" **ไม่ต้องแก้เลย** (เดิมประเมินว่าต้องแตะ 76 จุด 8 ไฟล์ — จบที่ 4 ไฟล์)
- เขียนถังได้ทางเดียว: `setQuarterVehicles()` / `assignVehicle()` → บังคับ **รถอยู่ได้ไตรมาสเดียว** (ติ๊กไตรมาสใหม่ = ย้ายออกจากเดิม) ไม่งั้นยอดอะไหล่นับซ้ำ
- `plan.workNumbers = {Q1..Q4}` ออกครบ 4 ใบพร้อมกัน · `SCHEMA_VERSION` 8 → 9 (breaking ⇒ storage เก่า auto-reset)

**หน้าจอ**
- ขั้น 1: แท็บไตรมาสพร้อมยอดรถต่อไตรมาส · ปุ่มถัดไปปิดจนครบทุกไตรมาส + กล่องเตือนบอกว่าขาดไตรมาสไหน · ตารางรถเพิ่มคอลัมน์ "จัดไว้แล้ว" (อยู่ไตรมาสอื่น / ยังไม่ระบุ)
- ขั้นสรุป: ตารางแจกแจงรายไตรมาส + เลขงาน 4 ใบ
- **แผนเดินทาง (เฟส 1 ขั้น 3): เลือกไตรมาสก่อน เห็นเฉพาะรถของไตรมาสนั้น** · แต่ละแถวย้ายไตรมาส/พักไว้ยังไม่ระบุ/เอาออกจากแผนได้ — ย้ายแล้วถอดออกจากใบเดินทางที่ถืออยู่ด้วยเสมอ ไม่งั้นใบของไตรมาสนี้จะค้างรถที่ย้ายไปแล้ว

**design system** `.note` (+warn/ok/info) · `.sg-sub` · `.worknos`/`.workno` · บั๊ม ds20 · README §8 v0.20

**ตรวจแล้ว** ขับเบราว์เซอร์จริง **26 ข้อผ่าน ทั้งบนเครื่องและบนเว็บ deploy** · skeleton 27/27 · mermaid parse 7/7

### 🗓️ แผนล่วงหน้า 2 ปี + รอบทบทวน + ฟอร์มแผนเดินทาง (17 ส.ค. 2569 — รอบเย็น)

**1. แผนทำล่วงหน้า 2 ปี + รอบทบทวน** — *"ทำแผนปีนี้จะทำของปี 2571 · ปลายปี 2570 จะมี revise อีกรอบ"* + *"flow การ revise คือการสรุปแผนก่อนออกปฏิบัติงาน"*
- ปฏิทินปีงบทั้งหมดเป็น **pure ใน `mock-yearly.js`** (ไฟล์นั้นห้ามเรียก `Date` ตามกติกาหัวไฟล์) — `fiscalYearOf` · `planningYearFrom` = fy+2 · `reviseRoundsFor` · `planStage`
- **นาฬิกาอยู่ฝั่งเบราว์เซอร์** (`common.js`): `simNow()`/`fiscalNow()` อ่านวันที่จำลองจาก localStorage · `nowTh()` ใช้ตัวนี้แล้ว ไม่มีใครเรียก `new Date()` ตรงๆ อีก
- **ตัวจำลองวันที่บน topbar ทุกหน้า** (5 หมุด) — ขาดไม่ได้ เพราะเหตุการณ์ทั้งหมดอยู่ในอนาคต จะเดโมรอบทบทวนไม่ได้เลย
- สถานะแผนไล่ตามเวลาเอง: `ฉบับร่าง → รอถึงรอบทบทวน → ถึงรอบทบทวน → สรุปแผนแล้ว → ปีนี้มีผล ออกปฏิบัติงาน → ปีงบผ่านไปแล้ว`
- โหมดทบทวน = เปิด wizard เดิมกลับมาแก้รถ/ไตรมาสได้ · ปุ่มสุดท้ายเป็น **"สรุปแผนก่อนออกปฏิบัติงาน"** → `commitRevision()` เก็บเวอร์ชัน + บันทึกว่าเพิ่ม/ถอด/ย้ายกี่คัน · **เลขงานเดิมไม่เปลี่ยน**
- 🐞 **เจอตอนเทส:** ถ้าทำแผนช่วง ก.ค.–ก.ย. (= ปลายปีงบพอดี อย่างวันที่ทำงานนี้) แผนที่เพิ่งสร้างเสร็จจะเด้งเข้ารอบทบทวน*ของตัวเอง*ทันที ซึ่งไม่มีอะไรให้ทบทวน ⇒ ให้รอบเริ่มที่ `createdFY + 1` ⇒ แผนทำ 2569 ใช้ 2571 มีรอบเดียว **ปลายปีงบ 2570** ตรงกับที่เจ้าของงานบอกพอดี

**2. ฟอร์มแผนเดินทาง** — *"หยิบแผนแต่ละไตรมาสออกมากำหนดช่วงการเดินทาง"*
- ระดับใบ: **พนักงาน กบค. ที่ออกไปซ่อม** (เพิ่ม/ลดคนได้ ปกติ 2-3 คน) + เบี้ยเลี้ยง/ที่พัก/เดินทาง (มีอยู่แล้ว)
- ระดับรายคัน: **งานที่จะทำ** (เปลี่ยนถ่าย/ตรวจน้ำมันไฮดรอลิก — เลือกอย่างเดียวหรือทั้งสอง · **ตั้งต้นติ๊กทั้งคู่**) + **สถานที่บำรุงรักษา มี default แก้ได้** (ลบข้อความ = กลับไปใช้ default)
- **default สถานที่** ตามกติกาที่เจ้าของงานให้: รถของหน่วยงานระดับจังหวัด/สาขา → จังหวัดที่สังกัด · **รถของเขต → กรย. เขต N** ⇒ ต้องเพิ่ม `vehicle.ownerLevel` ในข้อมูลจำลอง (ทุก 6 คันเป็นรถเขต 1 คัน) ไม่งั้นกติกาข้อนี้ไม่มีทางแสดงผลได้
- ส่งใบไม่ได้จนกว่าจะมีพนักงาน ≥1 คน และทุกคันเลือกงาน ≥1 อย่าง
- ❓ **ตอบคำถาม "ระบบรองรับ location ได้ไหม":** ตอนนี้สถานที่เป็น**ข้อความอิสระ ไม่มีพิกัด/แผนที่** และไม่มีทะเบียนสถานที่ให้เลือก — ถ้าจะรองรับจริงต้องรู้ก่อนว่า VMS Plus มี master สถานที่ให้ดึงหรือไม่ (ยังไม่เคยเห็น API นั้น) · บันทึกไว้ในผัง 02 แล้ว

**3. เลือกรถได้ทุกสถานะ** — *"ตอนเลือก ทุกสถานะสามารถเลือกได้ แล้วคนสร้างแผนค่อยมาเลือกออก"* ⇒ กลับกติกาตอนเช้าที่ให้ปิดช่องติ๊ก · ถอด `canJoinPlan()`/`blockReason()` เหลือ `statusNote()` = **คำเตือน ไม่ใช่คำห้าม** · ถอด `.tbl tr.vrow-blocked` ออกจาก design system

**ตรวจแล้ว** ขับเบราว์เซอร์จริง **verify-year 44 ข้อ + verify-revise 21 ข้อ ผ่านหมด ทั้งบนเครื่องและบนเว็บ deploy** · skeleton 27/27 · mermaid 7/7 · verify-tokens ผ่าน · ลบชุดเทส `verify-quarter` (โมเดล 1 แผน = 1 ไตรมาส ที่ไม่มีแล้ว) ทิ้ง

> บทเรียนรอบนี้ (เพิ่มจากรอบบ่าย): **ข้อตัดสินเรื่อง UI กลับทางได้เร็วมากเมื่อเจ้าของงานเห็นของจริง** — วันเดียวกลับ 2 เรื่อง (1 แผน/ไตรมาส → แผนทั้งปี · ปิดช่องติ๊กตามสถานะ → เลือกได้ทุกสถานะ) ⇒ อย่าลงแรงกับกติกาที่ยังไม่ได้ให้ดูหน้าจริง และเขียนโค้ดให้กลับทางถูก เช่น รวมกติกาไว้ที่ฟังก์ชันเดียว (`canJoinPlan` อยู่ที่เดียวจึงถอดออกได้ในไม่กี่บรรทัด)

### ค้างอยู่ (รอบถัดไป)
- [x] ~~sync ผัง + skeleton ให้ตรงลำดับใหม่~~ — **เสร็จแล้ว 11 ส.ค.** ผัง 7/7 parse ผ่าน · `skeleton-data.js` จอ 1a/1b/ต้นทาง ตรงกับโค้ด · เทส 27/27
- [ ] **สไตล์ช่องกรอกในตารางของ `confirm.html`** — รอเจ้าของงานเคาะ (ก ช่องเปล่าแบบตอนนี้ / ข ทรีตเมนต์เต็ม / ค เปลี่ยนเป็นการ์ดรายคัน)
- [ ] **เอกสารพัสดุออกก่อนยืนยันรถ** ⇒ ยอดในเอกสารเป็นของแผนเต็ม แต่เบิกจริงน้อยกว่า — รอเคาะว่าจะเป็นประมาณการ หรือมีใบปรับยอด
- [ ] **ไฟล์ `.js` ไม่มี `?v=` กันแคช** — ต้อง hard reload ทุกครั้งหลัง deploy (CSS มีแล้ว JS ยังไม่มี)
- [ ] **หน้าจริงของขั้น "ยืนยันรถ" ยังไม่ผูกสิทธิ์รายบุคคล** — พิมพ์ชื่อผู้ตอบเอง รอเคาะว่าใครในหน่วยงานกดได้
- [ ] **flow2 ยังไม่มิเรอร์** งาน กบค. รอบนี้ — มีแค่ขั้นอนุมัติหัวหน้า
- [ ] คลังอะไหล่ยังไม่ตัด/คืนยอดจริงเมื่อเพิ่ม-ลบรายการ
- [ ] หน้าประเมินความคุ้มค่า `#worth` ยังเป็นโครงเปล่า — รอเคาะเกณฑ์ตัดสิน/ใครกด
- [ ] ขั้นตอนหลังได้ใบเสนอราคาจากอู่ — รอเคาะว่าใครอนุมัติราคา
- [ ] หน้า กรย. · อนุมัติปิดงาน · เรื่องของฉัน ยังเป็น accordion แบบเดิม (ต่างจาก กบค. แล้ว)
- [ ] อะไหล่ผูกได้แค่ 3 รุ่นที่มีในข้อมูลจำลอง — ของจริงต้องมีทะเบียนรุ่น + ตารางความเข้ากันได้
- [ ] **ชื่อประเภทงบ** — ต้องขอค่าจริงจาก `vms_ref_cost_type` (ตอนนี้ใช้ชื่อสมมติ)
- [x] ~~ยืนยัน Pages deploy สำเร็จ~~ — **เสร็จแล้ว** workflow รันผ่านต่อเนื่องตั้งแต่ 7 ส.ค.
- [ ] **พื้นที่มีแค่ 2 จังหวัด** — ต้องเติมอีก 72 จังหวัดจาก `hierarchy-data.json` (ติดเรื่องสระ า/ำ ที่เพี้ยน ต้องไล่แก้ทีละชื่อ หรือ join กับ `mas_department`)
- [ ] **AC-D1 เปิดใหม่** — routing ตามสายงานอนุมัติ (ดูกล่องเตือนที่ AC-D1) · รอเจ้าของงานเคาะ 4 ข้อ ก่อนแตะโค้ด
- [x] ~~**Design System — ยืนยันสีที่ค้าง**~~ — **เสร็จแล้ว 11 ส.ค.** ดึงไฟล์ Figma ทั้งไฟล์ผ่าน **REST API** (43 หน้า · 58,331 node) แทน MCP ที่ติด rate limit ⇒ เทียบสีครบ 142 ค่า · `gray` ทั้งแถบยืนยันแล้ว · เจอผิด `--secondary-600` คนละสี · `tokens.css` v0.3 provenance `✔` 40 · `⚠` 6
- [x] ~~เทียบ radius 43 ค่า + typography 49 ชุด กับ `tokens.css`~~ — **เสร็จ 12 ส.ค.** (หมุด Phase 2) จูน `--fs-*` เข้าสเกลไลบรารี + เพิ่ม `--rounded-sm`
- [ ] **`--error-700` ใช้เป็น destructive hover หรือเปล่า ยังไม่ยืนยัน** — พบว่ามีใช้ 148 ครั้งในไลบรารี แต่ยังไม่รู้ว่าใช้ตรงไหน
- [x] ~~เทียบคอมโพเนนต์กับไลบรารี~~ — **เสร็จ 12 ส.ค.** `design-system/compare-figma.js` + จูน components.css v0.12 ตรงทุกตัว (หมุด Phase 2) · เหลือ: modal (เรายังไม่มี — ถ้าทำอิง `3:9`) · progress steps คงตาม screenshot หน้าจริง (คนละแบบกับไลบรารี ตัดสินใจแล้ว)
- [ ] **เทียบ layout หน้าจอกับ Figma** — ต้องขอลิงก์หน้าที่มีแบบหน้าจอจริง (ลิงก์ที่ได้เป็นไลบรารีคอมโพเนนต์ ไม่มี screen ให้เทียบ)
- [ ] **มือถือ 430px หน้าฟอร์มล้นด้านขวา** — stepper 5 ขั้นดันจนปุ่ม "ถัดไป" โดนตัด (ของเดิม ไม่ได้เกิดจากงาน v0.2 เพราะปุ่มแคบลงไม่ใช่กว้างขึ้น)
- [ ] **โฟลว์บำรุงรักษา — เฟส 2 (ดำเนินการบำรุงรักษา)** คือคิวถัดไป · เงื่อนไขที่ต้องเคาะ 5 ข้อ รวบรวมไว้ใน `plan-skeleton.html` (จอ `ph2`) แล้ว
  - รายคัน/รายแผน · "บำรุงรักษาไม่ได้" ไปทางไหน · รูปก่อน-หลังบังคับไหม · ตัวอย่างน้ำมันไฮดรอลิกทุกคันไหม · ใบ 3 ใบที่พัสดุรับทราบออกจากระบบหรืออัปโหลด
- [ ] **โฟลว์บำรุงรักษา — เฟส 3–5** ยังเป็นหน้าเปล่า (เงื่อนไขอีก 9 ข้อใน skeleton · แยกไว้ท้ายไฟล์ผังของแต่ละเฟสแล้ว)
- [x] ~~Diagram 02–06 ยังนับเป็น "6 ช่วง" ไม่ตรงโครงใหม่~~ — **เสร็จแล้ว 9 ส.ค.** sync ครบ 7 ไฟล์ + เปลี่ยนชื่อไฟล์ให้มีเลขเฟส
- [x] ~~`plan-skeleton.html` ยังครอบแค่ฝั่งออกเลขงาน~~ — **เสร็จแล้ว 9 ส.ค.** ครอบครบ 11 หน้าจอ
- [ ] **`plan-skeleton`** ยังไม่มีปุ่ม "ยกโครงไปหน้าจริง" · ธง `done` ยังตั้งมือใน `skeleton-data.js`
- [x] ~~**figma-export: gen Figma Variables จาก `tokens.css`**~~ — **เสร็จแล้ว 12 ส.ค.** 99 ตัว 3 collection + alias จริง + ผูก fill/stroke/สี text/radius (ดูหมุด 12 ส.ค.)
- [x] ~~**figma-export: ยก component/variant จริง**~~ — **เสร็จแล้ว 12 ส.ค.** 6 ชุด variant + specimen 6 + icon component 10 · หน้าจอเป็น instance 121 จุด + override (ดูหมุด 12 ส.ค.)
- [x] ~~figjam-plugin: กด "โหลด + สร้างบอร์ด" ในไฟล์จริง~~ — **สำเร็จ 12 ส.ค.** ผู้ใช้ยืนยัน "ได้แล้ว ถูกต้อง"
- [x] ~~figjam-plugin: กดรันซ้ำหลังเพิ่ม section ผัง~~ — **สำเร็จ 12 ส.ค.** (หลังแก้บั๊กโหลดฟอนต์ Inter default ของ ShapeWithText)
- [ ] **figjam-plugin: กดรันซ้ำ (ผู้ใช้กดแล้ว 12 ส.ค. เย็น — ยังไม่ได้ยืนยันผลด้วยตา)** — board.json = 4 section (ผัง + สร้างแผน + เดินทาง + แจ้งซ่อม อย่างละ 8 จอ) ภาพ capture เป็นดีไซน์ v0.12 แล้ว (คาด: section 4 · รูป 37 · ลูกศร 37)
- [x] ~~โฟลว์แจ้งซ่อม ฝั่งผู้แจ้ง — capture เส้นหลัก~~ — **เสร็จ 12 ส.ค.** `flow-report-capture.js` 8 หน้าจอ: ขั้น 1 เลือกรถ (ว่าง/เลือกแล้ว+จุดที่พบปัญหา) → อาการเสีย+สถานะรถ+รูป → ติดต่อ/งบ (prefill จังหวัดจากทะเบียน) → อะไหล่แนะนำ → ตัดสินใจ ส่ง กรย.+ผู้อนุมัติ+สรุป → ส่งสำเร็จได้เลขใบ → เรื่องของฉัน "รอ … อนุมัติ" · เจ้าของงานเคาะ: แค่ฝั่งผู้แจ้ง จบที่รอหัวหน้าอนุมัติ ไม่ทำผัง · เกร็ด: เลือกอาการเฉพาะที่มีอะไหล่ (ขั้นอะไหล่ไม่ว่าง) + radio จุดที่พบปัญหาต้องคลิกที่ label (input ถูกบัง)
- [x] ~~โฟลว์วางแผนการเดินทาง — capture เส้นหลัก~~ — **เสร็จ 12 ส.ค.** `flow-trips-capture.js` 8 หน้าจอ: ขั้น 3 ว่าง → แยกตามจังหวัด → กรอกใบ (วันนัดรายคันบังคับในช่วง) → ส่ง "รอตอบรับ"+ฟอร์มล็อก → หน่วยงานเปิดแผนนัด/ตอบรับ → กลับ กบค. ตอบรับครบ ปุ่มถัดไปติด · fast-forward gate ยืนยันรถแบบ `test/verify-trips.js` · เจ้าของงานสั่ง: เส้นหลักอย่างเดียว ไม่เอากิ่งปฏิเสธ ไม่ทำผัง
- [x] ~~โฟลว์แจ้งซ่อมเข้าท่อ design~~ — **เสร็จ 12 ส.ค. เย็น** `flow-report-extract.js` + `2-map.js --report` → `spec-report.json` เทสผ่าน 12/12 (ดูหมุด "ท่อ design กลับมาใช้")
- [x] ~~**figma-export (design): กด "โหลด + สร้าง" ให้ได้โฟลว์แจ้งซ่อม**~~ — **สำเร็จ 12 ส.ค. เย็น** ผู้ใช้ยืนยัน "เห็นแล้ว" (รอบแรกไม่ขึ้นเพราะปลั๊กอินยังไม่ได้โหลดไฟล์สเปกเข้า + ค่าเริ่มต้นเดิมชี้ `spec.json` เลยได้ชุด yearly ⇒ แก้ default ใน `plugin/ui.html` เป็น `spec-report.json` แล้ว) — **ครั้งแรกที่ท่อ design รันในไฟล์จริง** ได้หน้า Screens — แจ้งซ่อม (ฝั่งผู้แจ้ง) 8 เฟรม + Foundations + Variables 99
- [ ] **เทียบตาไฟล์ design กับต้นฉบับ** — ยังไม่ได้ไล่ดูทีละเฟรมเทียบ `out/shot-report-01..08.png` (mock จับตรรกะได้ แต่ฟอนต์/auto-layout/ขนาด svg ต้องดูของจริง) · เกณฑ์ตาดูอยู่ในข้อ 6 ของ design doc
- [ ] **DS audit ของ spec แจ้งซ่อม (12 ส.ค. เย็น) — ของหลุด token ใน components.css เอง 3 จุด**: `.draft` สี `#93370D` (ควรเป็น `--warning-800`) · `.work` พื้น `#FCFCFD` (ควรเป็น `--gray-25`) · `border-radius:10px` 38 จุด ไม่มี token (tokens มี 8/12/16/99) ตรงกับ diff ไลบรารี PEA (input h46→44 · r10→8) — แก้รวมใน Phase 2 · ที่เหลือตรงหมด: สี 26/30 ผูก variable ครบ, ฟอนต์ IBM Plex Sans Thai 366/366, instance 144 จุด (radius 50px ในรายงาน = วงกลม `50%` เป็น artifact ตัวแปลง ไม่ใช่ของผิด)
- [ ] **figma-export: หน้า `admin`** — extract ไว้แล้ว (3,770 node) แต่ยังไม่ได้ map/สร้าง อาจต้องตัดเป็นส่วนๆ
- [ ] **figma-export: `plan-skeleton`** — พักไว้ตามที่เจ้าของงานสั่ง 11 ส.ค. รอปลดล็อก
- [x] ~~หน้าจริงของ "ยืนยันรถเข้าร่วมแผน"~~ — **เสร็จแล้ว 10–11 ส.ค.** ทำครบทั้ง 2 ฝั่ง (กบค. + `confirm.html`) · เคาะ 4 ข้อกับเจ้าของงานแล้ว
- [ ] **อะไหล่ผูกกับชนิดรถ ไม่ใช่ยี่ห้อ** (`appliesToBrands`) — จัดกลุ่มตามยี่ห้อได้แล้ว แต่รายการอะไหล่ของยี่ห้อที่เป็นรถชนิดเดียวกันยังเหมือนกัน
- [ ] **ผบพ. / กบท. คือใคร · เกณฑ์ทรัค-เนต · สูตร SUM ต้นทุน** — ยังไม่เคาะ
- [ ] **ต่อ `GetMasterMatWiSto` จริง** — เขียน adapter + เปลี่ยนโครง `stock`/`stockAlt` เป็น `locations[]` · รอคำตอบ 3 ข้อจากเจ้าของ API ก่อน
- [ ] **หน้าเบิกอะไหล่ (เส้น กบค. ซ่อมเอง) → เลย์เอาต์ 2 คอลัมน์ + stepper 6 ขั้น** — สเปกเขียนแล้ว `docs/superpowers/specs/2026-08-13-kbk-self-repair-parts-design.md` (13 ส.ค. 2569) ยกโครงจาก `design-mock/kbk-self-repair-parts.html` · เจ้าของงานเคาะแล้ว 5 ข้อ: แทนเนื้อ+เปลี่ยนเป็น 6 ขั้นเฉพาะเส้นซ่อมเอง · เก็บป้าย "ผู้แจ้งเลือกมา"+ตัวกรองรุ่นอุปกรณ์ · ตัดปุ่มประเมินความคุ้มค่า+กล่อง TODO · ตรรกะใหม่ `onHand=stock+stockAlt` เกินยอด=จัดซื้อส่วนต่าง · เรียง state ใหม่ให้นัดหมายมาก่อนซ่อม (⇒ ต้องแก้ `Diagram/02-แจ้งซ่อม-กบค/` ในคอมมิตเดียวกัน) · **ยังไม่เริ่มเขียนโค้ด**
- [x] ~~**figma-export: chevron ของ stepper ออกมาผิด**~~ — **แก้แล้ว 13 ส.ค.** (เจ้าของงานเลือกทาง ก) สาเหตุ: `convertPseudo()` ทิ้ง `transform` + ฮาร์ดโค้ดตำแหน่งเป็นมุมซ้ายบนของ host · แก้ด้วยการเพิ่มคีย์ `rotation`/`rotateOrigin`/`absolute` เข้าสเปก · `dom-walk.js` เก็บ `position`/`top`/`right`/`bottom`/`left`/`transformOrigin` เพิ่ม · ปลั๊กอินตั้ง `layoutPositioning='ABSOLUTE'` แล้วชดเชยจุดหมุนด้วย `P = P0 + o − R·o` (Figma หมุนรอบมุมซ้ายบน CSS หมุนรอบ transform-origin) · เทส `test-pseudo-geom.js` 12 ข้อ + `test-plugin.js` 3 ข้อใหม่ (ผ่านทั้ง `spec.json` 4 เส้น และ `spec-report.json` 6 เส้น) · **ค้าง: เจ้าของงานกด "โหลด + สร้าง" ในไฟล์ Figma จริงเพื่อยืนยันด้วยตา**
  <br>เดิม: ในไฟล์ Figma เส้นคั่น `.wstep` กลายเป็นแท่งตรงแนวตั้ง 2 แท่งวางหน้าเลขขั้น แทนที่จะเป็นลูกศรที่ขอบขวา · หาสาเหตุครบแล้ว 13 ส.ค.: `dom-walk.js` เก็บ `transform: matrix(…)` (= `rotate(∓16°)`) มาครบ แต่ `convertPseudo()` (`2-map.js:464`) ทิ้งทั้ง**มุมหมุน** (สเปกไม่มีคีย์ `rotation` · `plugin/code.js` ไม่รองรับ) และ**ตำแหน่ง** (บรรทัด 481 ฮาร์ดโค้ดเป็นมุมซ้ายบนของ host ทิ้ง `position:absolute; right:0`) · `2-map.js:311` ใส่ `spec.pos` ให้เฉพาะตอนพ่อเป็น `layout.mode:'NONE'` แต่ `.wstep` เป็น auto-layout · เสนอ 3 ทาง (ก รองรับ rotation+absolute จริง · ข ยุบเป็น svg ชิ้นเดียว · ค ไม่ export เส้นที่หมุน) — เจ้าของงานเคาะ **ทาง ก**
- [ ] **โฟลว์ กบค. ยังไม่อยู่ในท่อ export เลย** — capture ที่มี 4 ชุดเป็นฝั่งผู้แจ้ง/yearly ทั้งหมด (`flow-report-capture.js:10` เขียนไว้เองว่า "สายอนุมัติ หัวหน้า → กรย. → กบค. ไม่อยู่ในโฟลว์นี้") · ถ้าจะส่งหน้าเบิกอะไหล่เข้า Figma ต้องเขียน `flow-kbk-capture.js` ใหม่ + เพิ่ม section ใน `3-figjam-board.js` · ท่อ design ต้องมี `flow-kbk-extract.js` + `2-map.js --kbk` ด้วย · `4-figjam-diagram.js:22` ฮาร์ดโค้ดชี้ผัง yearly ต้องทำให้รับ path เป็นอาร์กิวเมนต์ก่อน · **ควรทำหลังหน้าจอ+flow นิ่งแล้ว** ไม่งั้น capture ทิ้ง

---

## Verification รวม (ทุกหมุด)

- Playwright + Chrome ติดเครื่อง ตาม skill `.claude/skills/verify` — regression 14 จุด (หน้า 05 render 6 การ์ด · deep-link ทั้ง 6 · seed ไม่กระทบ default · ไม่มี JS pageerror)
- เดิน happy path M1→M2→M6 ให้เห็นสถานะฝั่งผู้แจ้ง (`#my`) mirror กับฝั่ง กบค.
