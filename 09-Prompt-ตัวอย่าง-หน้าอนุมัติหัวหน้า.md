# 📋 ตัวอย่าง Prompt พร้อมใช้ — หน้าอนุมัติของหัวหน้า (`#view-boss`)

> ใช้โครง 9 บล็อกจาก [`08-Prompt-Structure.md`](08-Prompt-Structure.md)
> เลือกงานนี้เพราะเป็นข้อค้างล่าสุดใน `plan.md`:
> *"ยังเหลือ: หน้าอนุมัติของหัวหน้า (view-boss) ยกเป็นโครงหน้ารายละเอียดเหตุการณ์ + บล็อกช่องทางการซ่อม ตามพรีวิว approver-incident-detail.html"*

**ก๊อปทั้งบล็อกข้างล่างไปวางได้เลย**

---

````markdown
## ① CONTEXT
ระบบซ่อมบำรุงรถ Maintain-D (ต้นแบบ static HTML, ดีไซน์อิง VMS Plus ของ กฟภ.)
ผู้ใช้หน้านี้: **หัวหน้าของผู้แจ้ง** — ใบแจ้งซ่อมที่ผู้แจ้งส่งมาจะค้างที่นี่ก่อนไปถึง กรย./กบค.
เขาเปิดหน้านี้เพื่อดูรายละเอียดเหตุการณ์ ตัดสินช่องทางการซ่อม แล้วกดอนุมัติ/ไม่อนุมัติ
ตอนนี้หน้ารายละเอียด (`#bossdetail`) ยังเป็นบล็อกสรุปแบบเก่า ไม่ตรงกับหน้าจริงของระบบ

## ② READ FIRST — อ่านให้ครบก่อน ห้ามเขียนโค้ดระหว่างนี้
- `CLAUDE.md` — กติกาบังคับทั้งหมด โดยเฉพาะ "สเต็ปบังคับก่อนออกแบบ/แก้ UI" 9 ขั้น
- `design-system/README.md` หัวข้อ 0 (ขั้นตอนบังคับ) + หัวข้อ 4 (ตารางคอมโพเนนต์ คอลัมน์ "ใช้เมื่อ")
- `design-system/components.css` — คลาสจริงที่มีให้ใช้
- `design-mock/approver-incident-detail.html` — **พรีวิวที่เจ้าของงานเคาะแล้ว** คือแบบที่ต้องยกมา
- `mock/Maintenance-Request-Form.html`
  - บรรทัด ~2026–2058 → `#view-boss` ปัจจุบัน (crumbs · หัวข้อ · `#bosstools` ค้นหา+ตัวกรอง · `#bosslist` · `#bossdetail`)
  - บรรทัด ~4934–4990 → `renderBossList()` · `openBoss()` · `renderBossDetail()` ที่ต้องแก้
- `plan.md` หัวข้อ "ค้างอยู่" ข้อโฟลว์แจ้งซ่อมแบบใหม่ (3–4 ก.ย. 2569) — บริบทว่าทำไมถึงต้องยก

เสร็จข้อนี้แล้ว **สรุปให้ดูก่อน** ว่าจะใช้คลาสอะไรบ้าง จะแตะไฟล์ไหนบ้าง แล้วรอเคาะ

## ③ GOAL
หัวหน้ากดแถวใน `#bosslist` แล้วเห็นหน้ารายละเอียดเหตุการณ์แบบเดียวกับระบบจริง
เลือก/แก้ **ช่องทางการซ่อม** (ซ่อมเอง / ส่งซ่อมอู่ / ส่งต่อให้ กรย.) ได้ก่อนกดอนุมัติ
และช่องทางที่เลือกถูกบันทึกติดใบงานไปกำหนดปลายทางของขั้นถัดไป

## ④ SCOPE
| ทำ | ไม่ทำ |
|---|---|
| ยกโครง `#bossdetail` ให้เป็นหน้ารายละเอียดเหตุการณ์ตามพรีวิว | แตะฝั่ง กบค. (`#kbk`) หรือ กรย. |
| บล็อก "ช่องทางการซ่อม" ที่เลือก/แก้ได้ + บันทึกลงใบงาน | สร้าง flow ใหม่ของ "ส่งซ่อมอู่" (แค่บันทึกช่องทาง) |
| แท็บ ข้อมูลการซ่อม / ประวัติการดำเนินการ | ระบบสิทธิ์รายบุคคลว่าใครกดอนุมัติได้ (ยังไม่เคาะ) |
| ปุ่มอนุมัติ / ไม่อนุมัติ + ผลต่อสถานะใบ | ต่อ API จริง |

## ⑤ SCREEN
โครงตาม `design-mock/approver-incident-detail.html` (ตัดแถบ `.zone-note` สีเหลืองออก — เป็นป้ายพรีวิว)

**หัวหน้า**
- breadcrumb: หน้าแรก › งานที่รอฉันอนุมัติ › หมายเลขเหตุการณ์ MTD-…
- `.incident-head`: `<h1>` หมายเลขเหตุการณ์ + `.badge` สถานะ + `.incident-actions` ปุ่ม
  `[ไม่อนุมัติ .btn.btn-td]` `[อนุมัติ .btn.btn-p]`
- แท็บ `.tabs` 2 อัน: **ข้อมูลการซ่อม** (default) · **ประวัติการดำเนินการ**

**แท็บ 1 — `.incident-layout` สองคอลัมน์**
คอลัมน์ซ้าย (`.incident-section` เรียงลง):
1. **ข้อมูลการซ่อม** — `dl.incident-summary`: วันที่แจ้ง · จุดที่พบปัญหา · อาการเสีย (ตามจุด) · ความรุนแรง · คำอธิบาย
2. **ช่องทางการซ่อม** ← ของใหม่ของงานนี้
   - บรรทัดบอกที่มา: "ผู้แจ้งเสนอ: **<ช่องทางที่ผู้แจ้งเลือก>** — หัวหน้าปรับเปลี่ยนได้ก่อนกดอนุมัติ"
   - `.radcards.cols-3` 3 ใบ (radio name="route"), ค่าเริ่มต้น = ที่ผู้แจ้งเสนอ:
     | ค่า | หัวข้อ | คำอธิบายย่อย | ไอคอน |
     |---|---|---|---|
     | `self` | ซ่อมเอง | ซ่อมได้ที่หน้างาน/หน่วยงาน | `build` |
     | `garage` | ส่งซ่อมอู่ | ส่งอู่ประเมินราคาก่อนซ่อม | `garage_home` |
     | `kry` | ส่งต่อให้ กรย. | ให้ กรย. คัดแยกช่องทางซ่อม | `alt_route` |
   - เปลี่ยนจากที่ผู้แจ้งเสนอ → ต้องกรอกเหตุผล (required ก่อนกดอนุมัติ)
3. **อะไหล่ที่ผู้แจ้งเลือกไว้** — รายการ + จำนวน + คลัง + ราคา + แถวรวม (เบิกจากคลัง / จัดซื้อ)
4. **รูปภาพเหตุการณ์** — แกลเลอรี ถ้าไม่มีรูปให้ขึ้นข้อความ ไม่ใช่กรอบเปล่า

คอลัมน์ขวา: **ยานพาหนะ** · **ผู้แจ้งเหตุ** · **ข้อมูลติดต่อหน่วยงานที่แจ้งซ่อม**

**แท็บ 2 — ประวัติการดำเนินการ**: timeline เวลา · ผู้ทำ · การกระทำ (สร้างใบ → ส่งอนุมัติ → …)

**พฤติกรรม**
- กด **อนุมัติ** → บันทึก route + ผู้อนุมัติ + เวลา → สถานะเปลี่ยนตามช่องทาง → กลับไป `#bosslist`
  พร้อม toast แล้วใบหายจากตัวกรอง "รอฉันอนุมัติ"
- กด **ไม่อนุมัติ** → บังคับกรอกเหตุผล → สถานะ "ไม่อนุมัติ — ตีกลับ" → ผู้แจ้งเห็นเหตุผลใน "เรื่องของฉัน"
- กดย้อนกลับ → ซ่อน `#bossdetail` โชว์ `#bosstools` + `#bosslist` เหมือนเดิม

**สถานะที่ต้องมี**: ไม่มีใบรออนุมัติ (empty ในลิสต์) · ใบไม่มีอะไหล่ผูก · ใบไม่มีรูป ·
เปลี่ยนช่องทางแล้วยังไม่กรอกเหตุผล (ปุ่มอนุมัติ disable + ชี้ฟิลด์) · จอ 430px คอลัมน์ยุบเป็นเดียว

## ⑥ DATA
ใช้ใบงานจาก `job-store.js` (key `maintaind.jobs.v1`) และ mock ที่มีอยู่เท่านั้น
รถ/อาการ/อะไหล่ ยึด `02-Flow-and-Mock-Data.md` — **ห้ามสร้างทะเบียนหรือรหัสอะไหล่ใหม่**
ฟิลด์ใหม่ที่จะเพิ่มในใบงาน ให้เสนอชื่อคีย์มาก่อน (เช่น `j.route`, `j.routeReason`, `j.approve`)
ต้องไม่ชนของเดิมและไม่ทำให้ตัวกรองสถานะที่มีอยู่พัง

## ⑦ RULES
- ทำตาม `CLAUDE.md` ทุกข้อ — สรุปที่กระทบงานนี้:
  - **คลาสที่มีใน `components.css` แล้วให้ใช้เลย**: `.incident-layout` `.incident-section`
    `.incident-summary` `.incident-gallery` `.tabs/.tab-btn` `.radcards/.radcard` `.badge` `.btn*`
  - 🔴 **`.route-hint` และ `.parts-mini` ยังมีแค่ในไฟล์พรีวิว ไม่มีใน `components.css`**
    → ต้องเช็คไลบรารี Figma ก่อนว่ามี component ที่ตรงไหม
    (`node design-system/figma-screens.js component "<ชื่อ>" --deep`)
    ถ้ามี → ยกค่าจากไลบรารีเข้า `components.css` · **ถ้าไม่มี → หยุดถามก่อน ห้ามออกแบบเอง**
  - แก้ `components.css`/`tokens.css` → บั๊ม `?v=` ทุกหน้า + Changelog + ตารางหัวข้อ 4
  - ห้าม hardcode hex · ห้าม emoji แทนไอคอน (ใช้ `<span class="ms">`) · light mode เท่านั้น
  - `<style>` ในหน้าเก็บได้เฉพาะของเฉพาะหน้าจริงๆ และต้องมีคอมเมนต์เหตุผล
  - วางคอมโพเนนต์ต่อกันแนวตั้ง → ห่อ `.stack` เสมอ
- static HTML ไม่มี build step · state ใน localStorage · ทดสอบผ่าน `http://127.0.0.1:8123` ห้าม `file://`
- ถ้าลำดับขั้น/ผู้รับผิดชอบเปลี่ยน → แก้ผัง `Diagram/02-แจ้งซ่อม-กบค/` **ในคอมมิตเดียวกัน**

## ⑧ DONE — ต้องรันจริงและรายงานผลจริง
1. `node design-system/verify-tokens.js` ผ่าน
2. `node design-system/audit-usage.js` ไม่มี error
3. grep hex + grep emoji (สองคำสั่งท้าย `CLAUDE.md`) ได้ผล **ว่างทั้งคู่**
4. `node mock/test/flow-regression.js` ผ่านครบ (ฐานเดิม 58 ข้อ — ถ้าจำนวนเปลี่ยนให้บอกว่าเพิ่มเทสอะไร)
5. เดินมือบนเซิร์ฟเวอร์: ผู้แจ้งส่งใบ → เข้า `#view-boss` → เปิดใบ → เปลี่ยนช่องทาง → อนุมัติ
   → ใบหายจาก "รอฉันอนุมัติ" และช่องทางใหม่ติดไปกับใบ → **รีโหลดแล้วค่ายังอยู่**
6. console **ไม่มี pageerror** · ลิงก์ทุกอันไม่ 404 · จอ 430px ไม่ล้นแนวนอน
7. push แล้ว → อัปเดตหัวข้อ "ค้างอยู่" ใน `plan.md` ให้ตรง

## ⑨ ORDER
1. อ่านไฟล์ในข้อ ② ให้ครบ
2. **สรุปมาก่อนเขียนโค้ด**: คลาสที่จะใช้ (แยกว่าอันไหนมีแล้ว/อันไหนต้องเช็คไลบรารี) ·
   ไฟล์ที่จะแตะ · ชื่อคีย์ใหม่ในใบงาน · assumption ที่ตั้งไว้ · สิ่งที่ยังไม่แน่ใจ
3. รอเคาะ แล้วค่อยลงมือ
4. เจอของที่ไลบรารีไม่มี (คาดว่าเจอที่ `.route-hint` / `.parts-mini`) → **หยุดถาม ห้ามออกแบบเอง**
5. ทำเสร็จ → รันข้อ ⑧ ทั้ง 7 ข้อ แล้วรายงานผลจริงทีละข้อ ไม่ใช่บอกว่า "น่าจะผ่าน"
````

---

## หมายเหตุ — ตัวเลขและชื่อในนี้ตรวจกับโค้ดจริงแล้ว

| อ้างอิงใน prompt | ตรวจจากไหน |
|---|---|
| `#view-boss` มี `#bosstools` / `#bosslist` / `#bossdetail` | `mock/Maintenance-Request-Form.html` ~2026–2058 |
| `renderBossList()` · `openBoss()` · `renderBossDetail()` | ไฟล์เดียวกัน ~4934–4990 |
| 7 section + 2 แท็บ + 3 การ์ดช่องทาง | `design-mock/approver-incident-detail.html` |
| `.incident-*` `.tab-btn` `.radcard` **มีแล้ว** ใน `components.css` | grep แล้วเจอทั้งหมด |
| `.route-hint` `.parts-mini` **ยังไม่มี** ใน `components.css` | grep แล้วเจอเฉพาะในไฟล์พรีวิว |
| ฐาน flow-regression 58 ข้อ | `plan.md` หมุด 4 ก.ย. 2569 (v0.55) |
